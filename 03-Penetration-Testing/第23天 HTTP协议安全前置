## HTTP协议安全前置
### 1. URL完整结构
`协议://域名:端口/路径?参数`

### 2. 核心请求头安全作用
- `User-Agent`：标识客户端浏览器/设备类型
- `Cookie`：维持会话、标识用户登录身份
- `Referer`：标识请求来源页面，可做防盗链、CSRF防护

### 3. GET 与 POST 区别
- GET：参数显式在URL、长度有限、适合查询
- POST：参数请求体提交、隐式传输、适合提交数据/登录上传

### 4. 高频安全状态码
- 200：请求正常成功
- 302：页面重定向
- 403：权限不足、禁止访问
- 404：资源/页面不存在
- 500：服务器程序内部错误

### 5. HTTP特性
协议**无状态**，依靠Cookie维持用户会话状态。
