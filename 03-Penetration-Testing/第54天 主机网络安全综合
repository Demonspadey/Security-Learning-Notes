## 一、核心目标
1. 熟练掌握主机安全加固的核心措施与实操命令
2. 掌握Windows/Linux系统日志分析的关键命令与事件ID
3. 清晰区分HIDS/NIDS/IPS的部署位置与工作模式
4. 掌握数据脱敏、备份、容灾的实操规范

---

## 二、主机安全加固实操
### 1. 账户与密码安全
- **禁用/清理**：禁用Windows `Administrator`/Linux `root` 默认管理员，清理离职闲置「僵尸账号」
- **密码策略**：长度≥8位（大小写+数字+特殊字符）、90天定期更换、输错5次锁定账号（防爆破）
- **文件权限（Linux）**：`/etc/shadow` 标准权限 `600`（仅root可读，禁止777等高风险配置）

### 2. 服务与端口管控
- 关闭无用系统服务、闲置端口，**缩小攻击面**
- 防火墙配置访问控制策略，限制非必要外部访问

---

## 三、系统日志分析实操
### 1. Windows日志
- 查看工具：`eventvwr.msc` 事件查看器
- 核心事件ID：
  - `4624`：账户登录成功
  - `4625`：账户登录失败
- Powershell查询命令：
  ```powershell
  # 筛选登录成功日志
  Get-WinEvent -FilterHashtable @{LogName='Security'; Id=4624} -MaxEvents 20
  # 筛选登录失败日志
  Get-WinEvent -FilterHashtable @{LogName='Security'; Id=4625} -MaxEvents 20
- 爆破IP统计命令：
grep "Failed password" /var/log/secure | awk '{print $(NF-3)}' | sort | uniq -c | sort -nr
