## 一、核心目标
1. 掌握Android四层安全机制、四大组件风险与数据存储方式
2. 掌握Windows隐藏账户、账户克隆的原理与排查方法
3. 熟记应急响应PDCERF六步标准流程与各阶段核心动作
4. 能执行主机入侵排查基础命令，完成账号风险检测

---
## 二、核心理论考点
### 1. Android安全核心体系
#### （1）四层安全机制
| 层级 | 对应安全机制 |
|------|--------------|
| 应用程序层 | 应用程序签名机制 |
| 应用框架层 | 权限声明机制 |
| 系统运行库层 | 安全沙箱 |
| Linux内核层 | ASLR地址空间布局随机化 |

#### （2）四大组件与对应风险
| 组件 | 核心作用 | 典型攻击风险 |
|------|----------|--------------|
| Activity | 页面UI交互 | 界面劫持攻击 |
| BroadcastReceiver | 接收系统广播 | 短信拦截、隐私窃取 |
| ContentProvider | 跨应用数据共享 | 目录遍历、敏感数据泄露 |
| Service | 后台常驻服务 | 权限提升、恶意后台行为 |

#### （3）核心规则
- 权限配置文件：`AndroidManifest.xml`
- 权限核心原则：最小必要原则，仅申请业务必需权限
- 五种数据存储：SharedPreferences、文件存储、SQLite数据库、ContentProvider、网络远程存储
- 组件风险：`exported=true`无权限校验时，可被恶意应用跨进程调用

### 2. Windows主机入侵排查
#### （1）隐藏账户
- 特征：用户名以`$`结尾，`net user`命令默认不显示
- 检测方法：
  1. `lusrmgr.msc`本地用户和组管理
  2. 注册表SAM路径查看：`HKEY_LOCAL_MACHINE\SAM\SAM\Domains\Account\Users`
- 管理员默认RID：`000001F4`

#### （2）账户克隆攻击
- 原理：将管理员账户的F键值复制到普通隐藏账户，使隐藏账户获得管理员权限
- 核心操作：复制注册表中的**F键值**（存储用户权限配置）
- 危害：隐蔽性极强，常规命令无法发现，用于内网权限维持

### 3. 应急响应PDCERF标准流程
1. **准备阶段**：制定应急预案、定期备份、组建响应团队
2. **检测阶段**：日志/流量溯源，确定攻击类型、范围与根源
3. **抑制阶段**：断网隔离、封禁攻击IP、关停风险服务，阻止危害扩大
4. **根除阶段**：清理后门木马、修补系统与代码漏洞、清除恶意账户
5. **恢复阶段**：干净备份回滚、校验数据完整性、逐步恢复业务
6. **跟踪阶段**：事件复盘、更新预案、安全培训、持续监控

---
