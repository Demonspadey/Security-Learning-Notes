## 一、核心目标
1.  彻底吃透序列化、反序列化基础概念与运行逻辑
2.  掌握反序列化漏洞定义、成因、危害、防护四大核心考点
3.  理解PHP/Java/Python反序列化触发机制与魔术方法作用
4.  建立「可控输入+自动调用方法+无校验=漏洞成立」的核心安全思维

---

## 二、核心原理精讲

### 1. 基础概念
- **序列化**：将程序对象、数据结构转换为字节流/字符串，用于存储、传输、持久化。
- **反序列化**：将字节流/字符串还原为程序可识别、可调用的对象。

### 2. 漏洞核心定义
程序接收用户可控的外部序列化数据，在反序列化还原对象过程中，未做任何合法性校验，导致攻击者构造的恶意代码被自动执行，形成高危应用层漏洞。

### 3. 漏洞三大根本成因
1.  **输入完全可控**：序列化数据来自客户端Cookie、参数、上传文件等，用户可任意篡改构造。
2.  **自动调用危险魔术方法**：反序列化还原对象时，会自动执行类内置方法：
    - PHP：`__wakeup`、`__destruct`
    - Java：`readObject`
    - Python：`__reduce__`
3.  **无安全校验机制**：未校验数据来源、未校验数据完整性、无白名单类限制、无恶意内容检测。

### 4. 漏洞核心危害
1.  **远程代码执行RCE**：最高危，可直接在服务器执行任意系统命令。
2.  **任意文件操作**：读取、写入、删除服务器本地文件。
3.  **权限提升与持久化**：获取系统最高权限，植入后门长期控制。
4.  **内网横向渗透**：以被控制服务器为跳板，攻击内网其他业务系统。
5.  **数据泄露与业务瘫痪**：窃取核心业务数据，造成业务系统中断。

### 5. 标准防护核心逻辑
- 优先采用JSON/XML等纯文本数据格式，避免不可信的反序列化操作
- 禁止反序列化所有不可信外部输入
- 配置安全类白名单，仅允许解析系统预设可信对象
- 序列化数据采用加密+数字签名校验，防止篡改伪造
- 清理、禁用代码中危险魔术方法，及时升级漏洞组件

---
