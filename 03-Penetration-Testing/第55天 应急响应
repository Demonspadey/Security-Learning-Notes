## 一、核心目标
1.  熟记应急响应六阶段的核心流程与各阶段典型操作，无流程顺序混淆
2.  掌握 MTTD/MTTR 的概念、计算规则与安全运营核心目标，规避计算易错点
3.  熟练区分 Linux 应急常用命令（日志/防火墙/文本处理）的用途与易错参数
4.  规范典型安全事件（勒索病毒/网页篡改/DDoS/暴力破解）的处置流程与禁忌操作
5.  建立应急响应合规意识，掌握取证红线与规范处置要求

---

## 二、核心考点整理
### 1. 应急响应标准六阶段（必考，附阶段边界区分）
| 阶段 | 核心定位 | 典型操作 | 阶段边界区分（易混点） |
|------|----------|----------|------------------------|
| 准备阶段（事前） | 预防+预案 | 制定应急预案、全量数据备份、储备应急工具、人员培训 | 事件发生前的所有工作，不属于处置流程本身 |
| 检测阶段 | 发现+确认 | 分析日志、溯源攻击路径、判定事件类型与影响范围、确认安全事件 | 必须完成“事件确认”，才算进入下一阶段 |
| 抑制阶段（控扩散） | 阻止攻击扩大、控制影响范围 | 封禁攻击IP、断网隔离受感染主机、关停高危服务、下线篡改页面 | 核心是“控扩散”，不解决根源问题 |
| 根除阶段（清威胁） | 彻底清除攻击根源 | 清除木马/恶意程序、删除隐藏账号、修补系统漏洞、清理后门 | 核心是“除根源”，必须在抑制完成后进行 |
| 恢复阶段（复业务） | 恢复正常服务与业务 | 使用离线可信备份还原数据、重启业务服务、验证服务可用性 | 必须验证服务正常，才算完成恢复 |
| 跟踪总结阶段（事后） | 复盘+优化 | 持续监控系统状态、复盘事件全过程、更新安全策略、优化应急流程 | 处置完成后的所有工作，属于闭环管理 |

> 标准流程顺序：**准备 → 检测 → 抑制 → 根除 → 恢复 → 跟踪总结**
> 易错点提醒：修补漏洞属于**根除阶段**，不属于抑制阶段；事后复盘属于**跟踪总结阶段**，不属于恢复阶段。

### 2. MTTD/MTTR 安全运营指标（高频计算+概念，附易错点）
#### （1）核心概念与公式
- **MTTD（平均检测时间，Mean Time To Detect）**
  公式：`发现事件时间 − 事件发生时间`
  含义：从攻击发生到被安全设备/人员发现的时长；数值越小，检测能力越强
- **MTTR（平均恢复时间，Mean Time To Respond/Recover）**
  公式：`业务完全恢复时间 − 发现事件时间`
  含义：从发现问题到业务完全恢复正常的时长；数值越小，处置恢复效率越高
- 事件总时长：`业务恢复时间 − 事件发生时间`（= MTTD + MTTR）

#### （2）易错点复盘（结合本次错题）
- 错误表述1：“MTTR的终点是管理员发现事件时” → 纠正：MTTR的终点是**业务完全恢复正常时**
- 错误表述2：“事件总时长=MTTR” → 纠正：总时长=MTTD+MTTR
- 错误表述3：“增大MTTD、减小MTTR是安全运营目标” → 纠正：核心目标是**同时压低MTTD、MTTR**

#### （3）优化方法
- 缩短MTTD：搭建日志审计系统、部署IDS/IPS、定期安全巡检
- 缩短MTTR：优化应急流程、定期应急演练、储备标准化处置脚本

### 3. Linux 应急常用命令（日志/防火墙/文本处理，附易错参数）
#### （1）日志相关（SSH登录日志高频考点）
- CentOS系统SSH登录认证日志默认路径：`/var/log/auth.log`
- 登录失败专属关键字：`Failed password`
- 基础查看命令：
  - `head -n 20 auth.log`：查看日志文件前20行（开头内容）
  - `tail -n 20 auth.log`：查看日志文件后20行（末尾内容）
  - `tail -f auth.log`：实时监控日志新增内容

#### （2）文本统计与排序（易错重点，结合本次错题）
- 核心规则：`uniq -c`仅能统计相邻重复内容，**必须先用`sort`全局排序，才能统计所有重复项**
- 易错参数解析：
  - `sort -nr`：`-n`代表按数字大小排序，`-r`代表倒序排列，整体功能为**按数字倒序排列**
  - 示例：`cat auth.log | grep "Failed password" | awk '{print $11}' | sort -nr | uniq -c` → 统计并排序登录失败次数最多的IP

#### （3）防火墙与流编辑器命令
- iptables防火墙命令：
  - `iptables -L`：查看当前所有防火墙规则
  - `iptables -A INPUT -s 192.168.1.100 -j DROP`：新增封禁IP规则
  - `iptables -F`：清空所有防火墙规则（应急场景慎用）
- 流编辑器：`sed`，可批量编辑、删除日志内容，例如：`sed '/Failed password/d' auth.log`（删除包含登录失败的行，仅用于测试，真实取证场景严禁篡改日志）

### 4. 典型安全事件应急处置方案（附禁忌操作）
| 事件类型 | 标准处置流程 | 核心操作 | 禁忌操作 |
|----------|--------------|----------|----------|
| 勒索病毒攻击 | 断网隔离 → 备份原始数据 → 评估影响范围 → 清除恶意程序 → 恢复业务 | 第一时间断开受感染主机网络，防止横向传播 | 直接重启主机、格式化全盘、打开加密文件、删除原始日志 |
| 网站被篡改 | 下线问题页面 → 关停Web服务 → 备份原始日志 → 溯源攻击路径 → 修复漏洞 → 恢复业务 | 优先下线篡改页面，阻断恶意内容传播 | 清空所有日志、直接删除篡改文件、忽略备份直接恢复 |
| 账号暴力破解 | 封禁攻击IP → 开启账号登录锁定 → 修改服务端口 → 加固账号安全 → 排查弱口令 | 常规抑制手段为封禁IP、账号锁定，无需全盘格式化 | 全盘格式化系统磁盘、直接删除日志、修改系统关键配置 |
| DDoS攻击 | 流量清洗 → 防火墙限流 → 扩容带宽 → 溯源攻击源 → 优化防护策略 | 采用流量清洗、防火墙限流缓解攻击 | 直接断电关机、关闭所有安全防护组件、删除系统日志 |

### 5. 应急合规与专业术语（取证红线）
- **SOAR（Security Orchestration, Automation and Response）**：安全编排自动化与响应，用于自动化应急处置流程
- 应急预案：事前编制，核心作用包括明确分工与上报流程、划分事件响应等级、规范日常应急要求，**不支持事发后临时编写**
- 取证红线：原始日志是安全事件取证的法定依据，**严禁随意篡改、删除原始日志**；处置过程需分步记录、留存完整处置记录

---
