## 一、核心目标
1. 掌握SQL注入、XSS、CSRF、路径遍历、命令注入五大高频攻击的原理与特征
2. 熟记每类攻击的核心危害与最优防护方案
3. 能通过日志关键字快速识别攻击类型
4. 掌握基础的攻击检测命令与排查思路

---
## 二、五大攻击核心知识点汇总
### 1. SQL注入
- **核心原理**：Web应用未过滤用户输入，恶意字符拼接进SQL语句，篡改原有查询逻辑
- **典型特征**：`' OR '1'='1`、`UNION SELECT`、`--`、`#`
- **核心危害**：数据库脱库、越权登录、数据篡改/删除、权限提升
- **核心防护**：
  1. 参数化查询（预编译语句，最根本手段）
  2. 输入白名单校验
  3. 数据库账号最小权限原则
  4. 屏蔽数据库报错信息

### 2. XSS跨站脚本攻击
- **核心原理**：恶意脚本注入网页，在用户浏览器端执行
- **三大分类**：反射型（单次触发）、存储型（持久化，危害最大）、DOM型（纯客户端）
- **典型特征**：`<script>`、`alert()`、`onerror`、`javascript:`
- **核心危害**：窃取Cookie、会话劫持、网页钓鱼、恶意跳转、篡改页面
- **核心防护**：
  1. 输出HTML实体编码（输出层核心手段）
  2. Cookie设置HttpOnly属性
  3. 配置CSP内容安全策略
  4. 输入过滤与白名单校验

### 3. CSRF跨站请求伪造
- **核心原理**：诱导用户在已登录状态下，伪造请求执行非预期操作，利用浏览器自动携带Cookie的特性
- **攻击前提**：用户已登录目标网站，浏览器保留有效会话
- **典型特征**：伪造GET/POST请求、无有效来源校验
- **核心危害**：非授权操作（转账、改密码、发消息等）
- **核心防护**：
  1. CSRF Token随机校验（最主流方案）
  2. Cookie设置SameSite属性
  3. 校验Referer请求头
  4. 关键操作增加二次验证

### 4. 路径遍历/文件包含
- **核心原理**：文件路径参数未校验，通过目录跳转字符读取/执行未授权文件
- **分类**：本地文件包含（LFI）、远程文件包含（RFI）
- **典型特征**：`../../etc/passwd`、远程http文件引入
- **核心危害**：敏感文件泄露、配置信息泄露、远程代码执行
- **核心防护**：
  1. 路径白名单，严格限制可访问目录
  2. 关闭远程文件包含功能
  3. 规范化路径，过滤`../`等危险字符
  4. 限制Web进程目录访问权限

### 5. 命令注入
- **核心原理**：用户输入直接拼接进系统命令执行
- **典型连接符**：`;`、`|`、`&`、`&&`
- **核心危害**：执行任意系统命令、接管服务器、数据销毁
- **核心防护**：
  1. 禁止直接拼接命令，使用安全函数执行
  2. 过滤特殊命令连接符
  3. 程序以最小权限运行
  4. 使用白名单限制可执行命令范围
