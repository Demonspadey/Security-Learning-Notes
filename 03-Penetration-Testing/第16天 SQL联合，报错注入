##  网安核心知识点
### 1. Union联合查询注入原理
#### 核心规则
- 前后两条`SELECT`语句**字段数量必须一致**
- 字段数据类型需兼容（数字/字符串类型匹配）
- 依赖页面**正常数据回显**，需先闭合原有SQL语句（单引号/双引号/数字型）

#### 字段数探测方法
- `order by 数字`：逐级排序，判断总字段数（例：`order by 3--`）
- `union select 1,2,3...`：枚举字段，寻找回显点位

#### 利用流程
注入点发现 → 闭合方式判断 → 字段数探测 → 回显位查找 → 查询库/表/字段敏感数据

---

### 2. 报错注入基础原理
#### 适用场景
页面**无正常数据回显**，但后端数据库详细错误信息对外暴露。

#### MySQL经典报错函数
- `updatexml()`
- `extractvalue()`
- `floor()+rand()` 组合报错

#### 核心原理
利用数据库内置高危函数的报错信息外带数据，借助错误弹窗/页面报错内容窃取数据。

---

### 3. SQL注入通用基础知识点
#### 注入两大必要条件
1. 开发者直接将用户输入**拼接进SQL语句**
2. 未做输入过滤、转义、预编译处理

#### 三类注入核心特征区分
| 注入类型 | 核心特征 | 依赖条件 |
| :--- | :--- | :--- |
| 联合注入 | 有正常回显，`union` 拼接查询 | 页面有数据回显、字段数一致 |
| 报错注入 | 无回显、错误外显，利用报错函数 | 数据库错误信息外泄 |
| 盲注 | 无回显、无报错，依靠页面差异/延时判断 | 页面状态差异、响应时间可控 |

### 4. pikachu注入复盘
最后阶段爆字段内容的时候用user，内容为空，选对应的username+password才行
#爆用户ID和密码，一起语句用法。
id=-1' union select 1,group_concat(concat(username,':',password) separator '|') from users --+

---
