## 一、今日学习目标
1. 熟记BLP Bell-LaPadula两条核心规则，判断任意密级读写权限，明确用于保护机密性
2. 熟记Biba完整性模型两条核心规则，区分与BLP完全相反的数据流限制，用于银行/医疗防数据污染
3. 快速区分BLP、Biba适用场景，选择题、案例场景分析不混淆
4. 掌握Linux传统UGO权限局限性，熟练使用setfacl/getfacl配置扩展ACL、默认ACL、递归ACL
5. 掌握Windows SID特征、常用RID、安全描述符四大组成、DACL/SACL核心差异、ACE拒绝优先规则
6. 理解Windows访问令牌作用、Windows与Linux身份标识/权限体系对比
7. 掌握RBAC1角色继承逻辑，读懂递归获取全权限Python代码并完成修改拓展

## 二、核心理论考点
### 1. BLP 多级保密模型（MAC）
#### 基础信息
全称：Bell-LaPadula
安全目标：保护数据**机密性**，阻止高密级信息流向低密级造成泄密
适用场景：军队、政府涉密文件系统
密级从高到低：绝密＞机密＞秘密＞公开

#### 两条强制规则（必背口诀）
1. 简单安全条件 No Read Up 不上读
主体可读客体：主体密级 ≥ 客体密级
含义：低级用户不能读取高级机密文件；高级可向下读取低密文件
2. *-星属性 No Write Down 不下写
主体可写客体：主体密级 ≤ 客体密级
含义：高级用户不能把机密写入低级文件；低级可以向上写入高级文件

#### 数据流规律
级别越高：可读范围越大，可写范围越小；信息只能向上流动，杜绝高密外泄
#### 场景判断速记
机密读秘密：允许；机密写公开：禁止
秘密读绝密：禁止；公开写绝密：允许

### 2. Biba 多级完整性模型（MAC）
#### 基础信息
安全目标：保护数据**完整性**，阻止低完整性脏数据污染高完整性核心数据
适用场景：银行交易系统、医院病历、金融核心业务
完整性等级：高(H)＞中(M)＞低(L)

#### 两条强制规则（与BLP完全反向）
1. 简单完整条件 No Read Down 不下读
主体可读客体：主体完整性 ≤ 客体完整性
含义：高完整主体不能读取低完整脏数据，防止被污染
2. *-完整属性 No Write Up 不上写
主体可写客体：主体完整性 ≥ 客体完整性
含义：低完整主体不能写入高完整核心数据；高完整可向下写入低完整文件

#### BLP vs Biba 对比总表
|维度|BLP（机密性）|Biba（完整性）|
|----|----|----|
|读规则|不上读（主体≥客体可读）|不下读（主体≤客体可读）|
|写规则|不下写（主体≤客体可写）|不上写（主体≥客体可写）|
|数据流方向|信息仅向上流|信息仅向下流|
|防护目标|防止高密数据泄露|防止低质数据污染高价值数据|
|典型场景|军队涉密系统|银行、医疗核心系统|

#### 典型场景判断
高完整读低完整：Biba禁止；高完整写低完整：Biba允许
低完整读高完整：Biba允许；低完整写高完整：Biba禁止

#### 补充：双模型共存问题
同一系统可同时部署BLP+Biba，但会出现规则冲突；如高完整主体读取低完整文件，BLP允许、Biba禁止，需根据业务安全优先级取舍。

### 3. Linux 扩展ACL（setfacl/getfacl）
#### 传统UGO权限缺陷
仅支持所有者、同组、其他人三类权限，无法单独给任意用户/组分配独立权限。
#### ACL核心概念
ACL（访问控制列表）：按**客体（文件/目录）**存储，给任意用户、组自定义读写执行权限；能力表按主体存储，二者区分必考。
Linux权限校验优先级
 
所有者权限 → 专属用户ACL → 用户所属组ACL → other其他权限
 
4. Windows 访问控制体系
 
（1）SID 安全标识符
 
1. 特征：每个用户/组唯一永久标识；删除同名用户重建，SID完全不同

2. 格式：S-1-5-21-域标识-RID

3. 高频固定RID
500：本地管理员Administrator
501：来宾Guest
502：域KDC服务账户KRBTGT
 
（2）安全描述符（所有Windows安全对象自带）
 
四大组成部分：
 
1. Owner SID：对象所有者，拥有修改权限的资格

2. Group SID：兼容POSIX，Windows极少使用

3. DACL 自主访问控制列表：管控访问权限（允许/拒绝），由多条ACE访问控制项组成

4. SACL 系统访问控制列表：管控审计日志，记录访问行为成功/失败
 
DACL vs SACL 核心区分
 
DACL：控制「谁能访问资源」，授权/拦截访问；
SACL：控制「哪些操作需要记录日志」，用于安全审计。
 
ACE关键规则
 
拒绝类型ACE优先级高于允许ACE；同一用户同时存在拒绝、允许规则，拒绝生效。
 
访问令牌 Access Token
 
用户登录系统生成，绑定进程；包含用户SID、所属全部组SID、系统特权Privileges；进程依靠令牌完成资源权限校验。
 
Windows vs Linux 权限体系对比
 
表格
   
对比项 Windows Linux 
身份标识 SID UID/GID 
基础权限 DACL ACE chmod UGO三元权限 
扩展权限 无原生扩展ACL setfacl文件ACL 
强制访问控制 MIC强制完整性标签 SELinux安全上下文 
审计机制 SACL+安全事件日志 auditd审计服务 
权限校验顺序 拒绝ACE优先 所有者→组→其他 
 
5. RBAC1 角色继承模型
 
RBAC分层回顾
 
RBAC0：基础 用户-角色-权限；
RBAC1：增加角色层级继承（上级自动拥有下级全部权限）；
RBAC2：职责分离约束；
RBAC3：RBAC1+RBAC2整合。
 
继承逻辑
 
父角色拥有子角色全部权限；递归遍历所有下级角色汇总全部有效权限。
 
Python实现逻辑
 
1. 定义角色层级映射关系；

2. 定义每个角色独立权限；

3. 递归函数逐层向上遍历父角色，合并所有权限；

4. 根据用户绑定角色，汇总完整权限用于校验。
