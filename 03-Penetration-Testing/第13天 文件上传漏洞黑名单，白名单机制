## 三、网安基础原理
### 1. 正常业务流程
用户上传图片/文档 → 服务端多维度校验 → 合规目录安全存储

### 2. 上传漏洞攻击链路
上传恶意脚本 → 校验机制缺失/被绕过 → 文件上传成功
→ 访问文件路径 → 中间件解析执行脚本 → 服务器权限被控

### 3. 基础绕过原理（仅理论）
- 前端绕过：禁用JS（容易禁用到一些有用的JS代码）、抓包修改请求参数、直接本地保存源代码删除Js内容，然后指定地址上传。
- 后缀绕过：大小写变异、特殊后缀、截断绕过
- MIME绕过：伪造`image/jpeg`、`image/png`伪装图片类型
- 解析绕过：利用中间件解析缺陷配合上传漏洞

### 4. 靶场关联
Upload-Labs1-13关 核心底层均围绕**文件上传校验缺陷**设计，今日理论为后续实操铺垫。

其中有些绕过原理看代码知道，但是版本格式不支持，后续还需要抽时间看看对应版本来重新验证绕过！
---
