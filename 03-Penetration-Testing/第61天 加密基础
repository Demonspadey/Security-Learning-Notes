## 一、核心目标
1. 掌握对称加密、非对称加密、哈希函数三大基础密码技术的原理与特性
2. 熟记典型算法分类，精准区分各算法的适用场景
3. 掌握国密SM2/SM3/SM4的定位与应用场景
4. 熟练使用openssl完成密钥生成、哈希校验等基础实操

---
## 二、核心理论考点
### 1. 对称加密（单钥/私钥密码体制）
- 核心特点：加密、解密使用**同一把密钥**
- 优势：运算速度极快，适合大批量业务数据加密
- 劣势：密钥分发管理困难，点对点传输存在泄露风险
- 典型算法：AES、DES、SM4、RC4
- 适用场景：大文件加密存储、视频流量加密、内网批量数据加密

### 2. 非对称加密（公钥密码体制）
- 核心特点：存在**公钥+私钥**一对密钥，公钥可公开，私钥必须保密
- 核心规则：公钥加密、私钥解密；私钥签名、公钥验签
- 优势：密钥分发简单，公钥可对外公开，仅需保护私钥
- 劣势：运算开销大、速度慢，仅适合小数据处理
- 典型算法：RSA、ECC、SM2
- 适用场景：数字签名、密钥交换、身份认证、数字证书

### 3. 哈希函数（散列函数）
- 三大核心特性：
  1. 单向不可逆：无法从哈希值反向还原原文
  2. 定长输出：无论原文多长，输出长度固定
  3. 抗碰撞性：很难找到两个不同原文产生相同哈希值
- 衍生特性：雪崩效应（原文微小改动，哈希值发生巨大变化）
- 典型算法：MD5、SHA-1、SHA-256、SM3
- 适用场景：文件完整性校验、用户密码加盐存储、数字签名摘要

### 4. 国密算法体系
| 算法 | 类型 | 核心用途 |
|------|------|----------|
| SM2 | 非对称加密（椭圆曲线） | 数字签名、密钥协商、身份认证 |
| SM3 | 哈希函数 | 完整性校验、签名摘要生成 |
| SM4 | 对称分组加密 | 大批量业务数据加密存储 |

### 5. 混合密码体制
- 核心原理：结合两类加密的优势，**非对称加密传递临时对称密钥，对称加密承载海量业务数据**
- 典型应用：HTTPS协议、加密文件传输、VPN隧道
- 优势：兼顾密钥分发的安全性和大数据加密的效率
