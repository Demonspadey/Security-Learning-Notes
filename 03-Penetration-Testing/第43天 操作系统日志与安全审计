## 一、核心目标
1.  掌握Linux登录日志的分类与存储位置，区分文本日志与二进制日志
2.  熟练使用`who`/`w`/`last`/`grep`完成登录行为审计，替代`lastb`方案
3.  能独立排查暴力破解攻击，梳理异常登录溯源流程
4.  形成标准化答题/面试话术，覆盖日志审计全场景

---

## 二、核心理论
Linux 登录与身份认证相关日志分为两大类：
1.  **文本认证日志**
    - 路径：`/var/log/auth.log`（Kali/Debian 系列默认）
    - 作用：记录账号登录、SSH远程连接、密码验证、sudo提权、账号权限变更；**同时包含登录成功、登录失败全量记录**，是认证溯源核心日志。

2.  **二进制登录日志（不可直接用cat/grep查看）**
    - `/var/log/wtmp`：存储历史登录、登出、系统重启数据，专属解析命令 `last`
    - `/var/run/utmp`：存储当前在线用户数据，专属解析命令 `who`、`w`

3.  **核心查询命令**
    - `last`：解析 wtmp，查看历史成功登录、登出、系统重启历史
    - `who`：解析 utmp，查看当前在线用户、终端、登录时间
    - `w`：解析 utmp，查看在线用户 + 终端 + 运行进程 + 系统负载（信息最全面）
