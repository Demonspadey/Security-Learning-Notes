## 一、核心目标
1. 掌握数字签名完整流程，明确签名/验签的密钥使用与功能边界
2. 区分PKI体系CA/RA/CRL三大核心组件的职责边界
3. 熟记哈希函数三大特性与主流算法的输出长度
4. 掌握四类密码分析攻击的强度排序与能力差异
5. 能通过OpenSSL完成数字签名生成与验证实操

## 二、核心理论考点
### 1. 数字签名
#### （1）核心流程
- **签名生成**：原文 → 计算哈希摘要 → 发送方私钥加密摘要 → 生成数字签名 → 随原文一同发送
- **签名验证**：接收方收到原文+签名 → 对原文重新计算哈希得摘要A → 用发送方公钥解密签名得摘要B → 对比A与B，一致则验签通过

#### （2）核心规则
- 签名密钥：发送方私钥（唯一持有，不可伪造）
- 验签密钥：发送方公钥（公开可查，所有人可验证）
- 核心功能：身份真实性、数据完整性、行为不可否认性
- 功能边界：**不提供数据机密性**，机密性是加密技术的作用

#### （3）典型应用
HTTPS证书校验、软件安装包签名、电子合同签署、电子公文签章

### 2. PKI体系核心组件
| 组件 | 全称 | 核心职责 | 易错边界 |
|------|------|----------|----------|
| CA | 证书认证机构 | 签发、管理、吊销数字证书，是PKI信任根 | 拥有最终签发权 |
| RA | 注册机构 | 用户身份初审、证书申请受理，作为CA前端 | 无证书签发权，仅负责审核 |
| CRL | 证书撤销列表 | 批量发布已吊销的证书清单，定期更新 | 离线批量校验，实时性弱 |
| OCSP | 在线证书状态协议 | 单张证书实时状态查询 | 在线实时校验，适合高频场景 |

#### 补充：数字证书
- 核心作用：绑定公钥与用户身份，证明公钥的合法归属
- 核心内容：用户公钥、身份信息、有效期、CA的数字签名
- 标准：X.509标准，包含签名算法标识，不含加密算法标识

### 3. 哈希函数（杂凑函数）
#### （1）三大核心特性
1. **单向性**：不可逆，无法从哈希值还原出原文
2. **定长输出**：无论输入长度多少，同一算法输出长度固定
3. **抗碰撞性**：计算上难以找到两个不同的输入得到相同的哈希值

#### （2）主流算法输出长度
| 算法 | 输出长度 | 安全状态 |
|------|----------|----------|
| MD5 | 128位 | 已被破解，存在碰撞风险 |
| SHA-1 | 160位 | 已被破解，不推荐使用 |
| SHA-256 | 256位 | 安全，主流商用 |
| SM3 | 256位 | 国密标准，安全 |

#### （3）典型应用
数据完整性校验、数字签名辅助、密码加盐存储、文件去重

### 4. 密码分析攻击类型
#### （1）强度排序（从弱到强）
唯密文攻击 < 已知明文攻击 < 选择明文攻击 < 选择密文攻击

#### （2）各类型能力边界
- 唯密文攻击：攻击者仅掌握密文，无任何明文信息，难度最高
- 已知明文攻击：攻击者掌握部分明文-密文对应对
- 选择明文攻击：攻击者可自主选择明文，获取对应密文
- 选择密文攻击：攻击者可自主选择密文，获取对应明文，威胁最大

记忆规律：攻击者掌握的控制权越多、已知信息越多，攻击强度越高。
