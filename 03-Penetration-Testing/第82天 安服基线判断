### 🌀 Part 1：ps aux 到底在看什么？——哪些进程是"正常的"

```
你敲了 ps aux 看到几十行输出，觉得眼花缭乱。
很正常。但你需要知道：90%的进程你根本不用管。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
第一类：永远正常的系统进程（看到直接略过）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  root  1   systemd              → 系统一号进程，所有进程的爹
  root  xxx  kthreadd             → 内核线程
  root  xxx  sshd                 → SSH服务（有人连你时会看到多个）
  root  xxx  cron/crond           → 定时任务
  root  xxx  rsyslogd             → 日志服务
  root  xxx  systemd-journald     → 日志服务
  root  xxx  networkd-dispatcher  → 网络管理
  root  xxx  snapd                → snap包管理
  root  xxx  NetworkManager       → 网络管理
  mysql xxx  mysqld               → MySQL数据库（你装的，你知道）
  www-data  apache2               → Web服务（你装的，你知道）
  你的用户名 bash/sshd            → 你自己登录的会话

→ 以上这些你统一略过，不用细看。
  每台Linux都跑着50-80个进程，其中95%是系统进程，你不需要认识每一个。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
第二类：需要警惕的进程（看到就要查）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  ① 程序名奇怪：随机字符串/隐藏名 → nc、bash -i、python -c 带payload的
    例：/tmp/... 下的可执行文件，名字像"..."或随机8位字母
  ② 进程所属用户不对：apache用户跑着bash、www-data起了一个netcat
  ③ CPU/内存异常高：某个进程占了90%+ CPU（典型的是挖矿）
  ④ 父进程不对：正常的程序PPID是1(systemd)或对应的服务
     如果看到一个bash的PPID是httpd，意味着Web服务器被用来执行命令了

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
碰到可疑进程后，用这3条命令查底细：
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  # ① 这个进程的可执行文件在哪？
  ls -la /proc/PID/exe
  # 例：ls -la /proc/8986/exe
  # → 如果是 /usr/bin/nc → 可疑
  # → 如果是 /tmp/xxx → 高危（正常程序不会在/tmp里跑）
  # → 如果是 /usr/sbin/sshd → 正常

  # ② 这个进程打开了哪些文件/网络连接？
  sudo lsof -p PID
  # 例：sudo lsof -p 8986
  # → 能看到它打开的TCP连接、读写的文件
  # → 如果它连着一个外部IP → 石锤C2

  # ③ 这个进程是谁启动的？
  ps -ef | grep PID
  # 看PPID（父进程ID），追溯谁是发起者

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
一个真实的"可疑进程发现"流程（用一个例子串起来）：
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  # Step 1: ps aux 扫一眼，看到一个 nc 进程
  ps aux | grep nc

  # Step 2: 查它的底细
  ls -la /proc/8986/exe        # 发现 nc 在 /usr/bin/nc
  sudo lsof -p 8986             # 发现它连着 8.8.8.8:80 → 石锤恶意外联

  # Step 3: 再查父进程
  ps -ef | grep 8986            # 发现PPID=1（systemd启动的）
                                 # 说明攻击者是通过远程执行的方式起的进程

  # Step 4: 确认后决定 kill
  kill 8986                     # 先温和终止
  ps aux | grep nc              # 验证死了没有
                                 # 如果没死 → kill -9 8986（强制杀）
```

---

### 🌀 Part 2：netstat 到底在看什么？——哪些外联是"正常的"

```
你敲了 netstat -anp | grep ESTABLISHED，只看到你自己机器的IP。
你可能在困惑："我的机器是不是没有外联？"

答案是：大概率你有外联，但你只看到了一个。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
正常Ubuntu系统一定有的ESTABLISHED连接：
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  # ① 你自己的SSH连接
  # 你的Windows/Mac → Ubuntu:22 → ESTABLISHED
  # 这就是你正在远程操作的连接

  # ② Ubuntu可能有的DNS/APT连接（UDP，netstat要加-u才看到）
  # ③ 如果你装了Web服务，可能还有localhost的内部连接

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
怎么区分"正常外联"和"恶意外联"？
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  # 先看本地端口
  # 你自己的SSH连接 → 本地端口是随机的（如 22→55678）
  # 服务监听端口 → 本地端口是固定的（如 :80、:22、:3306）

  # Example:
  # 192.168.1.100:22    → 192.168.1.50:54321    ESTABLISHED
  # ↑ 你的Ubuntu        ↑ 你的Windows/笔记本
  # 这是正常的SSH连接，看到不用管

  # 真正的恶意外连长什么样？
  # 192.168.1.100:44555 → 8.8.8.8:80    ESTABLISHED
  # ↑ 你的Ubuntu       ↑ 外部IP
  # 特征：你的服务器主动连外部IP的非标准端口（不一定是80/443）

  # 用这条命令看所有"出站"连接（比netstat不加参数更有用）：
  ss -tunp | grep -v "127.0.0.1" | grep ESTAB
  # -v 127.0.0.1 = 排除本机内部连接（那些不用管）
  # 留下的就是"你的机器连接外部的真实连接"

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
你看不到其他外联的原因分析（这才是关键）：
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  原因1：Ubuntu没装其他互联网服务 → 所以确实没有外联
         一个刚装好的Ubuntu只有SSH监听，没有应用往外连，这是正常的
         恶意外联想被我们看到 → 必须先有恶意进程在跑（nc、被注入的进程等）
  原因2：ping走ICMP，netstat看不到 → 所以不能用ping模拟外联（这是Day34学的教训）
  原因3：curl是短连接 → 一瞬间建立然后断开，netstat来不及捕捉

  → 所以看不到外联不是因为不会看，
     是因为Ubuntu上本来就没有恶意进程。
     Day34的演习里用nc 8.8.8.8 80 & 之后，再敲netstat就能看到了。
```

---

### 🌀 Part 3：WebShell怎么判断？——找藏在Web目录里的恶意文件

```
这是安服日常最常见的问题之一。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
WebShell的特征（你不需要猜，用规则找）：
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  ① 文件时间异常 → 最近没上传文件，但web目录里多了一个新文件
  ② 文件名可疑 → shell.php、cmd.php、1.php、a.asp、conn.jsp
  ③ 权限异常 → 正常文件644，WebShell经常被改成755或777
  ④ 内容特征 → 包含 eval、system、exec、passthru、base64_decode、shell_exec

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
找WebShell的命令（直接抄）：
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  # ① 在Web目录下找最近24小时修改过的php文件
  find /var/www/html -name "*.php" -mtime -1 -type f

  # ② 找包含危险函数的PHP文件（这条命令直接用）
  grep -r "eval\|system\|exec\|passthru\|shell_exec\|base64_decode" /var/www/html/ --include="*.php" 2>/dev/null

  # 正常网站也会有少量使用这些函数的文件 → 看文件名和路径判断
  # 例如：/var/www/html/wordpress/wp-includes/... 用了system → 可能是正常的
  # 例如：/var/www/html/uploads/shell.php 用了eval → 基本是WebShell

  # ③ 找权限异常的文件
  find /var/www/html -perm 777 -o -perm 755 | head -20
  # 如果发现一个php文件被改成777，基本可以确定是被上传的WebShell

  # ④ 最简单直接的方法（面试吹牛用的其实是这一条）
  ls -la /var/www/html/ | sort -t' ' -k7
  # 按时间排序，最新出现的文件优先查
```

---

### 🌀 Part 4：怎么看懂看到的东西——补一个"基线"概念

```
觉得模糊的核心原因不是不会命令，是没有"基线"。

什么是基线？
 基线 = "这台服务器正常情况下长什么样"
 不知道正常长什么样，当然看不出异常。

比如现在看到一台刚装好的Ubuntu：
  ps aux → 只有系统进程 → 这就是基线
  netstat → 只有自己的SSH连接 → 这就是基线
  /var/www/html/ → 没有文件 → 这就是基线

当基线变了，立刻就能发现：
  明天多了个nc进程 → 异常
  后天多了一个外联 → 异常
  大后天web目录多了一个php → 异常

给ubuntu打一个"基线快照"的（做一次就行，耗时5秒）：
  # 保存当前进程快照
  ps aux > ~/baseline_process_正常.txt
  # 保存当前网络快照
  ss -tunp > ~/baseline_network_正常.txt
  # 保存web目录状态
  find /var/www/html/ -type f > ~/baseline_web_正常.txt 2>/dev/null

下次觉得"好像不太对"的时候：
  diff ~/baseline_process_正常.txt <(ps aux)
  # 直接告诉多了哪些进程，少了对哪些进程
  # 比盯着几十行输出找异常准确100倍
```

---
