## 一、核心目标
掌握SQL注入进阶利用方式：
1.  堆叠注入的原理、适用条件与攻击场景
2.  二次注入的成因、完整攻击链路与隐蔽性特点
3.  对比六类SQL注入的适用场景与防御侧重点

---

## 二、核心知识点梳理
### 1. 堆叠注入
- **原理**：利用分号 `;` 分隔多条SQL语句，一次请求执行多条独立数据库指令。
- **适用条件**：后端数据库支持多语句执行（MySQL默认支持）。
- **核心危害**：查库、删表、清空数据、导出敏感数据、执行高危操作。
- **与Union注入区别**：
  - Union注入：合并查询结果，依赖页面回显。
  - 堆叠注入：独立执行多条语句，无需页面回显，可执行增删改操作。

### 2. 二次注入
- **产生原理**：恶意输入在入库时被转义过滤，存入数据库；后续业务读取该数据时未做二次过滤，直接拼接SQL语句，触发注入。
- **标准攻击流程**：
  1.  构造含恶意特殊字符的账号/昵称进行注册
  2.  恶意载荷被转义，正常存入数据库
  3.  后台业务读取库中原始数据
  4.  未做过滤直接拼接SQL，触发注入执行恶意语句
- **漏洞特点**：前端看似有过滤，实则后端逻辑缺陷，常规WAF易绕过，隐蔽性强。

### 3. 六类SQL注入横向对比
| 注入类型 | 适用场景 | 利用前提 | 防御侧重点 |
| :--- | :--- | :--- | :--- |
| 联合注入 | 有回显页面，可直接展示数据 | 页面回显数据、列数匹配 | 输入过滤+参数化查询 |
| 报错注入 | 无回显但有数据库报错信息 | 数据库报错信息回显 | 关闭详细错误回显 |
| 布尔盲注 | 无回显、无报错，仅页面有布尔差异 | 页面响应随条件真假变化 | 统一错误页面 |
| 时间盲注 | 无回显、无报错、无布尔差异 | 可通过sleep等函数控制响应时间 | 限制SQL执行时间 |
| 堆叠注入 | 支持多语句执行的数据库 | 数据库支持多语句执行 | 关闭多语句执行、最小权限 |
| 二次注入 | 数据入库后被读取拼接SQL | 入库过滤、出库无过滤 | 出库数据二次过滤+参数化查询 |

---

## 三、实操复盘与任务
1.  本地靶场复现堆叠注入基础Payload
2.  绘制二次注入完整攻击链路流程图
3.  整理六类SQL注入对比笔记，形成记忆清单

---

## 四、易错点总结
- 堆叠注入的标识是**分号 `;`**，而非逗号。
- 二次注入的核心是**入库过滤、出库无过滤**，仅前端过滤无法防御。
- 防御SQL注入的根本手段是**预编译参数化查询**，而非单一的输入过滤。


## 五、实战
今天把pikachu注入除了盲注，宽介子没有练习，其他都练习知晓了原理和注入方法，手工注入。
主要收获是在二次注入和堆叠注入弄懂了两者区别，目前所知晓的练习靶场不多，还需要继续深入学习。
