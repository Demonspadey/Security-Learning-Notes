## 1.身份认证三要素
1.所知：密码、密保答案；
2.所有：UKEY、硬件令牌、短信验证码；
3.固有：指纹、人脸、虹膜生物特征。
> 两种及以上不同类别组合 = MFA多因素认证。

## 2.认证分类
1.单向认证、双向认证；
2.第三方可信认证：Kerberos(KDC)、PKI/CA数字证书。

## 3.经典协议
1.S/Key：一次性口令，单口令只用一次，专门抵御重放攻击；
2.Kerberos：KDC提供票据分发，第三方票据认证；
3.X.509：国际标准，定义数字证书格式规范。

## 4.PKI四大组件
1.CA：证书签发、吊销、全生命周期管理；
2.RA：用户资料审核、证书注册受理；
3.CRL：离线批量证书吊销列表；
4.OCSP：在线实时查询单张证书有效性。
规则：公钥公开分发，用户私钥本地严格保密，证书内只存放公钥。

## 5.会话安全基础规范
1.会话ID：密码学随机生成，最低128bit，禁止顺序可预测编号；
2.禁止GET/URL拼接sid参数，避免日志、浏览器历史泄露；
3.关键动作(登录/改密/支付)完成强制刷新SID；
4.配置会话空闲超时，自动销毁过期会话。

## 6.Cookie安全双属性
1.HttpOnly：禁止JS脚本读取Cookie → 防XSS盗Cookie；
2.Secure：仅HTTPS加密链路才会携带Cookie → 防HTTP明文传输窃取。

## 7.两类Web会话漏洞
1.会话固定：攻击者提前预埋SID，用户登录前后SID不变；修复：登录立刻换新会话；
2.会话劫持：抓包/XSS窃取已有生效Cookie，冒用登录；修复：HTTPS+双Cookie属性+随机SID。

## 8.数字签名能力
✅完整性校验、身份鉴别、不可否认
❌无法实现数据机密性（数据加密实现机密性）

## 速记口诀
认证分三样，所知所有和固有；
SKey防重放，Kerberos靠KDC；
CA发证RA注册，OCSP在线查证书；
HttpOnly防XSS，Secure走HTTPS；
固定预埋劫持偷，登录换ID保平安。
