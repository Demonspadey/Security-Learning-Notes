## 一、核心目标
1.  掌握布尔盲注、时间盲注的适用场景与核心原理
2.  能区分联合注入/报错注入/盲注的选型逻辑
3.  完成盲注基础语句的本地靶场复现

---

## 二、核心知识点
### 1. 盲注适用场景
当Web页面**无数据回显、无报错信息**，仅返回正常/异常两种页面状态时，必须使用盲注技术。

### 2. 布尔盲注原理
- 核心逻辑：利用`AND/OR`逻辑构造真假条件，通过页面状态判断条件结果
- 典型载荷：`' AND (SELECT ASCII(SUBSTRING(database(),1,1))) > 100 -- `
- 猜解流程：逐位猜解库名 → 表名 → 字段名 → 数据

### 3. 时间盲注原理
- 核心逻辑：利用`sleep()`/`benchmark()`延时函数，通过响应时间判断条件真假
- 典型载荷：`' AND IF(ASCII(SUBSTRING(database(),1,1))>100, sleep(3), 1) -- `
- 适用场景：页面状态无法区分真假，仅能通过响应延迟判断

### 4. 三类注入对比
| 注入方式 | 适用场景 | 核心依赖 |
| :--- | :--- | :--- |
| Union联合注入 | 页面有直接数据回显 | 字段回显 |
| 报错注入 | 页面有数据库错误回显 | 错误详情 |
| 盲注（布尔/时间） | 无回显、无报错 | 页面状态/响应时间 |

---

## 三、实操任务
1.  本地搭建简易注入靶场，复现布尔盲注基础语句
2.  整理三类注入对比思维导图
3.  记录不同载荷的响应状态/时间差异

---
