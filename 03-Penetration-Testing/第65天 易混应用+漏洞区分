## 一、核心目标
1. 厘清8组高频易混概念的核心边界，掌握区分关键
2. 熟记每组概念的典型场景与核心特征，适配选择+案例题型
3. 掌握各技术的选型原则，能结合场景匹配对应方案

---
## 二、核心易混概念对比
### 1. Web攻击三类：SQL注入 / XSS / CSRF
| 对比维度 | SQL注入 | XSS跨站脚本 | CSRF跨站请求伪造 |
|----------|---------|-------------|------------------|
| 攻击目标 | 后端数据库 | 用户浏览器 | 登录用户身份 |
| 执行位置 | 服务器端 | 客户端浏览器 | 服务器端（冒用受害者身份） |
| 攻击本质 | 诱导数据库执行恶意SQL | 诱导浏览器执行恶意脚本 | 冒用用户会话发起非预期请求 |
| 典型载荷 | `' OR '1'='1 --`、`UNION SELECT` | `<script>stealCookie()</script>` | 隐藏图片、伪造表单自动提交 |
| 核心危害 | 拖库、数据篡改、删库 | 窃取Cookie、会话劫持、钓鱼 | 改密码、转账、恶意发布内容 |
| 核心防御 | 参数化预编译查询 | 输出转义编码 + Cookie HttpOnly | CSRF Token + SameSite Cookie |

**记忆口诀**：SQL骗数据库，XSS坑浏览器，CSRF借用户身份

### 2. 检测防御设备：IDS / IPS
| 对比维度 | IDS入侵检测系统 | IPS入侵防御系统 |
|----------|-----------------|-----------------|
| 部署方式 | 旁路镜像流量 | 串联在线链路 |
| 工作模式 | 被动监控 | 主动阻断 |
| 响应动作 | 仅告警、记录日志，无拦截能力 | 实时丢包、断开连接、主动拦截 |
| 业务影响 | 不影响正常业务流量 | 误报会阻断合法业务 |
| 适用场景 | 事后审计、溯源取证 | 线上实时防护、主动阻断攻击 |

**记忆口诀**：IDS旁路只报警，IPS串联能阻断

### 3. 访问控制模型：DAC / MAC / RBAC
| 对比维度 | DAC自主访问控制 | MAC强制访问控制 | RBAC基于角色访问控制 |
|----------|-----------------|-----------------|----------------------|
| 权限管控主体 | 资源所有者自主分配 | 系统安全标签强制管控 | 管理员通过角色批量分配 |
| 核心特征 | 权限可传递、灵活度高 | 标签匹配、策略固定不可改 | 用户→角色→权限三层映射 |
| 典型落地 | Linux chmod、文件ACL | SELinux、BLP/Biba模型 | 企业OA、后台管理系统 |
| 适用场景 | 个人电脑、共享文件 | 军工、政务等高密级系统 | 绝大多数企业业务系统 |

**记忆口诀**：DAC所有者说了算，MAC系统标签管，RBAC角色中间转

### 4. 安全模型：BLP / Biba
| 对比维度 | BLP模型 | Biba模型 |
|----------|---------|----------|
| 保护目标 | 机密性（防数据泄露） | 完整性（防数据篡改） |
| 核心规则 | 下读上写（No Read Up、No Write Down） | 上读下写（No Read Down、No Write Up） |
| 规则释义 | 低密级不能读高密级；高密级不能写入低密级 | 高密级不能读低密级；低密级不能写入高密级 |
| 典型场景 | 军事涉密文档分级管理 | 金融交易、工控系统防篡改 |

**记忆口诀**：BLP保机密，下读上写；Biba保完整，上读下写

### 5. 密码技术：对称加密 / 非对称加密
| 对比维度 | 对称加密 | 非对称加密 |
|----------|----------|------------|
| 密钥规则 | 加解密共用同一密钥 | 公私钥成对，公钥加密、私钥解密 |
| 运算速度 | 极快，适合大批量数据 | 速度慢，仅适合少量短数据 |
| 密钥分发 | 难度高，需安全渠道传输 | 公钥可公开分发，仅需保护私钥 |
| 代表算法 | AES、DES、SM4 | RSA、ECC、SM2 |
| 主要用途 | 大文件加密、传输报文加密 | 密钥协商、数字签名、身份认证 |

**记忆口诀**：对称单钥快，非对称双钥慢；对称存数据，非对称传密钥

### 6. 密码应用：数字签名 / 数据加密
| 对比维度 | 数字签名 | 非对称数据加密 |
|----------|----------|----------------|
| 核心目的 | 身份核验、防篡改、不可否认 | 保障传输机密性，防止窃听 |
| 密钥使用 | 己方私钥签名，所有人公钥验签 | 对方公钥加密，仅己方私钥解密 |
| 安全效果 | 证明发送者身份、校验数据完整性 | 保证第三方无法读取明文内容 |

**记忆口诀**：签名证身份，加密保机密

### 7. 基础概念：鉴别 / 认证
- **鉴别**：核验实体身份，解决“你是谁”的问题
- **认证**：授予访问权限，解决“允许你做什么”的问题
- 执行顺序：**先鉴别，后认证**

### 8. 风险三要素：漏洞 / 威胁 / 风险
- **漏洞**：系统自身的缺陷、薄弱点，是内因
- **威胁**：外部可能造成危害的行为、手段，是外因
- **风险**：漏洞被威胁利用，造成损失的可能性与影响程度

---
