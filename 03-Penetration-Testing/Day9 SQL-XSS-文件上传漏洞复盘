# Day9 （Web漏洞数据流梳理与复习漏洞绕过原理）

---

## 一、Web漏洞通用数据流模型
### 1. 完整攻击链
`输入点 → 服务器处理 → 输出点`
- 漏洞本质：攻击者通过控制输入点，利用服务器校验缺陷，在输出点触发恶意行为

### 2. 关键节点拆解
- **输入可控点**：
  - URL参数、POST请求数据、Cookie值、请求头（User-Agent/Referer等）、文件上传接口
- **服务器校验环节**：
  - 白名单/黑名单过滤、正则匹配、文件头校验、MIME类型检查、WAF防护
- **输出点**：
  - 页面渲染（HTML/JS执行）、数据库存储（SQL注入）、日志记录、文件系统写入（文件上传）

---

## 二、DVWA靶场漏洞复盘要点 （复习检测之前是否有遗忘点）
### 1. SQL注入数据流梳理
- 输入点：`id`参数（如 `?id=1`）
- 服务器校验：无强校验/仅简单过滤
- 执行过程：参数拼接进SQL语句 → 数据库执行 → 结果回显到页面
- 防护失效点：未使用预编译/参数化查询，直接拼接用户输入

### 2. XSS跨站脚本数据流梳理
- 输入点：留言框/搜索框/URL参数
- 服务器校验：部分过滤`<script>`标签，但未处理事件属性（`onerror`/`onload`）
- 执行过程：恶意脚本存储到数据库 → 页面加载时读取并渲染 → 浏览器执行脚本
- 防护失效点：输出编码不全，未对特殊字符转义，CSP配置宽松

### 3. 文件上传漏洞数据流梳理
- 输入点：文件上传表单
- 服务器校验：仅检查文件扩展名（黑名单过滤`.php`），未校验文件内容
- 执行过程：文件上传到服务器目录 → 访问文件URL触发解析
- 防护失效点：校验仅在客户端/前端，可通过抓包修改绕过；文件类型校验不全

---

## 三、漏洞防护与绕过对应关系
| 漏洞类型 | 常见防护措施 | 绕过思路 |
| :--- | :--- | :--- |
| SQL注入 | 黑名单过滤`union/select` | 大小写变形、注释绕过、编码绕过 |
| XSS | 过滤`<script>`标签 | 使用事件属性（`onclick`/`onerror`）、Img标签绕过 |
| 文件上传 | 限制文件扩展名 | 双扩展名（`.php%00.jpg`）、文件头伪造、解析漏洞利用 |

---

## 四、今日复盘总结
- 核心收获：理解了Web漏洞从输入到执行的完整链路，所有漏洞都源于**输入可控+校验缺陷**
- 关键结论：防护措施的有效性取决于校验环节的强度，绕过的本质是找到校验逻辑的“盲区”
- 下一步计划：针对每个漏洞类型，补充对应的Payload和绕过场景练习
