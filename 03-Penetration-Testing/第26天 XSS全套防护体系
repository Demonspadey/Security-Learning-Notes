## 一、核心目标
1.  掌握XSS五层完整防护机制的原理与适用场景
2.  理解各类防护的短板与常见绕过思路
3.  结合靶场实操，吃透防护与绕过的对抗逻辑

---

## 二、核心知识点

### 1. 输入过滤
- 服务端对用户传入数据做黑白名单校验，拦截 `<>`、`script`、`iframe` 等恶意关键字
- 黑名单局限性极强，极易被大小写混淆、编码、变形绕过
- 安全原则：**白名单校验的安全性远高于黑名单过滤**

### 2. 特殊字符转义（最核心通用防护）
- 对HTML、JS、URL特殊符号做实体编码，把可执行代码转为普通文本字符
- 从渲染层面阻断Payload执行，是防御XSS最基础、最稳定的手段

### 3. HttpOnly 属性
- Cookie配置HttpOnly后，JS无法读取Cookie，可有效防御XSS带来的会话劫持风险
- 局限性：仅能保护Cookie不被窃取，无法阻止脚本执行、页面篡改等其他攻击

### 4. CSP内容安全策略
- 限制页面资源加载来源、禁止内联JS与恶意脚本执行
- 属于强效顶层防护，可大幅压缩XSS攻击的生效空间

### 5. XSS防护绕过思路
- 编码变形绕过、大小写混淆、闭合原有标签绕过、绕过不完善的过滤正则
- 理解绕过逻辑，才能吃透防护的本质短板
