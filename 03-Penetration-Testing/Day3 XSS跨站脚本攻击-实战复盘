# XSS跨站脚本攻击实战复盘（DVWA全难度）

## 1. 本次注入核心收获
- 已成功通关DVWA反射型/存储型/DOM型XSS全难度，掌握了从判断注入点到执行脚本的完整流程。
- 明确了本次测试覆盖的XSS类型：
  - 反射型XSS
  - 存储型XSS
  - DOM型XSS
- 掌握了不同过滤场景下的绕过技巧，每一步的执行目的清晰。

## 2. 本次遇到的过滤 & 对应绕过
| 过滤类型 | 过滤内容 | 我用的绕过方法 | 备注 |
| :--- | :--- | :--- | :--- |
| 标签黑名单过滤 | `<script>` 标签被直接删除 | 改用 `< img src=x onerror=alert(1)>` / `<svg onload=alert(1)>` | 
利用事件属性执行JS，
完全避开script标签 |
| 引号转义过滤 | 单/双引号被转义为 `\'` / `\"` | 直接使用无引号Payload，如 `< img src=x onerror=alert(1)>` | 不依赖引号闭合，
直接构造标签执行 |
| 前端长度限制 | 输入框限制10位字符 | Burp抓包修改参数，发送完整Payload | 前端限制为客户端校验，可直接绕过 |
| 下拉列表闭合限制 | 输入被包裹在 `<option>` 标签内 | 用 `"></option></select>` 闭合原有标签后注入 | 先破坏原有HTML结构，
再插入恶意标签 |
| innerHTML安全限制 | 动态插入的 `<script>` 标签不执行 | 使用 `<img>` / `<svg>` 等带事件属性的标签 | 
浏览器限制innerHTML中script标签执行，
但不限制事件属性 |
| URL参数解析限制 | 参数拼接导致脚本无法执行 | 使用 `&` 或 `#` 切断参数传递，如 `English&<script>alert(1)</script>` | 
让Payload脱离服务器处理，
直接被前端解析 |

## 3. 今日通用Payload合集
1.  通用事件执行（无引号，兼容性最强）
    ```html
    < img src=x onerror=alert(1)>
    ```
2.  DOM型XSS专用（innerHTML场景稳定）
    ```html
    <svg onload=alert(1)>
    ```
3.  下拉列表/表单闭合专用
    ```html
    "></option></select>< img src=x onerror=alert(1)>
    ```
4.  引号/尖括号全过滤时的备选方案
    ```javascript
    javascript:alert(1)
    ```

## 4. 关键知识点复盘
- 浏览器对 `innerHTML` 动态插入的 `<script>` 标签有安全限制，不会自动执行，因此必须依赖事件属性标签绕过。
- `&` 和 `#` 是DOM型XSS中常用的参数切断符，可让Payload脱离服务器过滤，直接被前端解析。
- 前端的输入长度、字符限制均为客户端校验，可通过抓包直接绕过，真正的防御在后端。
