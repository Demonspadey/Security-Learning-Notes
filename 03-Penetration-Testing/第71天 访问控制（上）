## 一、今日学习目标
1. 熟记访问控制三要素：主体、客体、访问操作，能快速区分主体/客体
2. 精准区分DAC自主、MAC强制、RBAC基于角色三大模型核心特征、优缺点、适用场景
3. 熟练解析Linux 10位权限字符串、数字权限换算、umask计算题速算方法
4. 掌握SUID/SGID/Sticky特殊权限位含义、四进制chmod写法、安全风险
5. 掌握SELinux三种工作模式、配套查询修改命令
6. 分清ACL访问控制列表、能力表存储逻辑（按客体/按主体）
7. 看懂RBAC分层结构，能修改Python RBAC权限校验脚本
8. 结合BLP模型完成DAC/MAC场景案例分析

## 二、核心理论考点
### 1. 访问控制基础概念
#### （1）访问控制三要素（必考选择题）
1. 主体Subject：主动发起访问的实体，用户、进程、服务
2. 客体Object：被动被访问的资源，文件、数据库、端口、打印机
3. 访问操作：主体对客体执行动作，读r、写w、执行x、创建、删除、修改

#### （2）三层访问控制层级
1. 物理层：门禁、机柜锁，管控机房人员进出
2. 逻辑层：操作系统文件权限、数据库权限，管控资源访问
3. 管理层：权限审批流程、权限申请表，管控权限发放流程

#### （3）完整安全链路
认证（确认你是谁）→ 访问控制/授权（控制你能干什么）→ 审计（记录你做了什么）
区分：认证只核验身份；访问控制是分配、限制操作权限。

### 2. 三大访问控制模型（选择+案例大题核心）
#### 模型1 DAC 自主访问控制 Discretionary Access Control
1. 核心规则：资源所有者自主决定其他主体访问权限
2. 典型实现：Linux rwx文件权限、Windows NTFS DACL
3. 优点：灵活适配普通业务，管理简单
4. 致命缺点：存在权限委派传播问题；所有者授权他人后，无法管控对方复制转发资源；易被木马利用权限横向扩散
5. 委派问题示例：A授权B读文件，B可复制文件转发第三方，A无法拦截

#### Linux标准权限解析（DAC落地实现）
##### 10位权限字符串拆分
位置0：文件类型（-普通文件、d目录、l软链接）
1-3位：文件所有者user权限 r w x
4-6位：同组group权限 r w x
7-9位：其他other权限 r w x
权限数值：r=4 w=2 x=1
示例：-rwxr-x--- = 所有者7(rwx)、组5(r-x)、其他人0(---) → chmod 750

##### 特殊权限标识
1. setuid（SUID，数值4）：所有者执行位显示s，普通用户运行程序时以文件所有者身份运行，典型程序passwd
2. setgid（SGID，数值2）：组执行位显示s，目录下新建文件自动继承目录所属组
3. sticky粘滞位（数值1）：其他执行位显示t，目录内用户仅能删除自身创建文件，典型目录/tmp
4. 无执行权限却设置特殊位，显示大写S/T

##### umask 计算规则（计算题高频）
1. 基础默认值：新文件默认666(rw-rw-rw-)；新目录默认777(rwxrwxrwx)
2. 速算口诀：逐位相减，不够减则取0
3. 示例 umask=027
文件：666 - 027 = 640(rw-r-----)
目录：777 - 027 = 750(rwxr-x---)
4. 规律：umask数值越大，默认权限越严格

#### 模型2 MAC 强制访问控制 Mandatory Access Control
1. 核心规则：系统预设安全标签强制管控，任何用户无权修改权限策略
2. 典型实现：SELinux、BLP保密性模型、Biba完整性模型、Windows MIC强制标签
3. 优点：安全等级极高，无法人为绕过权限策略
4. 缺点：灵活性差、配置复杂、运维成本高
5. 适用场景：军队、政府涉密等高安全等级单位

##### SELinux三种工作模式
1. Enforcing强制模式：违反安全策略直接拦截操作
2. Permissive宽容模式：仅记录日志，不阻断操作（调试专用）
3. Disabled关闭模式：完全不生效
配套命令
getenforce 查看当前运行模式
setenforce 0/1 临时切换宽容/强制模式
ls -Z 查看文件安全上下文
ps -Z 查看进程安全上下文

##### BLP模型（MAC经典保密性模型）
两条核心规则：
1. 简单安全规则 No Read Up：不能读取安全级别高于自身的文件（高密不能读）
2. *特性 No Write Down：不能写入安全级别低于自身的文件（高密不能向下写，防止泄密）
示例：机密用户可读秘密文件，不可写入公开文件

#### 模型3 RBAC 基于角色访问控制 Role-Based Access Control
1. 三层核心关系：用户 → 角色 → 权限；权限直接绑定角色，用户分配对应角色即可获得权限
2. 四级分层标准
RBAC0：基础模型，用户-角色-权限映射
RBAC1：增加角色继承，上级角色自动拥有下级全部权限
RBAC2：增加约束，静态职责分离SSD、动态职责分离DSD
RBAC3：整合RBAC1+RBAC2完整模型
3. 优点：贴合企业组织架构、权限管理轻量化、完美适配最小权限原则
4. 适用场景：企业OA、ERP、内部管理系统

### 3. 访问控制矩阵两种存储形式
以「主体、客体、操作」构成二维矩阵
1. ACL访问控制列表：按客体（列）存储，每个资源附带可访问用户清单；Linux文件权限、Windows共享DACL
记忆：ACL=文件身上挂谁能访问我的名单
2. 能力表Capability：按主体（行）存储，每个用户持有可访问资源清单；票据、令牌类认证系统
记忆：能力表=用户手里持有可访问资源凭证
