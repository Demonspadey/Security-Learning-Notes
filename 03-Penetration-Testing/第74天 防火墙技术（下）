## 一、今日核心目标
1. 理清iptables 4表5链完整数据流，区分每张表、每条链的实际流量处理场景
2. 独立编写Linux主机、Linux网关两套iptables过滤规则（白名单加固规范）
3. 区分SNAT/DNAT/MASQUERADE流量修改逻辑、适用网络拓扑
4. 掌握Linux网关NAT组网完整配置流程，包含内核转发开关
5. 结合防火墙边界场景，区分旁路/串联类安全设备部署逻辑

## 二、上午核心实操理论
### Part1 iptables 4表5链流量架构
#### 1.1 四张功能表（处理优先级 raw > mangle > nat > filter）
1. filter 默认过滤表
    功能：管控数据包放行/丢弃，做边界访问控制
    绑定流量链：INPUT、OUTPUT、FORWARD
    实操场景：服务器端口放行、内网跨网段访问限制
2. nat 地址转换表
    功能：修改数据包源/目的IP、端口，实现内网上网、端口映射发布服务
    绑定流量链：PREROUTING、POSTROUTING、OUTPUT
    实操场景：企业网关、公网映射内网Web服务器
3. mangle 报文标记修改表
    功能：修改报文TTL、TOS、自定义流量标记
    绑定流量链：全部5条链，日常运维极少使用
4. raw 原始连接跟踪表
    功能：跳过内核conntrack连接跟踪机制
    绑定流量链：PREROUTING、OUTPUT，运维场景几乎不用

#### 1.2 五条流量链（数据包完整流经顺序）
1. PREROUTING：路由决策**之前**处理流量，仅用于DNAT修改目标IP
2. INPUT：目标IP为本机的入站流量，服务器本地端口过滤使用
3. FORWARD：经过本机路由转发、既不来自本机也不发给本机的跨网段流量
4. OUTPUT：本机进程主动向外发起的出站流量
5. POSTROUTING：路由决策完成、网卡出站前处理，仅用于SNAT修改源IP
