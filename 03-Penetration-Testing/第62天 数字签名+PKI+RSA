## 一、核心目标
1. 掌握数字签名的核心原理、特性与完整流程，明确与加密的边界区别
2. 熟记RSA密钥生成逻辑、四大核心公式，可独立完成手工计算
3. 掌握PKI体系全组件职责，厘清CA/RA/CRL/OCSP的功能边界
4. 熟练使用openssl完成证书生成、签名校验实操

---
## 二、核心理论考点
### 1. 数字签名核心体系
- **技术基础**：基于非对称加密体制实现
- **三大核心作用**：身份真实性、数据完整性、行为不可否认性
  > 注意：数字签名**不提供数据机密性**，不能替代加密
- **标准流程**：
  发送方：原文 → 生成哈希摘要 → 私钥加密摘要 → 生成数字签名
  接收方：原文重新算哈希 + 公钥解密签名得原哈希 → 对比两个哈希是否一致
- **典型应用**：电子合同签署、软件安装包防篡改、安全邮件、身份认证

### 2. RSA算法核心规则
#### （1）密钥生成六步流程
1. 选取两个保密大质数 p、q
2. 计算公开模数 n = p × q
3. 计算保密欧拉函数 φ(n) = (p-1)(q-1)
4. 选取公开指数 e，要求 e 与 φ(n) 互质
5. 计算保密私钥 d，d 是 e 关于 φ(n) 的模逆元
6. 公钥 (n,e) 公开，私钥 (n,d)、p、q、φ(n) 严格保密

#### （2）四大核心公式（不可混淆）
| 操作 | 所用密钥 | 公式 |
|------|----------|------|
| 加密 | 接收方公钥 | C = M^e mod n |
| 解密 | 接收方私钥 | M = C^d mod n |
| 生成签名 | 发送方私钥 | S = M^d mod n |
| 验证签名 | 发送方公钥 | M = S^e mod n |

#### （3）安全基础
基于大整数质因数分解难题，密钥长度越长安全性越高，典型密钥长度2048位。

### 3. PKI公钥基础设施
#### （1）核心组件与职责
| 组件 | 全称 | 核心职责 |
|------|------|----------|
| CA | 证书认证机构 | PKI核心信任根，负责证书签发、吊销、全生命周期管理 |
| RA | 注册机构 | 用户身份初审、受理证书申请，无签发权限 |
| CRL | 证书撤销列表 | CA定期发布，记录未到期但已吊销的证书，批量离线校验 |
| LDAP | 目录服务 | 存储、发布公开证书信息，提供查询服务 |

#### （2）证书校验机制对比
- **CRL**：批量离线校验，适合全量证书定期核查，实时性弱
- **OCSP**：在线证书状态协议，单张实时查询，实时性强，适合高频校验场景

#### （3）数字证书核心作用
绑定公钥与用户身份，证明公钥的合法归属，解决公钥分发的信任问题。

---
