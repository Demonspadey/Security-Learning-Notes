## 一、当日核心原则
不堆砌已学重复知识点，专注 **SQL注入基础绕过 + MySQL元数据表核心原理**，吃透底层逻辑，不赶进度，轻量化学习。

## 二、核心知识点

### 1. SQL注入全维度危害
1.  敏感业务数据泄露
2.  非法越权查询
3.  数据篡改、删除
4.  登录权限绕过
5.  服务器控制权窃取

### 2. 基础WAF绕过原理（理解层面）
- **关键字大小写绕过**
  利用WAF关键字匹配大小写敏感的缺陷，混搭关键字大小写（如`SeLeCt`），实现拦截绕过。
- **内联注释 `/* */` 绕过**
  通过注释包裹关键字（如`/*UnIoN*/`），规避WAF关键字检测，数据库仍可正常解析执行。

### 3. URL编码与特殊字符绕过基础逻辑
通过对关键字/特殊字符进行URL编码变形，突破基础字符拦截规则。

### 4. MySQL `information_schema` 元数据表核心
- 是MySQL系统自带的元数据库，专门存储所有数据库、数据表、字段的结构信息。
- 手工SQL注入查询库表字段的核心依赖，理解其存储逻辑即可掌握注入查数据的底层逻辑。

---

## 三、当日复盘要求
1.  能口述两种SQL注入基础绕过的核心原理；
2.  清晰说明 `information_schema` 元数据表的作用；
3.  熟练背诵SQL注入核心危害及软考简答要点；
4.  理解Python代码逻辑，看懂危险字符检测逻辑；
5.  思考并理解二次注入底层逻辑（无需写代码）。

## 四、实战复盘。
安装安全狗在虚拟操作，pikachu-SQL字符串注入这关开的安全狗尝试
可能是因为版本原因很多基本的绕过方法不太行，在查询字段数正常回显，查回显位时union select被拦截
因为是GET方式提交的，就用+代替空格提交的
先尝试:1'+unions%23a%0Aelect+1,2%23进行尝试绕过，WAF直接拦截，证明换行加#注释不行
尝试2：1'+union%23a%0A/*select*/+1,2%23还是被WAF拦截，加上内联注释也不行
尝试3: 1"+union%23a%0A/*!/*!select+1,2*/%23绕过成功，因此应该还可以尝试其他方式，只要被注释的select不被WAF识别为select
得知回显位后又尝试爆库名
尝试1：1"+union%23a%0A/*!/*!select+1,database/*()*/*/%23  WAF直接拦截，需要思考怎样分开database与()
尝试2: 1"+union%23a%0A/*!/*!select+1,schema()*/%23 尝试了很多种方法不是拦截就是语句报错，需要后续再思考方向问题，
然后把database()
换成了schema()爆出了库名，证明有时候可以更换不同的相同代替尝试绕过。
后续安全狗WAF绕过还需要精进。
---
