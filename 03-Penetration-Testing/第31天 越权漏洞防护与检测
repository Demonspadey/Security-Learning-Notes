## 一、核心目标
1.  吃透水平越权、垂直越权的核心定义、触发逻辑与本质区别
2.  熟记越权漏洞通用成因、标准修复话术、软考+面试答题模板
3.  掌握手工越权检测完整流程，能独立判断漏洞存在性
4.  理解访问控制核心逻辑，建立后端强制鉴权的安全思维

---

## 二、核心理论精讲

### 1. 两类越权核心定义
- **水平越权**：同权限等级用户之间，系统未校验数据归属，用户A可非法查看、修改、删除用户B的业务数据
- **垂直越权**：低权限用户绕过角色校验，非法访问高权限功能接口，普通用户直接越权获得管理员权限

### 2. 越权漏洞根本原因
后端接口仅校验用户登录状态，**不校验数据归属与角色权限**，过度信任前端传入的`username`、`id`、`uid`、`role`等可控参数，导致访问控制失效。

### 3. 越权漏洞典型场景
- 修改`userid`、`username`参数，查看他人订单、个人资料
- 篡改`role`参数，低权限用户执行高权限审核、管理操作
- 直接访问未做权限拦截的管理员后台路径，实现提权

### 4. 通用检测思路
1.  抓包找到用户/数据相关参数（`username`、`id`、`uid`）
2.  修改为其他用户参数发送请求
3.  能看到他人数据 → 水平越权存在
4.  低权限用户访问管理员接口 → 垂直越权存在

### 5. 标准防护与修复方案
- 后端从Session/Token中获取用户身份，**不依赖前端传入参数判断权限**
- 接口强制校验数据归属，仅允许用户访问自身对应资源
- 所有接口增加角色权限拦截，禁止低权限访问高权限接口
- 遵循最小权限原则分配权限，杜绝默认超额授权

---
