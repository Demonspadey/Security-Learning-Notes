# Day4 实战复盘：XSS防御与绕过（DVWA Medium/High）

---

## 一、核心知识点
- **XSS防御核心手段**：
  1.  输入过滤（黑名单/白名单校验）
  2.  输出编码（HTML实体转义）
  3.  CSP内容安全策略
  4.  HttpOnly Cookie（会话保护）
- **编码绕过原理**：
  - 对过滤逻辑做“语法变形”，如标签拆分、事件替代、双写/大小写混淆、HTML/URL编码等，让黑名单规则失效。

---

## 二、DVWA 实操过程
### 1. Medium 等级
- **问题**：对 `<script>` 标签做了简单的黑名单过滤
- **绕过方法**：
  1.  **双写绕过**：`scr<script>ipt>alert(1)</scr</ipt>`
     - 原理：服务器只做一次过滤，把中间的`<script>`删掉，拼接后恢复为完整的`<script>`标签。
  2.  **事件绕过**：`< img src=x onerror=alert(1)>`
     - 原理：黑名单仅拦截`script`标签，对`img`标签和`onerror`事件没有限制。

### 2. High 等级 DOM型
- **问题**：过滤了default参数值
- **绕过思路**：使用&分割参数（img src=x onerror=alert(1)）触发执行。

---

## 三、实战题易错点复盘
1.  **输出编码防御的绕过场景**:
    - 输出编码会把 `<`/`>` 转成 `&lt;`/`&gt;`，但在 `href`、`src` 等属性中，`javascript:` 伪协议不会被完全拦截，因此可以绕过。
2.  **CSP绕过的前提**:
    - 只有当CSP策略允许 `'unsafe-inline'` 或未限制 `img`/`svg` 等标签的事件时，`onerror`/`onload` 这类事件才能生效。

---

## 四、今日总结
- 掌握了DVWA中高级XSS的核心绕过逻辑，理解了“过滤→变形绕过”的对抗思路。
- 熟悉了不同防御手段的局限性，为后续更复杂的WAF绕过打下了基础。
