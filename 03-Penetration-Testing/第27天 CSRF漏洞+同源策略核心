## 一、核心目标
1.  吃透同源策略判定规则、安全作用与边界限制
2.  掌握CSRF攻击完整链路、生效三大必要条件
3.  精准区分CSRF与XSS的核心差异（软考高频对比考点）
4.  理解三类CSRF防护机制原理与适用场景

---

## 二、核心知识点

### 1. 同源策略核心规则
- 同源判定三要素：**协议、域名、端口必须完全一致**
- 核心作用：限制跨源DOM操作、Cookie读取、AJAX请求交互，是Web安全底层防线
- 关键边界：不拦截图片加载、CSS引用、表单提交、静态资源跨域加载，仅限制敏感读写操作

### 2. CSRF漏洞本质与攻击条件
- 全称：跨站请求伪造
- 核心逻辑：利用浏览器**自动携带同源Cookie**的特性，冒用已登录用户身份执行未授权操作
- 攻击生效三大必要条件：
  1. 用户已登录目标站点，浏览器保留有效会话Cookie
  2. 服务端仅校验Cookie，无额外身份校验
  3. 请求无随机、不可预测的防伪标识（Token）

### 3. CSRF与XSS核心差异对比
| 对比维度 | XSS 跨站脚本 | CSRF 跨站请求伪造 |
|---------|-------------|------------------|
| 漏洞本质 | 恶意脚本注入执行，代码注入类漏洞 | 请求伪造冒用身份，请求校验类漏洞 |
| 攻击载体 | 注入并执行JS脚本 | 构造伪造请求，无需脚本执行 |
| 数据能力 | 可窃取Cookie、读取页面数据 | 不窃取数据，仅冒用权限执行操作 |
| 防护核心 | 输入过滤、输出编码、CSP策略 | Token校验、SameSite、来源校验 |

### 4. CSRF攻击完整链路
用户登录目标站点 → 浏览器保存会话Cookie → 访问攻击者恶意页面 → 自动向目标站发送伪造请求 → 浏览器自动携带同源Cookie → 服务端校验通过 → 执行未授权操作（改密、转账、删数据）

### 5. CSRF基础防护机制
1.  **CSRF Token校验**（主流首选）：服务端生成随机不可预测Token，请求必须携带并校验
2.  **SameSite Cookie**：限制Cookie跨站携带，从源头阻断利用条件
3.  **Referer来源校验**：验证请求来源域名，简易辅助防护
4.  高危操作二次验证：验证码、密码二次确认，提升攻击门槛

---
