## 一、核心目标
1. 掌握认证两大组成：标识+鉴别；熟记认证三要素
2. 掌握Kerberos四大实体、完整6步流程、票据类型与对应攻击
3. 区分MAC/HMAC与数字签名的安全能力差异
4. 掌握多种实体认证方式、优缺点、适用场景
5. 理解S/Key一次性口令作用：抵御重放攻击
6. 掌握Windows/Linux账户安全配置、HMAC Python代码实操

## 二、核心理论考点
### 1. 认证基础概念
#### （1）认证两部分
标识（Identification）：实体唯一身份标记，解决「你是谁」
示例：用户名、ID、设备MAC、账号编号
鉴别（Authentication）：验证身份真伪，解决「证明你是你」
示例：密码、指纹、U盾校验
公式：完整身份认证 = 标识 + 鉴别

#### （2）认证三要素
1. 所知 Something you know：口令、PIN、密保问题
2. 所有 Something you have：U盾、动态令牌、手机验证码
3. 固有 Something you are：指纹、人脸、虹膜（生物特征）
双因素认证：**两类不同要素**组合，安全性大幅提升；同类叠加不算多因子。

#### （3）挑战-应答机制
流程：服务器下发随机挑战值 → 客户端使用密钥运算生成应答返回 → 服务端比对校验
作用：引入随机数，抵御重放攻击。

#### （4）认证、授权、审计区分
1. 认证 Authentication：你是谁（核验身份，出示身份证）
2. 授权 Authorization：你能干什么（分配资源访问权限，门禁权限）
3. 审计 Audit：你干了什么（全操作日志记录，事后追溯，监控录像）
执行顺序：认证 → 授权 → 审计

### 2. Kerberos协议（高频大题考点）
#### （1）基础定义
基于对称加密、依赖可信第三方KDC的内网单点登录认证协议。
#### （2）四大实体
1. Client 客户机：发起访问请求的用户终端
2. AS 认证服务器：校验用户身份，发放TGT票据
3. TGS 票据授予服务器：接收TGT，发放业务服务票据
4. SS 应用服务器：校验服务票据，提供业务资源
补充：AS + TGS 合称为 KDC（密钥分发中心）

#### （3）完整6步流程
1. Client → AS：AS-REQ，发送用户名请求认证
2. AS → Client：AS-REP，返回加密TGT（外层用用户密码哈希加密）
3. Client → TGS：TGS-REQ，携带TGT请求访问目标服务
4. TGS → Client：TGS-REP，返回服务票据
5. Client → SS：AP-REQ，携带服务票据发起业务访问
6. SS → Client：AP-REP，双向认证确认，开放访问权限

记忆口诀：先找AS拿TGT，再找TGS拿服务票据，最后找应用服务器访问资源。

#### （4）票据加密规则
1. TGT内部数据：TGS自身密钥加密；外层数据包：用户密码哈希加密
2. 服务票据内部：目标应用服务器长期密钥加密

#### （5）两种票据伪造攻击
1. 黄金票据：伪造TGT，获取域内全部权限，危害最大
2. 白银票据：伪造服务票据，仅能访问指定单一业务服务

#### （6）核心特点
全程对称加密；无天然不可否认性；密码不会在网络明文传输；依靠时间戳+认证器防重放。

### 3. MAC / HMAC 报文认证
#### （1）基础概念
MAC（消息认证码）：共享对称密钥+原始消息，生成固定长度校验码
HMAC：基于哈希实现的MAC（HMAC-SHA256工业主流）
安全能力：①报文完整性 ②数据源身份可信
无法实现：不可否认性（双方共享同一密钥，均可生成合法MAC）

#### （2）MAC vs 数字签名对比
|对比项|MAC/HMAC|数字签名(RSA/SM2)|
| ---- | ---- | ---- |
|密钥体系|对称共享密钥|非对称公私钥（签名私钥，验签公钥）|
|不可否认|不支持|支持（私钥唯一归属签名人）|
|运算性能|速度快|速度慢|
|典型场景|接口请求校验、报文防篡改|电子签章、合同防抵赖|

### 4. 实体认证方式汇总
1. 静态口令认证：用户名+密码；缺点：易暴力、嗅探、重放；适合低安全需求内网
2. 生物特征认证：指纹/人脸；高安全，缺点：生物特征不可撤销
3. 智能卡/U盾：硬件存储密钥；物理层面安全，成本较高
4. X.509证书认证：PKI体系公钥证书；高安全，支撑HTTPS、政务系统
5. 双因素2FA：两类认证要素组合；当前推荐安全方案，抵御密码泄露风险
6. SSO单点登录：一次认证，多系统免重复登录；便捷，存在单点故障风险

### 5. S/Key一次性口令协议
核心机制：哈希链倒序使用，每一条口令仅生效一次，使用后永久失效
核心防护目标：抵御重放攻击（即便口令被抓包截获，重复发送无效）
区分：不属于静态验证码，口令随登录逐次变更

### 6. 身份相关协议区分
- Kerberos：内网域单点登录认证协议
- S/Key：一次性口令身份认证协议
- X.509：数字证书标准，用于证书身份认证
- IPSec：网络层安全加密协议，**不属于身份认证协议**
