## 一、核心目标
1.  彻底区分三类XSS的成因、触发、存储、危害、防护全链路
2.  吃透同源策略、CSRF攻击逻辑、与XSS的本质差异
3.  固化漏洞对比框架，做到口述无卡顿、原理无混淆
4.  扫清所有易混点、边界知识点，无遗留疑问

---

## 二、XSS全体系复盘

### 1. 三类XSS核心定义与触发逻辑
- **反射型XSS**：非持久化漏洞，恶意Payload拼接在URL参数中，用户点击后后端直接回显渲染，单次访问触发，仅危害当前用户，常见于搜索、跳转、参数回显场景。
- **存储型XSS**：持久化漏洞，Payload提交后存入服务器数据库/文件，所有访问该页面的用户均会触发，危害范围最大、隐蔽性强，常见于评论、留言、用户资料、公告场景。
- **DOM型XSS**：纯前端漏洞，Payload不经过后端服务器处理，由前端JS直接读取URL参数并拼接DOM节点触发，HTTP流量无明显恶意特征，WAF极易漏报，隐蔽性最强。

### 2. 三类XSS完整对比表
| 对比维度 | 反射型XSS | 存储型XSS | DOM型XSS |
|---------|-----------|-----------|----------|
| 持久化 | 非持久化 | 持久化存储 | 非持久化 |
| 后端交互 | 经过后端渲染 | 经过后端渲染 | 不经过后端 |
| 存储位置 | URL参数中 | 服务器数据库 | 前端URL/内存 |
| 触发条件 | 用户点击恶意链接 | 访问对应页面即可 | 用户点击链接+前端JS解析 |
| 危害范围 | 单个用户 | 全部访问用户 | 单个用户 |
| 防护核心 | 输出转义+输入过滤 | 输出转义+输入过滤 | 前端安全渲染+输入校验 |

### 3. XSS防护全链路总结
- 输入层：白名单校验，过滤`< > ' "`等高危字符
- 输出层：HTML/JS/URL实体编码转义
- Cookie层：设置HttpOnly、Secure属性
- 安全策略：配置CSP限制脚本执行
- 开发规范：避免innerHTML等危险API，使用安全框架

---

## 三、CSRF全体系复盘

### 1. 同源策略核心规则
- 判定三要素：**协议、域名、端口必须完全一致**
- 核心作用：限制跨源DOM操作、Cookie读取、AJAX请求交互
- 边界限制：不拦截图片加载、表单提交、静态资源引用

### 2. CSRF漏洞本质与攻击条件
- 核心逻辑：利用浏览器自动携带同源Cookie的特性，冒用已登录用户身份执行未授权操作
- 攻击生效三大必要条件：
  1. 用户已登录目标站点，浏览器保留有效会话Cookie
  2. 服务端仅校验Cookie，无额外身份校验
  3. 请求无随机、不可预测的防伪标识（Token）

### 3. CSRF与XSS核心差异对比
| 对比维度 | XSS 跨站脚本 | CSRF 跨站请求伪造 |
|---------|-------------|------------------|
| 漏洞本质 | 恶意脚本注入执行，代码注入类漏洞 | 请求伪造冒用身份，请求校验类漏洞 |
| 攻击载体 | 注入并执行JS脚本 | 构造伪造请求，无需脚本执行 |
| 数据能力 | 可窃取Cookie、读取页面数据 | 不窃取数据，仅冒用权限执行操作 |
| 防护核心 | 输入过滤、输出编码、CSP策略 | Token校验、SameSite、来源校验 |

### 4. CSRF攻击完整链路与防护
- 攻击链路：用户登录目标站点 → 浏览器保存会话Cookie → 访问攻击者恶意页面 → 自动向目标站发送伪造请求 → 浏览器自动携带同源Cookie → 服务端校验通过 → 执行未授权操作
- 防护措施：
  1. CSRF Token校验（主流首选）
  2. 配置SameSite Cookie属性
  3. Referer来源校验
  4. 高危操作二次验证（验证码/密码确认）

---
