## 一、核心目标
1. 掌握对称加密与非对称加密的核心区别与适用场景
2. 熟记国密SM1-SM4的类型与用途
3. 掌握RSA密钥生成公式，可独立完成手工计算
4. 掌握OpenSSL密钥生成与加解密实操
5. 熟记主机安全基础命令与注册表核心路径

---
## 二、核心理论考点
### 1. 对称加密 vs 非对称加密
| 对比维度 | 对称加密 | 非对称加密 |
|----------|----------|------------|
| 密钥数量 | 1个，加解密共用同一密钥 | 2个，公钥+私钥成对出现 |
| 运算速度 | 极快，适合大批量数据 | 慢，仅适合小数据/短文本 |
| 密钥分发 | 困难，需安全信道传递 | 容易，公钥可公开分发 |
| 核心用途 | 大文件加密、流量加密 | 密钥交换、数字签名、身份认证 |
| 代表算法 | DES、3DES、AES、SM1、SM4 | RSA、ECC、SM2 |

**速记口诀**：对称单钥速度快，非对称双钥保安全

### 2. 国密SM系列算法
| 算法 | 类型 | 核心用途 | 关键参数 |
|------|------|----------|----------|
| SM1 | 对称分组密码 | 硬件芯片加密 | 128位密钥，算法不公开 |
| SM2 | 非对称椭圆曲线密码 | 数字签名、密钥协商 | 256位密钥，算法公开 |
| SM3 | 杂凑（哈希）算法 | 完整性校验、摘要生成 | 输出256位，算法公开 |
| SM4 | 对称分组密码 | 业务数据加密 | 128位分组+128位密钥，算法公开 |

**速记口诀**：1对称4对称，2非对称3哈希

### 3. RSA核心公式
1. 模数：\( n = p \times q \)（p、q为大质数，保密）
2. 欧拉函数：\( \varphi(n) = (p-1) \times (q-1) \)（保密）
3. 公钥指数：e，与φ(n)互质（公开）
4. 私钥指数：d，满足 \( e \times d \equiv 1 \pmod{\varphi(n)} \)（保密）
5. 加密：\( C = M^e \mod n \)（公钥加密）
6. 解密：\( M = C^d \mod n \)（私钥解密）
7. 安全基础：大整数质因数分解难题

### 4. 哈希函数核心特性
1. 单向不可逆：无法从哈希值还原原文
2. 定长输出：无论原文多长，输出长度固定
3. 抗碰撞性：计算上难以构造两个不同原文产生相同哈希值
4. 雪崩效应：原文微小改动，哈希值会发生巨大变化
- 常见算法：MD5（128位）、SHA-1（160位）、SHA-256（256位）、SM3（256位）

### 5. 数字信封技术
- 核心思路：结合对称加密的速度优势与非对称加密的密钥分发优势
- 流程：
  1. 发送方生成随机对称会话密钥，加密业务数据
  2. 用接收方公钥加密会话密钥
  3. 将加密后的数据+加密后的会话密钥一同发送
  4. 接收方用私钥解密得到会话密钥，再解密业务数据

---
