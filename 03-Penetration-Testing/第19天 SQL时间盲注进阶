## 一、核心目标
掌握SQL时间盲注的原理与实战，实现：
1.  理解时间延迟盲注的触发逻辑与判断特征
2.  熟练使用常用延时函数构造Payload
3.  掌握布尔盲注与时间盲注的完整手工猜解流程
4.  靶场实操：独立完成时间盲注整库脱库流程

---

## 二、核心知识点解析

### 1. 时间盲注原理
时间盲注属于SQL盲注的一种，适用于页面**无内容回显、无布尔状态变化**的场景。
核心逻辑：
-  向目标注入SQL延时语句
-  通过页面响应时间的差异，判断注入语句的执行结果（真/假）
-  响应延迟=条件为真；响应正常=条件为假

### 2. 常用延时函数与Payload
| 数据库 | 常用延时函数 | 基础Payload示例 |
|--------|--------------|-----------------|
| MySQL  | `sleep(n)` / `benchmark(count, expr)` | `and if(1=1, sleep(3), 1)` |
| SQL Server | `waitfor delay '0:0:3'` | `if(1=1) waitfor delay '0:0:3'` |
| Oracle | `dbms_lock.sleep(3)` | `and 1=utl_inaddr.get_host_name((select sys.user from dual))` |

### 3. 布尔盲注 vs 时间盲注对比
| 类型 | 判断依据 | 适用场景 |
|------|----------|----------|
| 布尔盲注 | 页面内容/状态变化 | 页面存在两种不同回显状态 |
| 时间盲注 | 页面响应时间差异 | 页面无回显、无状态变化 |

### 4. 时间盲注完整手工流程
1.  **注入判断**：测试延时语句是否生效，确认存在时间盲注
    - Payload：`?id=1' and sleep(3) --+`
2.  **闭合方式确认**：判断单/双引号、括号等闭合方式
3.  **字段数/查询位置判断**：`order by` 语句猜解列数
4.  **数据猜解（逐位二分法）**
    - 猜解数据库名：1' and if(ascii(substr(database(),1,1))>97, sleep(3), 0)-- -
    - 猜解表名、字段名、数据值
5.  **整库脱库**：编写脚本批量遍历，获取全库数据

---

## 三、靶场实操要点
-  独立完成**单表数据脱库**：从库名到字段值的完整流程
-  掌握二分法猜解技巧，大幅缩短手工猜解时间
-  熟练手写延时Payload，理解不同数据库函数差异

## 四、实战复盘
-  Pikachu盲注，时间注入练习，手工注入猜库，表，列，内容
   过程中明白了substr函数中三个变量为（字符串，起始位置，截取长度）；
   limit 配合表名更改 limit 0,1 第二个表为limit 1,1
   每个表中需要获取第二个字母时候就改substr 第一个字母(1,1) ,第二个字母(2,1),第三个字母（3,1）
