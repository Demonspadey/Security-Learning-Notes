## 一、核心理论
### 1.攻击分类（教材四大类）
信息泄露、完整性破坏、拒绝服务DoS/DDoS、非法使用资源。
攻击分为主动攻击(DDoS/ARP欺骗)、被动攻击(流量嗅探窃听)。

### 2.DDoS分层原理
1. 网络层：SYN Flood、ICMP Flood → 耗尽TCP半连接队列
2. 传输层：UDP Flood、DNS放大攻击 → 占满出口带宽
3. 应用层：HTTP Flood(CC攻击) → 消耗应用、数据库资源
> DDoS依靠僵尸网络（肉鸡集群）批量发包实现分布式攻击。

### 3.中间人攻击三类
1. ARP欺骗：局域网篡改网关MAC，劫持内网流量
2. DNS劫持/污染：篡改域名解析，跳转钓鱼IP
3. SSL剥离：强制HTTPS降级HTTP，抓取明文账号密码

1. 在攻击机上执行以下命令，将<目标IP>替换为靶机IP地址：
   ```bash
   sudo hping3 -S -p 80 --flood --rand-source <目标IP>
   ```
   · -S：表示发送的是TCPSYN包。
   · -p 80：指定攻击目标端口，这里是模拟对Web服务的攻击。
   · --flood：以最快的速度发送数据包，不加限制。
   · --rand-source：随机伪造源IP地址。

第二步：观察攻击效果

· 关键指标：靶机80端口会出现大量处于SYN_RECV状态的半连接。

1. 统计半连接数量：在靶机上打开一个新终端，执行命令查看半连接数量：
   ```bash
   netstat -nat | grep :80 | grep SYN_RECV | wc -l
   ```
   攻击发起后你会发现它在快速攀升。
2. 检查端口状态：你也可以直接用netstat命令，直观地查看80端口有哪些状态异常的连接：
   ```bash
   netstat -nat | grep :80
   ```
   会看到大量的连接处于SYN_RECV状态。

第三步：启用并验证防御机制

启用主动防御来对抗攻击。

方案一：开启内核SYN Cookie (核心防御)

SYN Cookie是抵御SYN Flood最核心的机制，它通过一种无状态的算法来验证请求，从而避免内存资源耗尽。

· 原理：启用后，当半连接队列满时，系统不分配资源，而是给客户端发一个加密的Cookie。只有能正确返回这个Cookie的客户端才会被认为合法并建立连接。

1. 临时启用（重启后失效）：
   ```bash
   sudo sysctl -w net.ipv4.tcp_syncookies=1
   ```
2. 永久启用：
   ```bash
   echo "net.ipv4.tcp_syncookies = 1" | sudo tee -a /etc/sysctl.conf
   sudo sysctl -p
   ```
3. 验证状态：
   ```bash
   sysctl net.ipv4.tcp_syncookies
   ```
   输出应为 net.ipv4.tcp_syncookies = 1。
4. 同步调整半连接队列：增大tcp_max_syn_backlog的值，为SYN队列“扩容”。
   ```bash
   sudo sysctl -w net.ipv4.tcp_max_syn_backlog=4096

第四步：启用iptables防火墙规则
1. 限制单IP连接速率：例如，限制每个源IP每分钟只允许建立10个新连接。
   ```bash
   sudo iptables -A INPUT -p tcp --syn -m limit --limit 10/min -j ACCEPT
综合验证防御效果

所有配置完成后，我们需要来验证防御是否真正生效。

1. 再次发起攻击：回到攻击机，执行第一步中的hping3命令，再次发起SYN Flood攻击。
2. 观察半连接队列情况：在靶机上重新执行之前查看SYN_RECV状态的命令：
   ```bash
   netstat -nat | grep :80 | grep SYN_RECV | wc -l
   ```
   你会发现，这个数字虽然可能比平时高，但会稳定在一个上限值，不会再无限制地增长。
3. 验证SYN Cookie生效：执行以下命令，查看系统处理SYN包溢出的统计数据：
   ```bash
   netstat -s | grep "SYNs to LISTEN"
   ```
   如果这个数字在攻击过程中不再增加或增加缓慢，就说明SYN Cookie机制已经成功接管了溢出的连接。
