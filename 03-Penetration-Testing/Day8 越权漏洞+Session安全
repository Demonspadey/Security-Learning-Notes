## 🌐 网安实操部分（越权漏洞+Session安全）
### 一、实战题易错点
1.  **水平/垂直越权判断标准与测试方法**
    - 水平越权：同权限级别用户之间的越权访问，核心是**权限等级相同、访问资源不同**。
      - 测试方法：注册2个同权限用户，登录用户A后修改请求中的用户ID/订单ID等参数为用户B的ID，验证是否能获取用户B的资源。
    - 垂直越权：低权限用户越权访问高权限用户的资源，核心是**权限等级不同、访问资源越界**。
      - 测试方法：使用普通用户登录，尝试访问管理员后台路径，或修改请求中的角色参数（如`role=admin`），验证是否能获取高权限功能。

2.  **越权漏洞的真实成因与修复要点**
    - 成因：服务端仅通过前端传入的`user_id`、`order_id`等参数判断资源归属，未校验当前登录用户与资源的所属关系，完全信任前端参数。
    - 修复：服务端必须从Session中获取当前登录用户的ID，拒绝直接使用前端传入的ID作为查询条件，对每一个资源请求做归属校验。

3.  **Session与Cookie的核心关系及安全误区**
    - 关系：SessionID存储在服务端，Cookie是客户端存储SessionID的容器；Session也可通过URL重写（`?sessionid=xxx`）传递，并非完全依赖Cookie。
    - 误区纠正：会话劫持≠窃取Cookie，会话固定攻击的核心是复用用户登录前后的SessionID，防御关键是登录后强制刷新SessionID。

4.  **Session安全基础实操要点**
    - 浏览器F12抓包查看Cookie中的SessionID，检查其随机性、过期时间和是否设置`HttpOnly`/`Secure`属性。
    - Session固定攻击流程：攻击者提供固定SessionID → 用户使用该ID登录 → 攻击者复用该SessionID冒充用户。

---
