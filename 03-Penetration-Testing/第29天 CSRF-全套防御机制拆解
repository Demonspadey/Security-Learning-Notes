## 一、核心目标
1.  逐个吃透CSRF四大防御机制：Token、Referer、SameSite、验证码
2.  掌握每种方案的原理、实现流程、优缺点、绕过风险、适用场景
3.  熟记企业落地安全优先级：Token > SameSite > Referer > 验证码
4.  靶场验证防御逻辑：正常请求通行、伪造请求拦截的完整流程

---

## 二、CSRF四大防御机制全拆解

### 1. CSRF Token 令牌防御（企业首选·核心防护）
- **原理**：服务端生成随机、唯一、与会话绑定、不可预测的令牌，前端请求必须携带，后端校验一致才放行，从根源让攻击者无法伪造合法请求。
- **实现流程**：
  1. 用户登录/访问页面，服务端生成专属Token，绑定会话
  2. Token下发到前端，隐藏在表单、请求头或Cookie中
  3. 前端发起业务请求时必须携带该Token
  4. 服务端校验Token有效性、一致性，校验不通过则拦截请求
- **优缺点**：安全性极高、绕过难度大、兼容性好、不影响正常业务；开发成本稍高，需前后端配合。
- **绕过风险**：若Token未与会话绑定、可预测、全局复用，则可能被绕过。

### 2. Referer 来源校验
- **原理**：通过校验请求头中Referer字段，判断请求来源是否为合法域名，非法来源则拦截。
- **失效场景**：Referer可被伪造、部分浏览器/代理会清空Referer，无法完全信任。
- **优缺点**：实现简单、无业务侵入；但安全性低，仅能作为辅助防护。

### 3. Cookie SameSite 属性
- **Strict模式**：最严格，完全禁止跨站携带Cookie，防御最强，但兼容性差，易破坏正常跳转业务。
- **Lax模式**：均衡模式，允许安全GET请求跨站携带Cookie，禁止POST/PUT/DELETE危险请求，兼顾安全与兼容，企业推荐默认配置。
- **None模式**：关闭限制，任何情况都允许跨站携带Cookie，无CSRF防御能力，必须配合HTTPS+Secure使用。
- **核心作用**：从浏览器层面限制Cookie跨站自动携带，阻断CSRF利用条件。

### 4. 验证码/短信二次校验
- **原理**：强交互机制，强制用户输入验证码/短信验证码，阻断自动化伪造请求。
- **优缺点**：能彻底拦截批量CSRF攻击，安全性极高；用户体验较差，适合改密、转账等高风险操作。
- **适用场景**：高风险操作的兜底防护，不适合高频业务接口。

---

## 三、企业CSRF防御落地优先级
1.  **核心防护：CSRF Token校验**（必须配置，所有写操作接口强制校验）
2.  **浏览器层防护：SameSite=Lax**（全局配置，限制Cookie跨站携带）
3.  **辅助防护：Referer校验**（拦截明显恶意跨域请求）
4.  **兜底防护：高危操作二次验证**（改密、提现、删除数据必须验证码）

---
