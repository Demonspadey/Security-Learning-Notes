## 一、核心基础概念
### 1. 包过滤依据：五元组
源IP、目的IP、源端口、目的端口、传输协议

### 2. 三类防火墙及工作原理
1. 包过滤防火墙
基于五元组校验数据包头部，工作在**网络层、传输层**，规则简单。
2. 状态检测防火墙
在包过滤基础上维护连接状态表，仅放行合法连接回包，安全性更高。
3. 应用代理防火墙
工作在**应用层**，深度解析应用层流量，可识别Web类应用攻击。

### 3. 三大部署模式
1. 路由模式：充当路由器，接口配置IP，支持NAT，多用于网络边界出口。
2. 透明模式：二层桥接，无需配置IP，串接链路，不改动原有网络拓扑。
3. 混合模式：兼具路由、透明两种模式能力，适用于复杂网络。

### 4. 安全策略核心原则
最小开放，默认拒绝：默认阻断所有流量，仅按需放行业务所需IP、端口、协议。

## 二、Windows 防火墙实操
1. 运行框输入 `wf.msc`，打开高级安全Windows防火墙
2. 入站规则 → 新建自定义规则
3. 配置作用域，添加指定远程IP，设置动作为「阻止连接」
4. 保存规则并测试访问效果

## 三、Linux iptables 防火墙实操
1. 查看现有规则
`sudo iptables -L -n -v`
2. 禁止指定IP访问本机
`sudo iptables -A INPUT -s 目标IP -j DROP`
3. 持久化保存规则
`sudo iptables-save > /etc/iptables/rules.v4`

## 四、核心逻辑与易错点
1. 规则匹配顺序：**从上至下，匹配即执行，不再向下遍历**。
2. 分层防护边界：网络层/传输层防火墙无法解析应用层，不能防御SQL注入等Web攻击。
3. 补充方案：针对Web攻击，搭配 WAF（Web应用防火墙）。

## 五、典型场景处置
1. 拦截指定IP+端口：使用 iptables 组合 `-s` 指定源IP、`--dport` 指定目的端口、`-j DROP` 丢弃数据包。
2. 现有网络不改拓扑：选用透明模式防火墙。
3. 网络出口做NAT转发：选用路由模式防火墙。
