## 一、当日学习目标
1. 梳理四类漏洞触发条件、攻击入口与破坏效果
2. 区分漏洞独立特征与组合攻击链路
3. 搭建五层统一Web应用安全防护框架
4. 提炼漏洞判定关键词，快速匹配考题场景
5. 统一危害、防护、对比类标准答题话术

## 二、四大漏洞核心四要素汇总
### 1. 文件上传漏洞
**定义**
系统未严格校验上传文件格式、内容，允许恶意脚本文件上传至服务器。

**成因**
1. 仅前端校验文件格式，后端无二次核验
2. 采用黑名单校验，易被各类方式绕过
3. 未检测文件头部与真实内容
4. 上传目录具备脚本执行权限
5. 文件未随机重命名，无隔离存储机制

**危害**
1. 上传木马后门，配合文件包含获取服务器权限
2. 篡改网站页面、植入恶意内容
3. 窃取业务数据、用户隐私信息
4. 横向渗透内网，扩大攻击范围
5. 超大文件上传引发存储耗尽、拒绝服务

**防护**
1. 后端采用文件后缀白名单校验，杜绝非法格式
2. 放弃前端校验结果，以服务端校验为准
3. 校验文件头与真实内容，防范文件伪装绕过
4. 上传目录关闭脚本执行权限
5. 文件随机哈希重命名，隔离存储目录
6. 限制单文件大小与上传频率

### 2. 文件包含漏洞
**定义**
程序根据用户可控参数动态加载文件，恶意文件被解析执行引发安全风险。
分类：本地包含 LFI、远程包含 RFI

**成因**
1. 文件路径参数可由用户任意控制
2. 未设置合法文件访问白名单
3. 未过滤路径穿越字符、恶意伪协议
4. 服务器开启远程文件包含配置
5. 无目录访问范围限制

**危害**
1. 读取网站源码、配置、账号密码等敏感文件
2. 执行恶意脚本，触发远程代码执行
3. 结合上传木马实现服务器权限接管
4. 数据泄露、业务中断、内网渗透

**防护**
1. 关闭远程文件包含功能
2. 配置可访问文件白名单
3. 过滤../、伪协议等危险字符
4. 限定合法访问目录，禁止跨目录读取
5. 禁用危险文件包含函数

### 3. 目录遍历（路径穿越）漏洞
**定义**
未拦截路径跳转字符，攻击者跨层级读取服务器本地任意文件。

**成因**
1. 文件读取参数用户可控可篡改
2. 未过滤../、..\\等穿越字符
3. 未锁定网站固定访问根目录
4. 缺少路径合法性校验与白名单机制

**危害**
1. 读取系统密码、服务器配置、日志文件
2. 泄露系统版本、网站架构信息
3. 核心业务数据、用户隐私泄露
4. 违反等保规范，引发安全事故

**防护**
1. 过滤所有路径穿越危险字符
2. 对访问路径做标准化解析
3. 锁定站点合法访问根目录
4. 配置文件访问白名单
5. 服务运行账号遵循最小权限原则

### 4. 反序列化漏洞
**定义**
程序直接解析不可信外部序列化数据，对象还原过程触发恶意代码执行。

**成因**
1. 后端直接处理用户可控序列化数据
2. 无数据合法性校验、无签名校验
3. 代码存在可被调用的高危魔术方法

**危害**
1. 执行系统任意命令，远程代码执行
2. 任意文件读写、权限绕过
3. 植入持久化后门，长期控制服务器
4. 内网横向渗透、核心数据泄露
5. 系统瘫痪、拒绝服务攻击

**防护**
1. 拒绝解析一切不可信外部序列化数据
2. 优先使用JSON、XML等安全数据格式
3. 配置可加载类对象白名单
4. 序列化数据增加加密与签名验证
5. 禁用危险魔术方法，及时修复组件漏洞
6. 最小权限运行程序，缩小危害范围

## 三、漏洞易混对比考点
1. 目录遍历 VS 文件包含
- 功能：遍历仅读取文件；包含可读可执行代码
- 危害：遍历只造成信息泄露；包含可直接拿下服务器权限
- 条件：遍历无需特殊配置；RFI需要开启远程包含

2. 文件上传 + 文件包含 组合攻击
上传漏洞负责将木马文件落地服务器，文件包含负责解析运行木马，二者配合实现GetShell控制服务器。

3. 漏洞危害等级排序
反序列化 > 文件包含 > 文件上传 > 目录遍历

## 四、五层Web统一防护体系
1. **输入层**：字符过滤、转义处理、正则校验、参数合法性限制
2. **权限层**：身份校验、会话管控、账号最小权限分配
3. **文件层**：上传校验、路径限制、禁用危险函数
4. **代码层**：摒弃危险写法，统一安全编码规范
5. **部署层**：服务器基线加固、目录权限严格管控
