## 一、核心目标
掌握**SQL布尔盲注的底层原理与手工猜解流程**，理解核心函数用法，固化逻辑链路，为后续盲注脚本编写打基础。

---## 二、核心知识点

### 1. 布尔盲注定义
页面无数据回显、无错误报错，仅通过页面**正常/异常两种状态（真/假）** 判断SQL语句执行结果，逐位猜解库、表、字段、数据的注入方式。

### 2. 必掌握核心函数
- `length()`：获取目标字符串的长度，确定猜解的位数范围
- `substr()`：截取字符串指定位置的字符，实现逐位猜解
- `ascii()`：将字符转换为ASCII码值，方便数值比较判断
- `ord()`：与`ascii()`逻辑一致，用于字符编码比对

### 3. 标准猜解流程
1.  猜解目标库名长度
2.  逐位猜解库名字符内容
3.  猜解表名列表
4.  猜解字段名列表
5.  猜解表中真实数据

### 4. 避坑关键要点
- 引号闭合逻辑（单/双引号、括号闭合）
- SQL逻辑运算符优先级（`and`/`or`的执行顺序）
- 关键字过滤基础绕过（大小写变形、内联注释）
- 严格依靠页面状态差异判断，避免主观猜测
