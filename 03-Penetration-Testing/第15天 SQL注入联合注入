## 一、网安学习

### 核心学习内容
#### 1. SQL注入漏洞核心成因
- 用户可控输入未做过滤、直接拼接至原生SQL语句执行
- 关键底层逻辑：字符串闭合、单引号逃逸、语句篡改

#### 2. 数据库基础前置知识点
- **MySQL高频语法**：`select`、`union`、`and`、`or`、`limit`
- **三类注释符用法**：
  - `#`：单行注释，从`#`到行尾的内容都会被忽略
  - `-- `：单行注释（注意`--`后需要有空格）
  - `/* */`：多行注释，可用于闭合绕过场景

#### 3. SQL注入分类（基础认知）
- **按数据类型划分**：
  - 数字型注入：参数直接作为数值拼接，无引号包裹
  - 字符型注入：参数被单/双引号包裹，需要先闭合引号才能篡改语句
- **按提交方式划分**：
  - GET注入：参数通过URL传递，直接可见
  - POST注入：参数通过请求体传递，需要抓包修改

#### 4. 漏洞危害梳理
- 敏感数据泄露（用户信息、密码、订单数据）
- 业务数据篡改（修改金额、订单状态）
- 后台登录绕过
- 整库脱库
- 权限越权访问

#### 5. 基础防护前置了解
- **原生SQL拼接的致命风险**：直接拼接用户输入会导致语句被篡改
- **防御核心思想**：预编译、参数化查询、ORM框架

#### 6.实战复盘
SQL注入除了 1 and 1=1    1 and 1=2 这种方法判断注入点，还可以1aaaaaaa方式看是否存在注入
跨站脱库方法和操作一样，要看数据库名schema_name
-1' union select 1,group_concat(schema_name),3 from information_schema.schemata
