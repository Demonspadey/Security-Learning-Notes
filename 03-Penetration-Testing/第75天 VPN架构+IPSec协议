## 一、VPN底层基础定义
1.VPN全称虚拟专用网络，依托公网互联网搭建加密虚拟专用传输通道，公共网络链路无法窃听、篡改传输数据。
2.VPN四大核心必备要素
隧道：数据包封装全新IP头部传输，代表技术GRE、IPSec隧道模式
加密：保障数据传输机密性，主流算法AES、3DES、SM4
认证：校验通信双方真实身份，认证方式PSK预共享密钥、数字证书
密钥交换：安全协商会话加密密钥，依靠IKE、Diffie-Hellman算法
精简总结公式：VPN=隧道+加密+认证+密钥交换

## 二、VPN两大连接分类
### 1.远程访问VPN
连接主体：个人终端PC连接VPN网关
适用场景：出差居家员工单点接入企业内网
发起方：员工本地电脑主动发起连接

### 2.站点到站点VPN
连接主体：总部VPN网关与分公司VPN网关互联
适用场景：多个分支机构、企业两地机房网段互通
发起方：两端网关后台自动维持隧道连接

### 3.主流技术分类
1.IPSec VPN：运行在网络层，企业站点互联首选
2.SSL VPN：运行在应用层，浏览器直接接入，适配外勤临时访问
3.MPLS VPN：运营商专线底层BGP架构，仅需基础了解

## 三、IPSec完整协议体系
### 1.三大核心组件
1.AH认证头，协议号51
仅提供数据源身份校验、报文完整性校验、抗重放攻击，**不具备任何数据加密能力**
2.ESP封装安全载荷，协议号50
同时实现加密、身份认证、完整性校验、抗重放，IPSec考试必考核心协议
3.IKE密钥交换协议，固定监听UDP500端口
专门负责协商加密算法、会话密钥、安全关联SA，拆分两个协商阶段

### 2.传输模式、隧道模式核心差异
1.传输模式
仅加密保护数据包上层载荷数据，原始IP头部全程保留不改动；应用场景为两台终端服务器点对点通信。记忆口诀：传输保留原IP头，只加密内部数据。

2.隧道模式
封装全新外层IP头部，原始IP头部+全部数据包内容加密保护；固定用于网关之间站点VPN组网，也是企业分支互联默认模式。记忆口诀：隧道新增外层IP，整包加密网关互通。

### 四、IKE双阶段协商机制
阶段一：协商生成ISAKMP SA
核心目的：搭建加密专用安全通道，为第二阶段协商流量做防护；两种协商模式，主模式6条交互报文安全性高，野蛮模式仅3条报文协商速度快；认证手段分为预共享密钥、CA数字证书。

阶段二：依托阶段一加密通道协商IPSec SA
最终生成真正保护业务数据的单向安全SA，采用快速模式交互3条报文；协商敲定ESP/AH协议类型、加密套件、会话密钥参数。

### 五、安全关联SA核心特性
1.SA属于单向安全规则，任意一组双向网络通信必须配置两条独立SA，分别负责入站、出站流量
2.SA三要素标识：SPI索引、目标IP地址、AH/ESP安全协议
3.所有SA配置条目统一存放于SAD安全关联数据库
4.流量选择器Selector：依靠源IP、目的IP、端口、传输协议，精准匹配需要转发进VPN隧道的流量

## 六、SSL VPN与IPSec分层对比
1.运行层级：IPSec工作网络层；SSL VPN依托应用层
2.客户端依赖：IPSec必须安装专用客户端程序；SSL VPN普通浏览器即可接入，无需额外软件
3.访问范围：IPSec全权覆盖内网所有IP业务流量；SSL VPN仅放开指定Web、应用服务
4.部署复杂度：IPSec配置繁琐修改成本高；SSL部署简易，权限可精细化管控
5.典型业务场景：IPSec用于固定站点网段互联；SSL适配外勤员工临时网页办公访问

## 七、SSL/TLS标准六步握手流程
1.ClientHello客户端发起请求：客户端上传自身支持的全部加密算法套件、客户端随机数
2.ServerHello服务器应答：服务器选定最终加密套件、下发CA数字证书、携带服务器随机数
3.客户端证书三重校验：核查证书有效时间、CA根证书签发合法性、证书域名与访问站点匹配性
4.密钥交换阶段：客户端自行生成预主密钥，使用服务器公钥加密密文后传输，仅服务器私钥能够解密读取
5.会话密钥联合计算：通信双方凭借预主密钥、两端随机数各自独立计算统一主密钥，主密钥全程不会在网络链路传输
6.加密会话建立：双方利用协商套件与主密钥启用对称加密，正式加密传输业务数据

简易背诵口诀：客户端问好、服务器应答、证书校验、公钥传密钥、双方算密钥、加密正式通信

### TLS版本迭代区别
TLS1.2握手往返2-RTT，兼容老旧加密算法；TLS1.3精简不安全算法，握手缩短至1次往返1-RTT，强制启用前向安全机制。

## 八、主流VPN实操部署配置
### 1.Linux strongSwan IPSec配置
核心配置文件路径
主配置文件：/etc/ipsec.conf
密钥存放文件：/etc/ipsec.secrets

站点到站点标准配置模板
conn site-to-site
    left=10.0.0.1
    leftsubnet=192.168.1.0/24
    right=10.0.0.2
    rightsubnet=192.168.2.0/24
    keyexchange=ikev2
    authby=secret
    esp=aes256-sha256!
    auto=start

常用管理命令
ipsec start启动服务、ipsec status查看隧道运行状态

### 2.Windows系统原生VPN
系统配置路径：设置→网络和Internet→添加VPN连接
协议特性：
PPTP老旧协议，依托GRE封装安全性缺陷大；
L2TP搭配IPSec组合，隧道封装加密分离；
IKEv2基于IPSec协议，微软官方主推稳定协议。

### 3.OpenVPN基础知识点
底层协议依托SSL/TLS应用层协议，默认端口UDP1194，备选TCP443；配置文件后缀.ovpn，支持证书、账号密码两种身份认证方式。

## 九、Windows注册表&流量过滤规则
1.远程访问注册表固定路径
HKLM\SYSTEM\CurrentControlSet\Services\RasMan\Parameters
注册表管控Windows RAS远程拨号、系统虚拟隧道网卡全部配置参数

2.Wireshark专用TLS过滤语句
tls.handshake.type == 1 || tls.handshake.type == 2
过滤抓取双向Hello握手报文；type11筛选证书传输数据包
