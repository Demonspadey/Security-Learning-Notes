实操需要两台 VM 互通网络：
- VMware 里两台 VM 的网络适配器都设为【VMnet8(NAT模式)】
- 用 ip addr 分别查看两台 VM 的 IP，确保能互相 ping 通
- 记下两台 VM 的 IP，下文用 <KALI_IP> 和 <UBUNTU_IP> 代替

如果两台 VM 现在没同时开着也不急→按步骤来，第一步就是开 VM。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━


Part 1｜环境准备 + 初始探测
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Step 1.1 启动两台 VM，确认网络互通
──────────────────────────────────────────────────────────────────────────────

【Ubuntu 上执行】
  ip addr show | grep inet
  # 记下 IP， 192.168.xxx.xxx

【Kali 上执行】
  ifconfig
  # 记下 IP

【互相 ping 确认】
  # Kali ping Ubuntu
  ping -c 4 <UBUNTU_IP>

  # Ubuntu ping Kali
  ping -c 4 <KALI_IP>

🔍 能看懂什么：
  · 两台 VM 在同一网段才能通信（子网掩码决定）
  · ping 通 = ICMP 协议没被防火墙拦（初始状态是放行的）

💼 工作用在哪：
  · 配任何服务器前第一步：确认网络通不通。不通就别往下查了
  · "先 ping 一下" 是排障第一步

🗣️ 能说：
  "排查网络问题遵循从下到上的思路：先 ping 测三层连通性，
   通 → 再查端口（telnet/nc），不通 → 查 IP 配置和防火墙"


Step 1.2 在 Kali 上扫 Ubuntu 的开放端口（看看没防火墙时什么样）
──────────────────────────────────────────────────────────────────────────────

【Kali 上执行】
  nmap -sT <UBUNTU_IP>
  # 不用加 sudo，普通 TCP 连接扫描

🔍 能看懂：
  · Ubuntu 默认开了哪些端口（ 22/SSH）
  · nmap 输出中 PORT STATE SERVICE 三列分别代表什么
  · STATE=open 表示端口在监听

💼 工作用在哪：
  · 渗透测试第一步就是端口扫描，了解目标开了哪些服务
  · 安全运维：定期扫自己服务器，发现不该开的端口就关掉

🗣️ 能说：
  "nmap 是最基本的信息收集工具，常用 -sT（TCP连接）和 -sS（半开扫描），
   扫完分析开放端口和服务版本，判断攻击面。"


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Part 2｜搭建防火墙：默认拒绝 + 白名单
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
⭐ 这是最能体现"懂安全"的操作，高频场景题

Step 2.1 先清空现有规则（从干净状态开始）
──────────────────────────────────────────────────────────────────────────────

【Ubuntu 上执行】
  sudo iptables -F           # 清空 filter 表所有规则
  sudo iptables -t nat -F    # 清空 nat 表所有规则
  sudo iptables -X           # 删除用户自定义链
  sudo iptables -P INPUT ACCEPT   # 先把默认策略设成 ACCEPT，防止把自己踢出去

🔍 能看懂：
  · -F 只清规则不改默认策略
  · 云服务器上贸然改默认策略为 DROP 会把自己 SSH 断掉 → 连不上服务器

💼 工作用在哪：
  · 生产服务器上改 iptables 前一定要先备份：iptables-save > backup.txt
  · 远程配防火墙时：先写个定时任务 5分钟后清空规则，防止自己锁在外面


Step 2.2 设置默认策略：先拒绝所有（Default Deny）
──────────────────────────────────────────────────────────────────────────────

【Ubuntu 上执行】
  sudo iptables -P INPUT DROP
  sudo iptables -P FORWARD DROP
  sudo iptables -P OUTPUT ACCEPT   # 出站先不管

🔍 能看懂：
  · 这就是"默认拒绝"原则：没明确允许的一律禁止
  · 设完 INPUT DROP 后， SSH 会话有没有断？
      → 如果断开了说明你是通过 SSH 操作的，这就是"把自己锁外面"
      → 没断开是因为当前 SSH 连接属于 ESTABLISHED，没被拦

💼 工作用在哪：
  · 安全基线第一条：白名单机制，默认拒绝
  · 银行/政府项目的服务器必须用默认拒绝策略

🗣️ 能说：
  "安全的基本原则是 Default Deny——先设默认策略为 DROP，
   再逐条放行需要的服务。这样即使忘了配某个服务，也是安全的。"


Step 2.3 放行 SSH（否则连不上）
──────────────────────────────────────────────────────────────────────────────

【Ubuntu 上执行】
  sudo iptables -A INPUT -m state --state ESTABLISHED,RELATED -j ACCEPT
  # ⭐ 放行已建立的连接（保命规则！当前 SSH 连接靠这个不断）

  sudo iptables -A INPUT -p tcp --dport 22 -j ACCEPT
  # 放行新的 SSH 连接（防止 SSH 断开后重连不上）

🔍 能看懂：
  · ESTABLISHED 表示"已经建立好的连接"——当前 SSH 会话就是
  · RELATED 表示"关联的连接"——比如 FTP 的数据连接
  · 这两条是 iptables 最常用的规则，几乎每台服务器都有

💼 工作用在哪：
  · 任何时候配防火墙，第一条永远是放行 ESTABLISHED
  · 这是"防止把自己锁在外面"的保命规则

⚠️ 测试：
  打开一个新的终端窗口（不要关当前会话！），尝试 SSH 连 Ubuntu：
  【Kali 上执行】 ssh <UBUNTU_USER>@<UBUNTU_IP>
  如果能连上 → SSH 放行成功


Step 2.4 放行 ICMP（方便排障）
──────────────────────────────────────────────────────────────────────────────

【Ubuntu 上执行】
  sudo iptables -A INPUT -p icmp --icmp-type echo-request -j ACCEPT

🔍 能看懂：
  · ICMP 有很多类型，echo-request 就是 ping 发出的包
  · 很多安全加固教程会建议禁 ping（DROP icmp），但生产环境不建议禁
    → 禁了 ping 后出问题你没法快速判断网络通不通

💼 工作用在哪：
  · 工作里大多数服务器会保留 ICMP 方便排障
  · 只有高安全级别（如核心 DB 服务器）才禁 ping


Step 2.5 验证：从 Kali 再次扫描
──────────────────────────────────────────────────────────────────────────────

【Kali 上执行】
  nmap -sT <UBUNTU_IP>

🔍 能看懂什么：
  · 对比 Step 1.2，现在只有 22 端口是 open 的
  · 其他端口全部被 DROP 了 → nmap 显示 filtered 或 不显示

💼 工作用在哪：
  · 配完防火墙立刻验证——这是安全运维的必备习惯
  · "配完不验证等于没配"


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Part 3｜Wireshark 抓包看 iptables 效果（约30分钟）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
⭐ 这步最能帮你理解 iptables 到底在干什么

Step 3.1 在 Ubuntu 上启动 Wireshark
──────────────────────────────────────────────────────────────────────────────

【Ubuntu 上执行】
  sudo wireshark &
  # 如果没装：sudo apt install wireshark -y

  选择网络接口 → "开始捕获"
  设置过滤表达式：icmp or tcp.port == 22
  # 只显示 ICMP(ping) 和 SSH 的包

🔍 能看懂：
  · Wireshark 列：No. / Time / Source / Destination / Protocol / Info
  · 编号 / 时间 / 来源 / 目标地址 / 协议 / 信息


Step 3.2 Kali ping Ubuntu，看 ICMP 包在 Wireshark 里长什么样
──────────────────────────────────────────────────────────────────────────────

【Kali 上执行】
  ping -c 4 <UBUNTU_IP>

🔍 在 Wireshark 里能看懂什么：
  · 第1个包：Kali → Ubuntu  ICMP Echo Request
  · 第2个包：Ubuntu → Kali  ICMP Echo Reply
  · 看到 4 对 request-reply，说明防火墙放行了 ICMP

💼 工作用在哪：
  · ICMP 是最常用的排障协议，"ping 通不代表端口通"
  · "ping 不通不代表服务器宕机"——可能是禁 ping 了


Step 3.3 临时禁掉 ICMP，再看 Wireshark
──────────────────────────────────────────────────────────────────────────────

【Ubuntu 上执行】
  sudo iptables -D INPUT -p icmp --icmp-type echo-request -j ACCEPT
  # -D = Delete，删除刚才加的那条规则

【Kali 上执行】
  ping -c 2 <UBUNTU_IP>
  # 此时应该 ping 不通了（超时）

🔍 在 Wireshark 里能看懂什么：
  · Kali 发出了 Echo Request
  · Ubuntu 没有任何回应！
  · 这就是 DROP 的效果——包被丢了，不发任何回复
    → 攻击者没法判断是被拦了还是目标不存在（增加信息收集难度）

💼 工作用在哪：
  · DROP vs REJECT 的选择：
    - DROP：静默丢弃，攻击者不知道有没有这台机器（更安全）
    - REJECT：告诉对方"被拒绝了"（排障时更方便）
  · 高频题："防火墙 DROP 和 REJECT 有什么区别？"
DROP 是直接丢弃数据包，不回复任何报文，客户端会等待连接超时；REJECT 丢弃数据包同时返回 RST/ICMP 不可达应答，
客户端立即收到拒绝提示。DROP 隐蔽性更强适合公网防护，REJECT 便于内网故障排查。

Step 3.4 恢复 ICMP 放行（方便后面用）
──────────────────────────────────────────────────────────────────────────────

【Ubuntu 上执行】
  sudo iptables -A INPUT -p icmp --icmp-type echo-request -j ACCEPT


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Part 4｜实战场景：模拟攻击 + 应急封禁
──────────────────────────────────────────────────────────────────────────────
⭐ 这是安全服务岗的日常工作场景

场景设定：Kali 模拟攻击者扫你的 Ubuntu 服务器，你发现后手动封禁

Step 4.1 Kali 模拟密集扫描
──────────────────────────────────────────────────────────────────────────────

【Kali 上执行】
  nmap -sT -p 1-10000 <UBUNTU_IP>
  # 扫 1~10000 端口，模拟攻击者在踩点

【Ubuntu 上开另一个终端，查看日志】
  sudo iptables -L -v -n
  # -v 显示计数：看每个规则匹配了多少个包
  # Columns: pkts bytes target prot opt in out source destination

🔍 能看懂：
  · INPUT DROP 那行的 pkts（包计数）在快速增加
  · 这就是攻击扫描的痕迹——大量连接被防火墙拦截
  · 实际工作中：iptables 的 pkts 计数是发现异常的重要指标

💼 工作用在哪：
  · 应急响应第一步：看防火墙计数有没有异常飙升
  · "正常服务器一天 INPUT DROP 几百个包正常，几万个就是被扫了"


Step 4.2 手动封禁攻击者 IP
──────────────────────────────────────────────────────────────────────────────

【Ubuntu 上执行】
  # 先看看日志里谁在扫
  sudo iptables -L -v -n

  # 假设攻击 IP 就是 Kali 的 IP
  sudo iptables -A INPUT -s <KALI_IP> -j DROP
  # 这条规则比默认策略优先级高：先匹配这条，直接丢弃 Kali 的所有包

🔍 能看懂：
  · iptables 是顺序匹配的：规则在列表里从上到下逐条匹配
  · 这条 -s KALI_IP 放在最前面，Kali 的包走到这就被 DROP 了
  · 不继续往下匹配 → 节省 CPU

💼 工作用在哪：
  · 工作中遇到扫描/攻击 → 临时封 IP（应急止血）
  · ⚠️ 但注意：攻击者换 IP 就绕过了 → 这只是临时手段

🗣️ 能说：
  "应急响应的止血操作包括：防火墙封禁攻击源 IP、
   修改服务端口、接入 WAF/CDN 做流量清洗。"


Step 4.3 验证封禁效果
──────────────────────────────────────────────────────────────────────────────

【Kali 上执行】
  ping -c 2 <UBUNTU_IP>         # 现在应该 ping 不通了
  nmap -sT <UBUNTU_IP>          # 所有端口显示 filtered
  ssh <UBUNTU_USER>@<UBUNTU_IP> # 连不上

🔍 能看懂：
  · 封禁 IP 的效果 vs 之前只封端口的效果
  · 之前是"除了22其他端口不通"，现在是"这个IP什么都不通"

💼 工作用在哪：
  · 临时封禁是最基础的应急操作，但治标不治本
  · 后续要做：分析日志 → 找到漏洞 → 修复


Step 4.4 解除封禁（练习用，恢复环境）
──────────────────────────────────────────────────────────────────────────────

【Ubuntu 上执行】
  sudo iptables -D INPUT -s <KALI_IP> -j DROP
  # -D 删除这条规则

【验证恢复】
  # Kali ping Ubuntu，应该又通了
  ping -c 2 <UBUNTU_IP>
