## 一、核心目标
1.  吃透文件上传漏洞的核心定义、根本成因与两类绕过逻辑
2.  区分前端校验与后端校验的安全差异，牢记前端校验无防护作用
3.  掌握漏洞完整危害链条、标准防护方案与软考+面试通用答题话术
4.  完成靶场手工上传测试，理解木马上传→解析执行→服务器被控的完整流程

---

## 二、核心原理精讲

### 1. 漏洞核心定义
文件上传功能未做严格安全校验，攻击者可上传恶意Web脚本文件，并被服务器正常解析执行，最终实现服务器远程控制、权限获取的高危Web应用漏洞。

### 2. 根本成因
服务器未做**后端强制校验**，仅依赖前端JS或弱规则黑名单过滤，信任用户可控上传参数，导致输入验证失效、访问控制缺失，恶意文件可直接上传并解析。

### 3. 两类绕过方式
- **前端校验绕过**：仅通过前端JS限制后缀与类型，抓包修改数据包即可直接绕过，无任何实际安全防护能力。
- **后端校验绕过**：服务端存在校验规则，但采用黑名单策略、未校验文件头与真实内容，可通过后缀伪装、解析漏洞、文件头欺骗等方式绕过。

### 4. 核心危害层级
1.  上传Webshell获取服务器权限，实现远程控制
2.  篡改网站页面、植入后门与恶意内容
3.  窃取数据库、用户信息、核心业务数据
4.  以服务器为跳板，开展内网横向渗透
5.  系统权限全面失控，业务中断，引发安全责任事件

### 5. 标准防护核心逻辑
- 后端强制校验，绝对不依赖前端校验
- 采用**白名单机制**校验后缀、MIME类型
- 校验文件幻数（文件头），检测真实文件内容
- 文件随机重命名、隔离存储，杜绝路径遍历
- 上传目录禁止脚本解析，配置只读权限
