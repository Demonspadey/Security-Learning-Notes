## 一、今日核心目标
1.  掌握SQL注入根本成因、防护与危害的标准答题话术
2.  熟记HTTP协议核心安全考点
3.  背熟软考简答/案例题通用模板

---

## 二、核心考点清单
### 1. SQL注入核心考点
- 根本成因：用户可控输入未过滤，直接拼接SQL语句执行。
- 最根本防护手段：参数化预编译SQL/ORM框架。
- 数据库安全原则：账号最小权限。

### 2. HTTP协议核心考点
- HTTP特性：无状态，依靠Cookie维持会话身份。
- 高频状态码含义：200、302、403、404、500。

---

## 三、标准答题模板
### 模板1：SQL注入危害（简答专用）
泄露数据库敏感数据、篡改网页内容、拿下服务器权限、越权登录后台、破坏数据完整性。

### 模板2：SQL注入五层防护（简答/案例专用）
1.  开发层：采用预编译SQL、ORM框架；
2.  输入层：白名单校验、特殊字符转义；
3.  数据库层：分配最小权限、禁用高危账号；
4.  应用层：关闭数据库错误回显、统一错误页面；
5.  网络层：部署WAF拦截注入流量。

### 模板3：SQL注入案例题通用答题框架
1.  漏洞：SQL注入漏洞
2.  成因：用户可控输入未过滤，直接拼接SQL执行
3.  整改：按五层防护体系逐条落地
4.  长效加固：定期漏洞扫描、代码审计、安全培训、补丁更新

### . SQL注入五层防护体系
1.  **开发层**：使用参数化预编译SQL、ORM框架，杜绝字符串拼接SQL
2.  **输入层**：白名单校验、特殊字符转义、过滤敏感关键字
3.  **数据库层**：账号最小权限原则、禁用高危root权限账号
4.  **应用层**：关闭数据库详细错误回显，统一自定义错误页面
5.  **网络层**：部署WAF，拦截SQL注入特征载荷

### . 根本成因与危害
- **成因**：用户可控输入未做过滤校验，直接拼接到SQL语句中被数据库解析执行。
- **危害**：泄露敏感数据、篡改业务数据、绕过身份认证、脱库、服务器被控。
