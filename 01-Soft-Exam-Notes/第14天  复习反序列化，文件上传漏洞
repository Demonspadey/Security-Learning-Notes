## 软考核心考点复盘
### 2.1 不安全反序列化漏洞
- **核心成因（软考标准表述）**：应用程序盲目信任外部不可信的可控数据，未做严格校验、白名单限制与合法性检测，直接对用户可控的序列化数据执行反序列化操作，从而触发漏洞。
- **主要危害**：远程代码执行、系统命令执行；任意文件读取/写入；权限绕过、身份伪造；拒绝服务攻击。
- **核心防护措施**：
  1.  优先使用JSON、XML等安全纯数据格式，避免危险序列化方式
  2.  对反序列化数据做白名单校验，限制可加载类与数据类型
  3.  对序列化数据增加数字签名/加密，防止数据篡改
  4.  禁止反序列化外部不可信未知数据

### 2.2 文件上传漏洞
- **高频易混考点**：
  1.  前端JS校验可直接抓包绕过，无实际安全防护作用
  2.  黑名单过滤存在大量绕过方式（大小写、双后缀、畸形后缀等），安全性极差
  3.  白名单校验是文件类型校验最优方案
  4.  上传目录执行权限是漏洞利用的关键条件
- **必背6条防护措施（软考简答满分）**：
  1.  采用白名单机制校验文件后缀，禁止脚本类文件格式
  2.  服务端二次校验，彻底摒弃前端JS单一校验
  3.  多层校验文件真实类型（后缀+文件头+内容+请求头），防止绕过
  4.  上传目录关闭脚本执行权限，与Web业务目录物理隔离
  5.  对上传文件随机哈希重命名，防范路径遍历与解析漏洞
  6.  限制单文件大小与上传频率，缓解拒绝服务攻击
