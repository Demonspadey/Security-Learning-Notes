## 一、核心目标
1.  熟记同源策略、CSRF漏洞的核心考点
2.  掌握CSRF攻击原理与防御措施的标准简答模板
3.  规范CSRF与XSS对比题、案例题的作答思路

---

## 二、必背考点卡片
1.  同源策略判定：**协议、域名、端口**三者完全相同
2.  CSRF核心利用点：浏览器**自动携带同源会话Cookie**
3.  CSRF攻击三条件：已登录、仅校验Cookie、无Token防护
4.  CSRF首选防护：**CSRF Token 随机校验**
5.  SameSite作用：限制Cookie跨站携带，源头防御CSRF
6.  CSRF与XSS是软考**高频对比简答题**，必须熟记差异

---

## 三、标准简答满分模板
### 题目1：简述CSRF攻击原理与防御措施
> 标准答案：
> 1. 攻击原理：跨站请求伪造（CSRF）是攻击者利用浏览器自动携带同源会话Cookie的特性，诱导已登录目标站点的用户访问恶意页面，
伪造用户身份向目标站点发送未授权请求，冒用用户权限执行违规操作，攻击者无需窃取用户Cookie，仅借助用户已登录状态完成攻击。
> 2. 防御措施：
> - 校验CSRF Token：服务端生成随机、唯一、不可预测的Token，嵌入请求并校验有效性；
> - 配置SameSite Cookie：限制Cookie仅同站携带，阻断跨站自动携带；
> - Referer来源校验：验证请求来源域名，拦截非法跨源请求；
> - 高危操作二次验证：增加验证码、密码二次确认，提升攻击门槛。

### 题目2：简述CSRF与XSS漏洞的核心区别
> 标准答案：
> 1. 漏洞本质不同：XSS是注入恶意脚本在用户浏览器执行，属于代码注入类漏洞；CSRF是伪造用户请求冒用身份操作，属于请求伪造类漏洞。
> 2. 攻击目标不同：XSS攻击目标是用户浏览器，窃取用户信息、修改页面内容；CSRF攻击目标是目标服务器，冒用用户权限执行未授权操作。
> 3. 利用条件不同：XSS需要站点存在输入输出过滤缺陷，能注入并执行脚本；CSRF需要用户已登录目标站点、请求无Token校验、
     浏览器自动携带Cookie。
> 4. 防护核心不同：XSS防护核心是输入过滤、输出编码、CSP安全策略；CSRF防护核心是Token校验、SameSite Cookie、来源校验。

---

## 四、模拟练习题（附你的作答批改）

### 1. 单项选择题
1.  浏览器同源策略的判定三要素是（）
    A. 域名、主机、路径
    B. 协议、域名、端口
    C. 协议、IP、路径
    D. 域名、端口、参数
    ✅ 你的答案：B（正确）

2.  下列关于同源策略作用说法错误的是（）
    A. 限制跨域DOM操作
    B. 限制跨域读取Cookie
    C. 禁止所有跨域网络请求
    D. 作为Web安全的基础安全边界
    ❌ 你的答案：D（错误，正确答案：C）
    解析：同源策略不会禁止图片、表单、脚本引用这类常规跨域资源加载，仅限制敏感读写操作，不是禁止所有跨域请求。

3.  CSRF跨站请求伪造的核心利用条件是（）
    A. 浏览器会自动携带同源会话Cookie
    B. 攻击者可以直接窃取用户Cookie
    C. 服务器存在SQL注入漏洞
    D. 前端未做特殊字符转义
    ✅ 你的答案：A（正确）

4.  下列关于CSRF与XSS区别说法正确的是（）
    A. CSRF属于脚本注入漏洞，XSS属于请求伪造漏洞
    B. CSRF需要注入并执行恶意脚本
    C. XSS不窃取数据，仅冒用用户身份执行操作
    D. CSRF防护核心是Token校验，XSS防护核心是输出编码
    ✅ 你的答案：D（正确）

5.  目前防御CSRF最主流、最稳定、最推荐的防护手段是（）
    A. Referer来源校验
    B. CSRF Token校验
    C. 增加验证码
    D. 前端过滤关键字
    ✅ 你的答案：B（正确）

6.  Cookie的SameSite属性的核心作用是（）
    A. 加密Cookie内容防止窃取
    B. 限制Cookie跨站自动携带，从源头防御CSRF
    C. 禁止前端JS读取Cookie
    D. 对Cookie做编码转义防御XSS
    ✅ 你的答案：B（正确）

---

### 2. 判断题
1.  协议、域名、端口任意一项不同，即判定为跨源。（✅ 你的答案：对）
2.  CSRF攻击中，攻击者不需要窃取用户的Cookie。（✅ 你的答案：对）
3.  同源策略会完全禁止所有跨域图片、表单提交行为。（❌ 你的答案：对，正确答案：错）
    解析：同源策略不会拦截跨域图片、表单提交、静态资源加载，并不是完全禁止所有跨域行为。
4.  Referer校验可以完全防御CSRF，不会被绕过。（✅ 你的答案：错）
5.  CSRF漏洞的本质是请求缺少不可预测的身份校验标识。（✅ 你的答案：对）

---

### 3. 场景案例分析题
#### 场景描述
某网站用户中心支持在线修改密码、修改绑定手机号功能，用户登录后会话Cookie保存在浏览器；系统仅校验Cookie是否有效，
未在请求中加入任何Token校验，未配置SameSite属性，也未校验请求来源Referer。攻击者构造恶意网页，诱导已登录该网站的用户访问，
页面自动向用户中心接口发送POST请求，成功修改用户登录密码，实现账号劫持。

1.  **漏洞类型与原理**：
    该网站存在CSRF漏洞，CSRF(跨站请求伪造)是攻击者利用浏览器自动携带同源会话cookie的特性，诱导已登录用户访问恶意页面，
    伪造用户身份向目标站点发送非授权请求，冒用用户权限执行违规操作，攻击者无需窃取用户cookie，仅借助用户已登录状态完成攻击。
    ✅ 批改：表述与软考标准答案话术完全一致，原理、核心逻辑、攻击链路全部踩中采分点。

2.  **攻击成功的三个必要条件**：
    - 用户已登录目标站点。
    - cookie无额外校验。
    - 请求无不可预测的防伪标识。
    ✅ 批改：精准对应教材原话，三点齐全，表述规范。

3.  **CSRF与XSS漏洞的四点核心区别**：
    - 漏洞本质不同:Xss是注入恶意脚本在用户浏览器执行，属于代码注入类漏洞，csrf是伪造用户请求冒用身份操作，属于伪造类漏洞。
    - 攻击目标不同:xss攻击目标是用户浏览器，窃取用户信息，修改页面内容。csrf攻击目标是目标服务器，冒用用户权限执行未授权操作。
    - 利用条件不同:xss需要站点存在输入输出过滤缺陷，能注入并执行脚本。csrf需要用户已登录目标站点，请求无token校验，浏览器自动携带cookie。
    - 防护核心不同:xss防护核心是输入过滤，输出转码，csp安全策略。csrf防护核心是token校验，samesite cookie,来源校验。
    ✅ 批改：从四个维度分层作答，条理清晰、专业术语标准，完全符合软考阅卷采分点。

4.  **CSRF防护措施**：
    - 校验csrf token，服务端为用户会话生成随机，唯一，无预测的token，嵌入请求参数或请求头中，服务端接收请求后先校验token有效性。
    - 设置cookie的samesite属性，限制cookie仅在同站请求时携带，阻断跨站请求自动携带cookie的漏洞利用条件。
    - 同源referer校验，验证请求头中的referer字段，确认请求来源为合法可信域名，拦截非法跨源请求。
    - 关键操作二次校验，对于转账，修改密码，删除数据等高风险操作，增加短信验证码，登录密码二次验证，提升攻击门槛。
