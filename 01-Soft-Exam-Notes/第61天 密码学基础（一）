## 一、核心目标
1. 熟记单选高频考点，精准区分算法分类、特性、场景
2. 掌握对称与非对称加密区别的标准简答话术
3. 梳理易混坑点，规避算法分类、功能边界类错题

---
## 二、单选高频考点（必背）
### 考点1 算法分类（必考）
- 对称加密：AES、DES、SM4、RC4
- 非对称加密：RSA、ECC、SM2
- 哈希函数：MD5、SHA系列、SM3

### 考点2 核心特性
- 对称加密：单钥、速度快、适合大数据、密钥分发难
- 非对称加密：双钥、速度慢、适合小数据、密钥管理简单
- 哈希函数：单向不可逆、定长输出、抗碰撞、雪崩效应

### 考点3 国密算法对应
- SM2 → 非对称 → 签名、密钥协商
- SM3 → 哈希 → 完整性校验
- SM4 → 对称 → 数据加密

### 考点4 安全强度
- 弱算法（不推荐）：MD5、SHA-1、DES
- 强算法（推荐）：SHA-256、AES-256、SM系列

### 考点5 典型场景
- 大文件加密 → AES/SM4（对称）
- 数字签名/密钥交换 → RSA/SM2（非对称）
- 文件完整性校验 → SHA-256/SM3（哈希）
- HTTPS → 混合密码体制

---
## 三、简答/案例题 标准答题模板
### 模板1 简述对称加密与非对称加密的区别
作答：
1. **密钥体系不同**：对称加密加解密共用同一密钥；非对称加密分为公钥、私钥密钥对。
2. **运算效率不同**：对称加密速度快，适合大批量业务数据；非对称加密运算开销大，仅适合小数据处理。
3. **密钥管理不同**：对称密钥需安全专线传输，分发难度大；非对称公钥可公开分发，仅需保管私钥。
4. **适用场景不同**：对称加密用于业务数据加密；非对称加密用于身份认证、数字签名、密钥交换。
5. **典型算法不同**：对称加密代表为AES、DES、SM4；非对称加密代表为RSA、ECC、SM2。

### 模板2 简述哈希函数的核心特性与典型应用
作答：
哈希函数核心特性包括：
1. 单向性：无法从哈希值反向还原原始数据；
2. 定长输出：无论原文长度，输出哈希值长度固定；
3. 抗碰撞性：很难找到两个不同的原文产生相同的哈希值；
4. 雪崩效应：原文微小改动会导致哈希值发生显著变化。
典型应用：文件完整性校验、用户密码加盐存储、数字签名摘要生成。

### 模板3 简述混合密码体制的原理与优势
作答：
混合密码体制结合了对称加密与非对称加密的优势：
1. 原理：使用非对称加密传递临时对称会话密钥，再使用对称加密对大批量业务数据进行加密。
2. 优势：既解决了对称密钥分发的安全问题，又保障了大数据加密的运算效率，兼顾安全与性能。
3. 典型应用：HTTPS协议、VPN加密隧道、大文件加密传输。

---
## 四、易错题坑点汇总
1. 功能边界坑：哈希不是加密算法，不能加密数据，只能生成摘要；不能从哈希值还原原文
2. 国密场景坑：SM2签名、SM3校验、SM4加密，三者不可混淆
3. 安全强度坑：MD5、DES已被证明安全性不足，核心场景不推荐使用
4. 机制定性坑：HTTPS是混合密码体制，不是纯对称也不是纯非对称
5. 数字签名坑：私钥签名、公钥验签，顺序不可颠倒
