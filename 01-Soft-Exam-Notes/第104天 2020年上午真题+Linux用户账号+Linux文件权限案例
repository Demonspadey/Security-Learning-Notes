━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
❌ Q20 数字水印解释攻击
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  题干：数字水印的解释攻击是以阻止版权所有者对所有权的断言为攻击目的。
       以下不能有效解决解释攻击的方案是（）
  你选：D（利用单向水印方案消除可逆性）→ 正确：B（引入验证码机制）

  选项分析：
  A. 引入时间戳机制 → ✅ 能解决（时间戳证明所有权先后）
  B. 引入验证码机制 → ❌ 验证码是防机器攻击的，与解释攻击无关，不能解决 ⭐
  C. 作者注册水印序列同时注册原作品 → ✅ 能解决（原作品登记证明所有权）
  D. 利用单向水印方案消除可逆性 → ✅ 能解决（消除可逆性→攻击者无法逆向），
    所以不是"不能解决"的

  记忆：能解决解释攻击 = 时间戳 / 注册原作品 / 单向水印；验证码不行

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
❌ Q23 操作系统安全机制
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  题干：操作系统的安全机制不包括（）
  你选：B（访问控制机制）→ 正确：C（密钥管理机制）

  选项分析：
  A. 标识与鉴别机制 → ✅ OS安全机制
  B. 访问控制机制 → ✅ OS安全机制（你以为它不算，其实算）
  C. 密钥管理机制 → ❌ 密码学范畴，不是OS安全机制 ⭐
  D. 安全审计机制 → ✅ OS安全机制

  记忆：OS安全机制 = 标识鉴别 + 访问控制 + 审计；密钥管理是密码学的

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
❌ Q30 智能卡COS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  题干：片内操作系统COS中，对接收命令进行可执行判断属于（）
  你选：B（安全管理模块）→ 正确：C（应用管理模块）

  选项分析：
  A. 通信管理模块 → ❌ 负责通信
  B. 安全管理模块 → ❌ 负责安全策略（你选的）
  C. 应用管理模块 → ✅ 对接收命令做可执行判断，处理命令执行 ⭐
  D. 文件管理模块 → ❌ 负责文件

  记忆：COS四模块 = 通信/安全/应用/文件；命令执行判断 → 应用管理

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
❌ Q40 FTP两个连接
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  题干：FTP的客户和服务器之间需要建立两个TCP连接，分别是（）
  你选：A（认证连接和数据连接）→ 正确：B（控制连接和数据连接）

  选项分析：
  A. 认证连接和数据连接 → ❌ 没有"认证连接"这种东西
  B. 控制连接和数据连接 → ✅ 控制连接(21,传命令)+数据连接(20,传数据) ⭐
  C. 认证连接和控制连接 → ❌ 无认证连接
  D. 控制连接和登录连接 → ❌ 无登录连接

  记忆：FTP = 控制连接(21,命令) + 数据连接(20,数据)；没有"认证连接"

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
❌ Q47 HTTP协议漏洞类型
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  题干：Web服务器受到HTTP协议本身安全问题的困扰，这种类型的信息系统安全漏洞属于（）
  你选：B（开发型漏洞）→ 正确：A（设计型漏洞）

  选项分析：
  A. 设计型漏洞 → ✅ 协议本身缺陷 = 设计阶段引入 ⭐
  B. 开发型漏洞 → ❌ 编码阶段缺陷（你选的）
  C. 运行型漏洞 → ❌ 配置/运维缺陷
  D. 代码型漏洞 → ❌ 类似开发型

  记忆：协议本身问题 = 设计型；代码写错 = 开发型；配置不当 = 运行型

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
❌ Q48 访问监控器需求（⭐重点，以GB17859标准原文为准）
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  题干：要求计算机信息系统可信计算基满足访问监控器需求的是（）
  你选：C（结构化保护级）→ 正确：D（访问验证保护级）⭐

  ⚠️ 依据GB17859-1999标准原文：
     "第五级 访问验证保护级：本级的计算机信息系统可信计算机满足访问监控器需求。
      访问监控器仲裁主体对客体的全部访问。访问监控器本身是抗篡改的。
      支持安全管理员职能；扩充审计机制；提供系统恢复机制。"
     → 访问监控器明确在第5级访问验证保护级（D）

  选项分析：
  A. 系统审计保护级 → ❌ 第2级，审计
  B. 安全标记保护级 → ❌ 第3级，自主+强制访问控制
     （注意：部分资料误标B，但GB17859标准原文是D，以标准为准）
  C. 结构化保护级 → ❌ 第4级，结构化设计+隐蔽信道分析（你选的）
  D. 访问验证保护级 → ✅ 第5级！满足访问监控器需求 ⭐

  记忆：GB17859五级：1自主 → 2审计 → 3标记 → 4结构化 → 5访问验证
       访问监控器 + 系统恢复机制 → 都在第5级访问验证保护级

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
❌ Q63 非网络安全控制技术
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  题干：以下不属于网络安全控制技术的是（）
  你选：A（VPN技术）→ 正确：B（容灾与备份技术）

  选项分析：
  A. VPN技术 → ✅ 网络安全控制技术（保证传输安全），所以不属于"不属于"
  B. 容灾与备份技术 → ❌ 数据可用性/恢复技术，不是网络安全控制 ⭐
  C. 入侵检测技术 → ✅ 网络安全控制
  D. 信息认证技术 → ✅ 网络安全控制

  记忆：网络安全控制 = 防火墙/IDS/VPN/信息认证/访问控制；容灾备份 = 数据恢复

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
❌ Q65 密码系统安全性
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  题干：一个密码系统的安全性是由（）决定的
  你选：A（加密算法）→ 正确：D（密钥）

  选项分析：
  A. 加密算法 → ❌ 算法可公开（Kerckhoffs原则）
  B. 解密算法 → ❌ 同加密算法
  C. 加解密算法 → ❌ 同上
  D. 密钥 → ✅ Kerckhoffs原则：算法公开，安全性依赖密钥保密 ⭐

  记忆：Kerckhoffs原则 = 算法公开，安全性靠密钥；密钥泄露 = 系统被破

🔴 Part 3：案例① Linux用户账号（2020Q1 完整真题，35min）

📌 纸笔计时30min，完整手写，然后对照答案红笔标采分点。

```
【2020年 下午案例 第1题 · 完整真题】

【说明】Linux系统通常将用户名相关信息存放在/etc/passwd文件中，假如有
/etc/passwd文件的部分内容如下：
user1:x:0:0:user:/home/user1:/bin/bash
user2:x:1000:1000:ubuntu64:/home/user2:/bin/bash
daemon:x:1:1:daemon:/usr/sbin:/usr/sbin/nologin
bin:x:2:2:bin:/bin:/usr/sbin/nologin
sys:x:3:3:sys:/dev:/usr/sbin/nologin
sync:x:4:65534:sync:/bin:/bin/sync

问题1（2分）口令字文件/etc/passwd是否允许任何用户访问？
问题2（2分）根据上述/etc/passwd显示的内容，给出系统权限最低的用户名字。
问题3（2分）/etc/passwd每行用冒号分隔为7个字段，Linux操作系统是根据
哪个字段来判断用户的？
问题4（3分）根据上述内容，请指出该系统中允许远程登录的用户名。
问题5（2分）Linux把用户密码保存在影子文件中，请给出影子文件的完整路径
及名字。
问题6（3分）如果使用ls -al查看影子文件的详细信息，请给出数字形式表示的
影子文件访问权限。

【参考答案】
问题1：允许。/etc/passwd对任何用户可读（权限644），存放账户信息但不存密码
问题2：user2（UID=1000，数值最大，普通用户权限最低）⭐【待核对】
问题3：UID（第3个字段，第一个数字）⭐
问题4：user1、user2（登录shell为/bin/bash，可远程登录；nologin用户不能登录）
问题5：/etc/shadow
问题6：600（-rw-------，仅root可读写）
```

**✏️ 细致追问（口答）**：
```
① /etc/passwd 7个字段分别是什么？
   → 用户名:密码占位(x):UID:GID:描述:家目录:登录Shell
② UID=0 表示什么？
   → root（超级用户，权限最高）
③ nologin 用户能登录系统吗？
   → 不能（/usr/sbin/nologin作为登录shell，拒绝交互登录，用于系统服务）
④ 为什么 passwd 可读、shadow 不可读？
   → passwd只存账户信息（可公开）；shadow存密码哈希（敏感，仅root）
⑤ /etc/shadow 默认权限？
   → 600（-rw-------）
⑥ passwd vs shadow 的关系？
   → passwd是用户账户清单，shadow存对应密码哈希，二者通过用户名关联
```

---

### 🔴 Part 4：案例② Linux文件权限（2022Q2 完整真题，35min）

📌 纸笔计时30min，完整手写，然后对照答案红笔标采分点。

```
【2022年 下午案例 第2题 · 完整真题】

【说明】在Linux系统执行ls -l命令，输出如下5行文件信息：
d rwx r-x r-x  2 root root openvpn
l rwx rwx rwx  1 root root shm->/dev/shm
s rw- rw-  1 root root snapd.socket
- rw- r-- r--  1 root root crond.pid
- rwx r-x r-x  1 root root abc
当前终端提示符为 Renshuo@local:~$

问题1：从提示符判断当前登录是root用户还是普通用户？
问题2：根据文件首位标识分类：
  (1) 普通文件有哪些？
  (2) 目录文件有哪些？
  (3) 软链接文件有哪些？
问题3：创建sh指向/dev/shm软链接的完整命令？
问题4：系统存在失效断链软链接：
  (1) 查找/home目录下所有失效软链接命令；
  (2) 查找并直接删除/home下全部失效软链接命令；
问题5：转换权限数字表示：
  (1) openvpn目录 drwxr-xr-x 对应数字权限；
  (2) abc文件 -rwxr-xr-x 对应数字权限。

【参考答案】
问题1：普通用户（提示符结尾是 $；root的提示符结尾是 #）⭐
问题2：(1) 普通文件：crond.pid、abc（首字符-）
       (2) 目录文件：openvpn（首字符d）
       (3) 软链接文件：shm（首字符l）
问题3：ln -s /dev/shm sh ⭐
问题4：(1) find /home -xtype l ⭐（-xtype l=查找断链软链接）
       (2) find /home -xtype l -delete
问题5：(1) drwxr-xr-x = 755 ⭐（rwx=7, r-x=5, r-x=5）
       (2) -rwxr-xr-x = 755
```

**✏️ 细致追问（口答）**：
```
① ls -l 首字符各代表什么？
   → -普通文件 / d目录 / l软链接 / s套接字 / b块设备 / c字符设备 / p管道
② rwx 对应数字？
   → r=4, w=2, x=1；rwx=7, r-x=5, rw-=6
③ 软链接 vs 硬链接？
   → 软链接=快捷方式(存目标路径，失效=目标删)；硬链接=同一inode多个名字
④ find -xtype l 和 -type l 区别？
   → -type l找所有链接；-xtype l只找"失效/断链"的软链接
⑤ 为什么shadow文件要600？
   → 密码哈希敏感，仅root可读写，防普通用户读取破解
⑥ root vs 普通用户提示符？
   → root=# ，普通用户=$
```

---
