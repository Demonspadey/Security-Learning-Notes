Part 1：案例 · 身份认证协议（2024Q3 完整真题，35min）

📌 纸笔计时30min，完整手写，然后对照答案红笔标采分点。

```
【2024年 下午案例 第3题 · 完整真题】

【说明】假设用户A和用户B为了互相验证对方的身份，设计了如下通信协议：
1. A→B: RA
2. B→A: f(PAB || RA) || RB
3. A→B: f(PAB || ________)
其中：RA、RB是随机数，PAB是双方事先约定并共享的口令，
"||"表示连接操作。f是哈希函数。

问题1. 身份认证可以通过用户知道什么、用户拥有什么和用户的生理特征等
方法来验证。请问上述通信协议是采用哪种方法实现的？

问题2. 根据身份的互相验证需求，补充协议第3步的空白内容。

问题3. 如果将哈希函数替换成对称加密函数，是否可以提高该协议的安全性？
为什么？

【参考答案】
问题1：知识因素（"用户知道什么"）——双方共享口令PAB ⭐
问题2：RB（第3步应填 RB，即 f(PAB || RB)）⭐
  → A收到B的挑战RB后，用共享口令PAB和RB做哈希回应，
    完成"互相验证"（A验证B、B验证A）
问题3：不能提高安全性。⭐
  · 哈希用于"完整性校验"（单向性，防篡改，能验证消息是否被改）
  · 对称加密用于"保密"（可被解密/重放），换成对称加密后：
    ① 失去哈希的完整性校验特性，无法发现篡改
    ② 共享密钥方式存在重放攻击风险
  → 所以不能提高，反而可能降低
```

**✏️ 细致追问（口答）**：
```
① 协议里 RA、RB 随机数的作用？
   → 抗重放攻击（Nonce一次性，每次认证不同，防止旧消息重放）
② 这是"挑战-响应"机制吗？
   → 是！B发挑战RB，A用共享口令+RB响应，验证双方都知道口令
③ 身份认证三因素？
   → 知道什么(口令) / 拥有什么(智能卡/令牌) / 是什么(生物特征)
④ 哈希函数在这里的作用？
   → 单向性保护口令（不让PAB直接暴露），+完整性校验
⑤ 如果第3步不填RB只填f(PAB)，会怎样？
   → 无法证明A确实收到B的挑战，B无法确认A"活着"，重放风险高
```

---
