1.【2022Q56】攻击者将恶意代码植入目标计算机，进而操纵受害机执行恶意活动，属于（  ）
  A.ARP欺骗 B.网络钓鱼 C.僵尸网络 D.特洛伊木马
2.【2022Q54】文件型病毒不能感染的文件类型是（  ）
  A.HTML型 B.COM型 C.SYS型 D.EXE型
3.【2022Q50】冲击波蠕虫(Blaster)利用的漏洞是（  ）
  A.TCP/IP协议漏洞 B.RPC DCOM漏洞(MS03-026) C.SMB漏洞(MS17-010) D.MS SQL缓冲溢出
4.【2022Q31】Cisco通配符掩码中，0表示该位（  ）
  A.忽略 B.必须精确匹配 C.任意匹配 D.禁止访问
5.【2022Q30】关于防火墙白名单策略，叙述正确的是（  ）
  A.禁止与规则冲突的包，其余全放行 B.只放行明确允许的流量，其余全拒绝
  C.默认全部放行 D.只拦截检测到的恶意流量
6.【2022Q24】Kerberos中KDC为（  ）提供认证服务
  A.应用服务器 B.客户端 C.数据库 D.网络设备
7.【2022Q47】Windows中仅允许系统管理员访问的日志是（  ）
  A.系统日志 B.应用程序日志 C.安全日志 D.性能日志
8.【2022Q63】Syslog日志每行由4个固定字段组成，正确的是（  ）
  A.时间+主机+子系统+消息 B.时间+主机+用户名+消息
  C.主机+子系统+消息+备注 D.时间+主机+消息+备注
9.【2022Q69】SNMPv1/v2c不具备的安全访问控制措施是（  ）
  A.限制访问IP B.共同体字符串认证 C.权限分级机制 D.关闭SNMP访问
10.【2022Q58】将数据<张三，1965年4月15日，男，12345678>处理后为<张*，1960-1970年生，男，1234****>，这种隐私保护措施被称为（  ）
  A.泛化 B.抑制 C.扰动 D.置换

答案：1C 2A 3B 4B 5B 6B 7C 8A 9C 10A
```

---

### 📋 Part 4：案例答题模板整理——8大主题框架（1h）

```
📌 把Week6-7做过的案例题，整理成8大主题答题模板。
   考试时看到对应主题，直接套框架。

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
① 防火墙/iptables 主题
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  答题模板：
  · 防火墙体系结构：双宿主机/屏蔽主机/屏蔽子网（最安全）
  · iptables表：filter（过滤）/nat（转换）/mangle（改包）
  · 链：INPUT/OUTPUT/FORWARD（filter）；PREROUTING/POSTROUTING（nat）
  · 默认策略：-P INPUT DROP（白名单）/ ACCEPT（黑名单）
  · 放行规则：-A FORWARD -s 源 -d 目的 -p 协议 --dport 端口 -j ACCEPT
  · 封禁规则：-A INPUT -s 攻击IP -j DROP
  · 白名单顺序：放行lo → 放行ESTABLISHED → 放行服务端口 → 默认拒绝
  安服转译：客户Web被攻击→确认IP→临时封禁→约窗口配永久策略

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
② PKI数字信封 主题
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  答题模板：
  · CA签发证书 / RA审核信息
  · 先签名后加密（推荐顺序）：
    明文→哈希→私钥签名→M+签名→会话密钥K加密→K用接收方公钥加密→传输
  · 实现三特性：机密性(K加密)+完整性(哈希)+不可否认性(私钥签名)
  · 先加密后签名缺陷：接收方只能验证密文完整性，不能验证原文完整性
  · CRL=证书吊销列表 / PGP=Web of Trust信任模型
  安服转译：数字签名=防篡改抗否认，数字信封=加密传输

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
③ Windows日志 主题
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  答题模板：
  · 打开命令：eventvwr.msc
  · 日志分类：系统/安全/应用（安全日志仅管理员可读）
  · 事件ID：4624成功登录 / 4625失败登录 / 4634注销 / 4720创建用户
  · 大量4625=暴力破解，配合3389端口→RDP爆破
  · Wireshark过滤：tcp.port == 3389
  · 防护：改端口+防火墙白名单限制来源IP
  安服转译：客户说服务器被爆破→查4625→封IP+改端口

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
④ 缓冲区溢出 主题
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  答题模板：
  · 漏洞类型：缓冲区溢出（栈溢出）
  · 成因：C数组不检查边界 + gets/strcpy等不安全函数
  · 内存区域：局部变量在stack区（栈）
  · 栈操作：push(压栈)/pop(出栈)；栈向低地址增长
  · 先声明变量在高地址，后声明在低地址（buffer溢出覆盖authenticated）
  · 缓解：/GS编译选项（但不保护≤4字节数组）+ 用strncpy替代strcpy
  · 模糊测试可以检测
  安服转译：代码审计发现strcpy→建议strncpy+开启/GS

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
⑤ SSH安全配置 主题
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  答题模板：
  · 配置文件：/etc/ssh/sshd_config（服务端）
  · 重启命令：systemctl restart ssh（Debian系）/ sshd（RedHat系）
  · 加固5项：①改端口 ②PermitRootLogin prohibit-password
    ③PasswordAuthentication no ④PubkeyAuthentication yes
    ⑤authorized_keys权限600
  · 日志：/var/log/auth.log（Failed/Accepted）
  · 清除历史：history -c 或 rm -f ~/.bash_history
  安服转译：SSH被爆破→查auth.log→封IP+加固配置

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
⑥ ISO/OSI安全服务 主题
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  答题模板：
  七类服务：①认证②访问控制③保密性④完整性⑤抗否认⑥审计⑦可用性
  · CSRF → ①认证（验证请求来源）
  · 口令明文 → ③保密性（加密传输）
  · Smurf → ⑦可用性（DoS耗带宽）
  · 签名伪造 → ⑤抗否认（签名不可抵赖）
  · 追踪溯源 → ⑥审计（日志记录）
  · 软件植入木马 → ④完整性（哈希校验）
  易错：每题可能单选，选最核心的一个
```

---

# 🌤️ 下午（2.5h）：答题模板整理（续）+ Python

---

### 📋 Part 1：答题模板整理——剩下2个主题（1h）

```
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
⑦ Android/Snort 主题
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  答题模板：
  Android四层安全：
  · ASLR(地址空间布局随机化) → 内核层
  · 安全沙箱 → 系统运行层
  · 权限声明机制 → 应用框架层
  · 应用程序签名 → 应用程序层
  · 权限配置文件：AndroidManifest.xml
  Snort：
  · 基于规则的误用检测 + 基于网络的IDS
  · 三种模式：嗅探/包记录/NIDS
  · 规则结构：alert 协议 源 -> 目的 (msg:"";content:"";sid:)
  · 部署：旁路监听核心交换机（端口镜像SPAN）
  安服转译：客户问怎么检测入侵→部署Snort→旁路监听→写规则

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
⑧ DNS隧道/CC攻击 主题
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
  答题模板：
  · DNS属于应用层协议
  · DNS报文域名编码：长度前缀格式（03 www 05 humen 03 com 00）
  · Windows查DNS流量来源：netstat -ano | findstr 53
  · 恶意类型：CC攻击/僵尸网络（DNS隧道做C2）
  · 异常行为：大量随机Base64子域名
  · 违反：可用性+可控性
  · iptables禁止DNS出站：-A OUTPUT -s IP -p udp --dport 53 -j DROP
  · DNS成CC首选原因：防火墙默认放行+可携带数据+包体小+UDP无状态
  安服转译：发现DNS异常→查随机子域名→封禁DNS出站
```

---
