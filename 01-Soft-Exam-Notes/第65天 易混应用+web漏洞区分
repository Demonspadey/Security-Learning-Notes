## 一、核心目标
1. 熟记选择题易混选项的区分关键，快速锁定正确答案
2. 掌握案例题概念辨析的标准答题话术，踩中得分点
3. 梳理易错题坑点，避免概念混淆失分

---
## 二、选择题高频考点速记
### 考点1 Web攻击辨析
- 看执行位置：服务端数据库→SQL注入；客户端浏览器→XSS；冒用用户会话→CSRF
- 看载荷特征：单引号+SQL关键字→注入；script标签+脚本→XSS；跨站伪造请求→CSRF

### 考点2 IDS与IPS辨析
- 看部署方式：旁路→IDS；串联→IPS
- 看响应能力：只告警不拦截→IDS；能主动阻断→IPS

### 考点3 访问控制模型辨析
- 所有者自主授权→DAC；安全标签强制管控→MAC；角色中间层→RBAC
- 保机密→BLP；保完整→Biba

### 考点4 密码技术辨析
- 单密钥、速度快、大数据→对称加密
- 双密钥、速度慢、签名密钥交换→非对称加密
- 身份、完整性、不可否认→数字签名
- 机密性、防窃听→数据加密

### 考点5 基础概念辨析
- 先鉴别（验身份），后认证（授权限）
- 漏洞是内因，威胁是外因，风险是可能性

---
## 三、案例题标准答题模板
### 模板1 三类Web攻击对比辨析
作答：
三者核心区别在于攻击目标与执行位置不同：
1. **SQL注入**：攻击目标为后端数据库，在服务器端执行，通过恶意SQL拼接篡改查询逻辑，核心危害是数据泄露与篡改，核心防御手段为参数化查询。
2. **XSS跨站脚本**：攻击目标为用户浏览器，在客户端执行，通过注入恶意脚本窃取用户会话，核心防御手段为输出编码与Cookie HttpOnly。
3. **CSRF跨站请求伪造**：攻击目标为用户登录身份，冒用受害者会话向服务器发起请求，核心防御手段为CSRF Token校验。

### 模板2 IDS与IPS的区别与选型
作答：
1. **核心区别**：
   - IDS为入侵检测系统，旁路镜像部署，仅被动监控告警，不拦截攻击，不影响业务流量，适合事后审计溯源。
   - IPS为入侵防御系统，串联在线部署，可主动实时阻断攻击，误报可能影响正常业务，适合线上实时防护。
2. **选型建议**：
   - 互联网出口、核心业务链路，需要实时拦截攻击的场景选用IPS；
   - 内网审计、溯源取证、合规留存场景选用IDS；
   - 高安全要求场景可二者配合部署。

### 模板3 三种访问控制模型对比
作答：
1. **DAC自主访问控制**：由资源所有者自主分配权限，灵活度高，权限可传递，适用于个人设备、共享文件场景。
2. **MAC强制访问控制**：由系统安全标签决定访问权限，策略固定不可自主修改，安全性极高，适用于军工、政务等高密级系统。
3. **RBAC基于角色的访问控制**：通过角色解耦用户与权限，用户绑定角色获取对应权限，便于批量管理，是企业业务系统的主流方案。

### 模板4 BLP与Biba模型对比
作答：
1. **BLP模型**：核心保护机密性，遵循“下读上写”规则，防止高密级数据流向低密级主体，多用于涉密文档管理场景。
2. **Biba模型**：核心保护完整性，遵循“上读下写”规则，防止低可信数据污染高可信数据，多用于金融交易、工控系统防篡改场景。

---
## 四、易错题坑点汇总
1. 概念混淆坑：数字签名无机密性，数据加密无不可否认性，二者功能不可混淆
2. 部署方式坑：IDS旁路、IPS串联，记反则整题失分
3. 模型规则坑：BLP与Biba的读写规则易记反，可通过“保机密下读，保完整上读”辅助记忆
4. 顺序坑：先鉴别后认证，先有漏洞再遇威胁才产生风险，顺序类题目注意先后逻辑
5. 场景坑：企业主流选RBAC，高密级选MAC，不要混淆适用场景
