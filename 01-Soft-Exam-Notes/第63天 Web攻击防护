## 一、核心目标
1. 熟记单选高频考点，快速匹配攻击类型与防护方案
2. 掌握五大攻击的标准简答话术，案例题直接套用
3. 梳理易混攻击的边界区别，规避选择题坑点

---
## 二、单选高频考点（必背）
### 考点1 攻击类型与特征匹配
| 攻击类型 | 核心特征关键字 |
|----------|----------------|
| SQL注入 | 单引号、UNION、OR 1=1、数据库报错 |
| XSS | script、alert、onerror、javascript |
| CSRF | 跨站请求、自动携带Cookie、伪造操作 |
| 路径遍历 | ../、etc/passwd、目录跳转 |
| 命令注入 | ;、|、&、系统命令关键字 |

### 考点2 分类与分级
- XSS三大类：反射型、存储型、DOM型；存储型危害最大、最持久
- 文件包含两类：本地文件包含（LFI）、远程文件包含（RFI）

### 考点3 核心防护手段对应
- SQL注入 → 参数化查询（预编译）
- XSS → HTML实体编码、HttpOnly、CSP
- CSRF → CSRF Token、SameSite、Referer校验
- 路径遍历 → 路径白名单、目录锁定
- 命令注入 → 禁止命令拼接、安全函数执行

### 考点4 危害等级
- 命令注入：可直接接管服务器，危害等级最高
- SQL注入：可导致核心数据泄露，高危
- 存储型XSS：影响所有访问用户，高危
- CSRF：非授权操作，中高危
- 路径遍历：信息泄露，中危

---
## 三、简答/案例题 标准答题模板
### 模板1 简述SQL注入的原理与防护措施
作答：
1. **原理**：Web应用未对用户输入做有效过滤，恶意内容被拼接进SQL语句执行，篡改了原有查询逻辑。
2. **防护措施**：
   ① 使用参数化查询（预编译语句），从根源杜绝SQL语义篡改；
   ② 对用户输入进行白名单校验；
   ③ 数据库账号遵循最小权限原则，降低注入危害；
   ④ 屏蔽数据库详细报错信息，避免泄露库表结构。

### 模板2 简述XSS的分类与防护措施
作答：
1. **分类**：XSS分为三类
   ① 反射型：恶意脚本通过URL传递，单次触发，无持久化；
   ② 存储型：恶意脚本存入服务器数据库，所有访问用户均触发，危害最大；
   ③ DOM型：攻击完全发生在客户端，由前端JS处理不当导致。
2. **防护措施**：
   ① 页面输出内容进行HTML实体编码；
   ② Cookie设置HttpOnly属性，防止脚本窃取会话；
   ③ 配置CSP内容安全策略，限制外部脚本加载。

### 模板3 简述CSRF的原理与防护措施
作答：
1. **原理**：攻击者诱导用户在已登录目标网站的状态下，访问恶意构造的请求页面，利用浏览器自动携带会话Cookie的特性，执行非预期的操作。
2. **防护措施**：
   ① 请求中添加随机CSRF Token并校验，是最主流的防护方案；
   ② 设置Cookie的SameSite属性，限制跨站请求携带Cookie；
   ③ 校验HTTP请求的Referer字段，验证请求来源。

### 模板4 简述命令注入的原理与防护措施
作答：
1. **原理**：Web应用将用户可控的输入直接拼接进系统命令执行，攻击者通过命令连接符注入恶意指令。
2. **防护措施**：
   ① 禁止直接拼接系统命令，使用安全的函数接口执行操作；
   ② 对用户输入进行严格的特殊字符过滤；
   ③ 运行Web服务的账号遵循最小权限原则，限制命令执行范围。

---
## 四、易错题坑点汇总
1. 功能边界坑：XSS偷Cookie、SQL注入脱库、命令注入拿服务器，三者危害不可混淆
2. 分类坑：XSS三大类无“遍历型”，路径遍历不属于XSS
3. 防护坑：参数化查询只防SQL注入，HttpOnly只防XSS偷Cookie，不可交叉错配
4. 原理坑：CSRF不需要注入脚本，XSS才需要注入脚本
5. 场景坑：存储型XSS持久化影响所有用户，反射型仅单次触发
