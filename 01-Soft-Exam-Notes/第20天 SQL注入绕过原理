## 一、必背简答考点
### 1. SQL注入的主要危害
可导致业务敏感数据非法泄露、攻击者越权访问系统、篡改或删除业务数据、绕过身份登录验证，严重时可获取服务器控制权，危害系统整体安全。

### 2. SQL注入基础绕过防护思路
对用户输入进行关键字归一化处理、拦截危险特殊字符、采用预编译语句从根源避免注入风险。

---

## 二、高频选择考点
- `information_schema` 是MySQL系统元数据库，存储数据库、表、字段的全部元数据信息；
- 大小写变形、内联注释是SQL注入最基础的关键字绕过方式；
- 预编译/参数化查询是防范SQL注入的核心手段。

---

## 三、模拟练习题 

### 一、单项选择题
1. 以下不属于SQL注入攻击危害的是（）
   A. 敏感业务数据泄露
   B. 攻击者获取服务器控制权
   C. 网络物理线路损坏
   D. 非法篡改、删除数据库数据
   > 答案：C

2. 攻击者使用`/*UnIoN*/`绕过WAF检测，采用的是哪种SQL注入基础绕过方式（）
   A. URL编码绕过
   B. 内联注释绕过
   C. 特殊字符截断绕过
   D. 数据加密绕过
   > 答案：B

3. MySQL中用于存储所有数据库、表、字段结构信息的系统元数据表是（）
   A. mysql.user
   B. information_schema
   C. sys
   D. performance_schema
   > 答案：B

4. 针对SQL注入关键字大小写绕过，有效的基础防护手段是（）
   A. 直接拦截所有含英文字母的输入
   B. 对用户输入关键字做统一大小写归一化处理
   C. 关闭数据库服务
   D. 禁止用户提交表单数据
   > 答案：B

5. 以下不属于SQL注入基础防护思路的是（）
   A. 使用预编译SQL语句
   B. 对用户输入进行危险字符过滤
   C. 完全开放数据库远程访问权限
   D. 拦截危险注释符、分号等特殊字符
   > 答案：C

---

### 二、案例分析题
#### 案例场景
某企业开发用户登录系统，后端代码直接将用户输入的用户名、密码拼接成SQL语句执行，未做任何安全校验。攻击者在登录用户名输入框中输入：`admin' OR 1=1 -- `，成功绕过登录验证；同时攻击者利用关键字大小写变形、内联注释等方式，绕过了网站部署的基础WAF，进一步查询数据库中的用户敏感数据。

#### 问题
1. 结合案例，分析该系统存在SQL注入漏洞的根本原因。
2. 列举案例中攻击者使用的2种SQL注入基础绕过技术，并说明其原理。
3. 针对该系统的SQL注入漏洞，提出3条切实可行的防护措施。


1.  **漏洞根本原因**
    - Web应用对用户输入数据缺乏有效的过滤、转义和合法性校验；
    - 后台程序直接采用字符串拼接方式构造SQL语句；
    - 未使用参数化查询、预编译等安全编码方式；
    - 数据库账户权限过大，敏感信息对外暴露。

2.  **绕过技术及原理**
    - **关键字大小写变形绕过**：利用WAF对关键字匹配大小写敏感缺陷，混搭大小写绕过执行；
    - **内联注释绕过**：利用内联注释包裹关键字，规避WAF关键字检查，数据库仍可正常执行。

3.  **防御措施**
    - 采用参数化查询、预编译语句，杜绝SQL语句拼接；
    - 采用输入白名单机制，对所有用户输入严格过滤，特殊字符转义处理；
    - 遵循数据库最小权限原则，降低账户操作权限。

---

## 四、判断题（易混考点自检）
1. `information_schema` 是用户自定义数据表，可随意修改删除。（×）
2. SQL注入内联注释绕过，会导致数据库无法正常解析语句。（×）
3. 预编译语句是防范SQL注入的有效核心手段。（√）
