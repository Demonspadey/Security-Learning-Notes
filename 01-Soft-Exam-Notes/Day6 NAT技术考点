---

## 二、【01-软考笔记/】考点版
# NAT技术考点复盘（信息安全工程师 Day6）

---

## 1. 核心概念与分类
### 1.1 NAT定义
NAT（网络地址转换）是一种将内网私有IP地址映射为公网IP地址的技术，核心作用：
- 解决IPv4地址枯竭问题，实现多主机共享公网IP
- 隐藏内网拓扑结构，减少攻击面，提升内网安全性

### 1.2 三大核心类型
| 类型 | 全称 | 核心作用 | 部署位置 |
| :--- | :--- | :--- | :--- |
| **SNAT** | 源地址转换 | 内网主机访问外网时，修改源IP为公网IP | `POSTROUTING`链（路由后） |
| **DNAT** | 目的地址转换 | 外网访问内网服务时，修改目的IP为内网IP | `PREROUTING`链（路由前） |
| **NAPT/PAT** | 端口多路复用 | 多内网主机共享同一公网IP，通过端口区分连接 | 与SNAT/DNAT配合使用 |

---

## 2. NAT技术的优缺点
| 优点 | 缺点 |
| :--- | :--- |
| 节省公网IP地址资源 | 部分端到端协议（IPSec VPN、P2P应用）无法正常工作 |
| 隐藏内网拓扑，减少攻击面 | 增加数据包转发延迟，不利于日志审计与溯源 |
| 实现内网服务对外发布 | **无法防御应用层攻击（SQL注入、XSS）和DDoS攻击** |

---

## 3. 高频考点与易错题
### 3.1 必背考点
1.  NAT本身**不是安全防护手段**，仅为地址转换技术，需配合防火墙ACL规则使用。
2.  防火墙中NAT执行顺序：`PREROUTING → 路由判断 → POSTROUTING`，因此DNAT优先于SNAT执行。
3.  FTP主动模式在NAPT环境下易失效，因数据端口固定（20），无法动态映射；被动模式不受影响。

### 3.2 易错题复盘
1.  **题目**：内网主机主动访问外网时，将内网IP映射为同一公网IP的不同端口号的技术是？
    - 错误选项：SNAT
    - 正确选项：NAPT（端口多路复用）
    - 解析：NAPT通过端口号区分多主机连接，是实现共享公网IP的核心技术。

2.  **题目**：以下关于SNAT和DNAT的应用场景，说法正确的是？
    - 错误选项：SNAT用于外网用户访问内网服务器时，修改目的IP为内网服务器IP
    - 正确选项：SNAT通常部署在防火墙的出站方向，修改数据包的源IP
    - 解析：SNAT用于内网访问外网，修改源IP；DNAT用于外网访问内网，修改目的IP。

---

## 4. 答题模板
- **问题**：简述SNAT与DNAT的核心区别及典型应用场景？
  > 答：SNAT部署在防火墙出站方向（POSTROUTING链），修改数据包源IP，用于内网主机共享公网IP访问外网；
        DNAT部署在入站方向（PREROUTING链），修改数据包目的IP，用于外网用户访问内网服务器。两者配合可实现内网服务对外发布，
        同时隐藏真实IP。

- **问题**：NAPT的工作原理及优势是什么？
  > 答：NAPT是一种多对一的NAT技术，核心是通过端口号复用实现多个内网主机共享同一个公网IP。
       内网主机访问外网时，NAT设备将（内网IP+源端口）映射为同一公网IP的不同端口，并维护映射表；外网响应时，
       再根据端口号将数据包转发回对应内网主机。优势是仅需1个公网IP即可支持数百台内网主机同时上网，极大节省IP资源，同时隐藏内网拓扑。
