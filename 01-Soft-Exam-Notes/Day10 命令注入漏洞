# 软考｜命令注入漏洞 学习笔记
---

### 1. 漏洞核心定义
- **漏洞成因**：后端程序未对用户可控输入进行过滤/转义/白名单限制，直接将输入拼接到系统命令中执行，导致攻击者可注入恶意命令，实现任意系统指令执行。
- **常考场景**：ping工具、文件管理、系统运维类Web功能。

### 2. PHP高危命令执行函数（必背）
| 函数名 | 功能说明 | 风险等级 |
|--------|----------|----------|
| `system()` | 执行命令并输出结果 | 极高 |
| `exec()` | 执行命令，返回最后一行输出 | 高 |
| `shell_exec()` | 执行命令，返回完整输出 | 高 |
| `passthru()` | 执行命令并直接输出原始结果 | 极高 |
| `popen()` | 执行命令，返回文件指针 | 中 |

### 3. 命令分隔符与执行逻辑（高频考点）
| 分隔符 | 执行逻辑 | 典型场景 |
|--------|----------|----------|
| `;` | 无论前命令成败，全部顺序执行 | 无条件拼接命令 |
| `&&` | 前命令执行成功，才执行后命令 | 条件执行（成功才走） |
| `||` | 前命令执行失败，才执行后命令 | 条件执行（失败才走） |
| `&` | 后台并行执行，不阻塞主进程 | 异步执行命令 |
| `|` | 管道符，前命令输出作为后命令输入 | 命令结果传递 |

### 4. 命令注入防御措施（简答默写模板）
1. **输入白名单校验**：严格限制输入格式（如IP仅允许数字和`.`）。
2. **特殊字符转义**：使用`escapeshellarg()`/`escapeshellcmd()`对输入转义，破坏命令拼接语法。
3. **禁用高危函数**：通过`php.ini`的`disable_functions`禁用命令执行类函数。
4. **最小权限原则**：Web服务以低权限用户运行，限制命令执行影响范围。

### 5. 高频易混考点
- SQL注入：依靠**预编译、参数化查询**根治。
- 命令注入：**无预编译**，只能通过过滤、转义、白名单防御。
- `trim()`仅去除首尾空格，**无法防御命令注入**。
