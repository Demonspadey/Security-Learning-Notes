# Day4 软考复盘：XSS防御考点（教材第22章）

---

## 一、核心考点梳理
| 防御手段 | 适用场景 | 优缺点 |
| :--- | :--- | :--- |
| 输入过滤（白名单） | 严格可控的输入场景（如用户名） | 优点：从源头拦截；缺点：灵活性差，易误杀合法内容 |
| 输出编码 | 所有用户输入直接渲染的场景（如评论、昵称） | 优点：不影响正常显示，同时阻断脚本执行；缺点：无法完全防御伪协议类绕过 |
| CSP内容安全策略 | 高安全需求的站点 | 优点：从执行层面限制脚本来源；缺点：配置复杂，配置不当易误杀合法脚本 |
| HttpOnly Cookie | 所有涉及会话的站点 | 优点：即使XSS触发也无法直接窃取Cookie；缺点：无法阻止脚本执行本身 |

---

## 二、软考题易错点复盘
1.  **XSS防御的适用场景**:
    - 输出编码优先用于用户输入直接渲染的场景（如评论、昵称），既能防XSS，又不影响正常字符显示。
    - 白名单标签过滤会限制用户输入的灵活性，不适合需要特殊符号的场景（如表情 `<3`）。
2.  **HTTPS与XSS的区别**:
    - HTTPS是传输层加密，防止数据在传输过程中被窃听/篡改；
    - XSS是应用层攻击，HTTPS无法直接防御XSS，两者没有直接关联。

---

## 三、错题修正
- **第3题**：原答案D（白名单标签过滤） → 修正为 **B（输出编码）**
  - **原因**：评论场景需要保留正常特殊字符，输出编码既能防XSS，又不影响显示，是最适合的方案。

---

## 四、今日总结
- 完全对应教材第22章考点，理清了不同防御手段的适用场景与局限性。
- 为软考中的案例分析题（如“某论坛XSS漏洞加固方案”）打下了坚实的理论基础。
