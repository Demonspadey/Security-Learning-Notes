## 📚 软考理论部分（访问控制+越权漏洞）
### 二、软考题易错点
1.  **四种访问控制模型核心考点对比**
    - DAC（自主访问控制）：由资源所有者自主分配权限，用户可修改权限，灵活性高但安全性低，适用于普通企业系统。
    - MAC（强制访问控制）：由系统按安全级别/密级统一控制，用户和资源所有者均无权修改权限，安全性极高，适用于军事、政府涉密系统。
    - RBAC（基于角色访问控制）：权限先分配给角色，再将角色分配给用户，便于批量管理权限，是当前企业主流的权限管理方案。
    - ABAC（基于属性访问控制）：基于用户属性、资源属性、环境属性、操作属性进行动态权限判断，灵活性最强，适用于复杂场景（如云平台）。

2.  **访问控制核心概念高频考点**
    - 主体：访问资源的发起者（用户、进程）。
    - 客体：被访问的资源（文件、数据库、接口）。
    - 参考监视器：访问控制模型的核心组件，负责拦截非法访问请求，判断主体对客体的访问是否合法。
    - 访问控制核心目标：防止非法用户访问资源，防止合法用户越权访问资源。

3.  **越权漏洞软考简答题满分模板**
    - 定义：
      - 水平越权：同一权限级别的用户，突破边界访问其他用户的资源。
      - 垂直越权：低权限用户突破边界访问高权限用户的资源。
    - 成因：① 服务端过度依赖前端传入的用户ID/资源ID标识资源归属；② 未校验当前用户是否拥有对应资源的访问权限；
      ③ 会话校验机制缺失，未验证用户身份凭证的有效性。
    - 防御措施：① 服务端对所有资源请求执行严格的身份+权限双重校验；② 从服务端Session获取用户身份信息，不信任前端传入的身份参数；
      ③ 使用UUID等无序字符替代简单数字ID，避免资源ID被遍历猜测；④ 完善会话管理，设置Session过期机制，用户登录后强制刷新SessionID；
      ⑤ 实现全局权限拦截器，统一校验接口访问权限。

4.  **会话固定攻击原理与防御（软考高频考点）**
    - 原理：攻击者提前获取一个有效SessionID，诱导用户使用该SessionID登录系统；用户登录后，攻击者复用此SessionID，
      即可冒充用户身份访问系统资源。
    - 防御方法：① 用户登录成功后，服务端立即刷新生成新的SessionID，废弃原有SessionID；② 为Cookie设置`HttpOnly`属性，
      防止脚本窃取SessionID；③ 设置合理的会话过期时间，长时间无操作自动销毁会话；④ 绑定用户Session与客户端IP、UA信息，提升会话唯一性。

5.  **访问控制技术缺陷高频考点**
    - 会话管理缺陷：SessionID随机性不足、会话超时失效配置不当、会话固定/劫持风险。
    - 权限校验缺陷：服务端权限校验缺失、过度信任前端参数、未实现全局权限拦截。
    - Cookie安全缺陷：未设置`HttpOnly`/`Secure`属性、Cookie传输未加密。
