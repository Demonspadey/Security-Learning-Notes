## 一、主机安全加固（账、密、权、志、端口）
### 1.账户安全
理论：禁用系统默认管理员账户，清理僵尸闲置账号，遵循最小权限分配
实例：Windows重命名Administrator、Linux锁定无用账号
话术：禁用默认账号，清理僵尸账户，权限最小化管控。

### 2.密码安全策略
理论：密码≥8位(大小写+数字+特殊符号)、90天定期更换、连续输错5次锁定30min
实例：域控组策略配置全局口令规范
话术：高复杂度、周期换密、错误锁定三重防护。

### 3.文件权限加固
理论：敏感系统文件收紧权限，禁止777全局开放；/etc/shadow标准权限600仅root可读
实例：chmod 600 /etc/shadow
话术：关键文件严控权限，杜绝权限溢出泄露密码。

### 4.日志审计规范
理论：等保要求本地日志留存≥180天，异地远端备份，防止入侵后日志被篡改删除
实例：rsyslog转发日志至SIEM
话术：本地留足180天、异地备份、常态化审计。

### 5.服务端口加固
理论：关闭无用预装服务与闲置端口，防火墙做访问黑白名单
话术：无用端口全关停，边界防火墙管控访问。

## 二、网络安全体系考点
### 1.网络安全五大特征
整体性、协同性、过程性、全面性、适应性；不含随机/瞬时性
话术：整体协同过全面，动态适配新威胁。

### 2.经典安全模型
BLP：保障机密性，规则下读上写；
Biba：保障完整性，规则上读下写；
话术：BLP保机密防外泄，Biba保完整防篡改。

### 3.PDR闭环模型
Protect防护→Detect检测→Response响应
实例：防火墙防护、IDS检测告警、封禁攻击IP做响应
话术：防护拦攻击、检测抓异常、响应处置威胁。

## 三、日志核心价值（必考简答）
1.合规审计：留存操作记录，满足等保审计要求
2.异常监测：从日志识别SSH暴力破解、越权访问
3.入侵取证：入侵后作为电子证据还原攻击链路
4.策略优化：依托攻击日志迭代安全策略
话术：审计、监测、取证、优化四大作用。

## 四、HIDS与NIDS区分
HIDS主机型：数据源为本机日志、进程、文件变更；
NIDS网络型：数据源为全网链路流量、交换机镜像。

## 速记口诀
主机加固账密权志口；五特整协过全适；BLP密Biba整；PDR防护检测响应。

### 简答1：操作系统主机安全加固措施
1.账户：禁用默认管理员，清理僵尸账号，最小权限分配；
2.口令：复杂度、90天有效期、错误锁定策略；
3.权限：收紧/etc/shadow等关键文件权限为600；
4.端口：关停无用服务端口，防火墙访问管控；
5.日志：日志留存≥180天+异地备份。
答题话术：清账号、强密码、锁权限、封端口、存日志。

### 简答2：日志四大作用
合规审计、异常入侵监测、入侵取证溯源、优化安全防护策略。
答题话术：审计留痕、监测告警、取证溯源、策略闭环。

## 二、案例答题模板
场景：服务器遭SSH爆破，漏洞：僵尸账号+弱口令+shadow权限777+日志仅存30天无异地备份。
整改：清理闲置账号、配置密码锁定策略、shadow改为600、日志异地留存180天、部署HIDS。
排查命令：last、lastb、grep筛选secure失败IP、who查看在线用户。
答题话术：删废号、加固口令、收紧权限、规范日志、主机入侵监测兜底。

## 三、高频错题归档
1.Windows日志：4624登录成功，4625登录失败，eventvwr.msc打开事件查看器
2.Linux日志区分
utmp-who(在线用户)、wtmp-last(历史登录)、btmp-lastb(登录失败)
CentOS认证:/var/log/secure；Ubuntu:/var/log/auth.log
3./etc/shadow权限=600，严禁777/755
4.等保日志最低留存180天
5.HIDS不取全网流量，流量属于NIDS数据源
