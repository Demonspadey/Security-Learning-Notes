Part 1：案例① 访问控制ACL（2024Q2 完整真题，35min）

📌 纸笔计时30min，完整手写，然后对照答案红笔标采分点。

```
【2024年 下午案例 第2题 · 完整真题】

【说明】访问控制是对信息系统资源进行保护的重要措施，适当的访问控制
能够阻止未经授权的用户有意或者无意地获取资源。访问控制一般是在操作系统
的控制下，按照事先确定的规则决定是否允许用户对资源的访问。图2-1给出了
某系统对客体traceroute.mpg实施的访问控制规则。

【图2-1 字符还原（Windows文件安全权限界面）】
文件名：traceroute.mpg
属性窗口 - 安全 选项卡
组或用户名列表：□ Administrator  □ Everyone
选中用户：Administrator
权限项目          允许  拒绝
完全控制          □     □
修改              □     □
读取及运行        √     □
读取              √     □
写入              □     □
☑ 允许将来自父系的可继承权限传播给该对象

问题1. 针对信息系统的访问控制包含哪些基本要素？

问题2. 分别写出图2-1中用户Administrator对应三种访问控制实现方法，
即能力表、访问控制表和访问控制矩阵下的访问控制规则。

━━━ 加练·2023Q1（同主题真题，补全三要素）━━━
【说明】访问控制是保障信息系统安全的主要策略之一，其主要任务是保证系统
资源不被非法使用和非常规访问。访问控制规定了主体对客体访问的限制，并在
身份认证的基础上，对用户提出的资源访问请求加以控制。当前，主要的访问控制
模型包括：自主访问控制(DAC)模型和强制访问控制(MAC)模型。
问题1. 针对信息系统的访问控制包含哪三个基本要素？

【参考答案】
2024Q2问题1：主体、客体、访问控制策略（控制规则）⭐三要素
2024Q2问题2：Administrator对traceroute.mpg拥有"读取"和"读取及运行"权限（√）
  ① 能力表（Capability List）：以主体为中心，
     Administrator持有的能力表：{traceroute.mpg：读取、读取及运行}
  ② 访问控制表（ACL）：以客体为中心，
     traceroute.mpg的ACL条目：Administrator → 允许读取、允许读取及运行
  ③ 访问控制矩阵：行列交叉点，
     矩阵[Administrator][traceroute.mpg] = {读取、读取及运行}
2023Q1加练问题1：主体、客体、授权访问（访问控制策略/规则）⭐
  · 主体 = 发起访问者（用户/进程）
  · 客体 = 被访问资源（文件/数据/设备）
  · 授权规则 = 允许/拒绝的策略

📌 三者关系（补全）：
  能力表和ACL是访问控制矩阵的两种特殊形式
  · 能力表 = 矩阵的"行"（以主体为中心：我有哪些权限）
  · ACL   = 矩阵的"列"（以客体为中心：谁能访问我）
  · 矩阵  = 完整行列二维表
```

**✏️ 细致追问（口答）**：
```
① 能力表 vs ACL vs 矩阵 的核心区别？
   → 能力表=以主体为中心(我有哪些权限)；ACL=以客体为中心(谁能访问我)；
     矩阵=行列二维(行列交叉点即权限)
② 图2-1中Administrator没有哪些权限？
   → 无完全控制、无修改、无写入（只有读相关）
③ 为什么Everyone没在"允许"里给权限？
   → 未授权（默认拒绝/最小权限），Everyone通常需单独设置
④ Windows ACL vs Linux权限 区别？
   → Windows ACL基于用户/组+具体权限(读/写/执行/修改)；Linux基于owner/group/other+rwx
⑤ 这个图体现的是DAC还是MAC？
   → DAC（自主访问控制），资源所有者可自主授予/撤销权限
⑥ DAC vs MAC 核心区别？
   → DAC=所有者自主决定权限(ACL/能力表/矩阵是实现方式)；MAC=系统强制策略(BLP/Biba)
   ⚠️ 别混淆：能力表/ACL/矩阵是"实现方式"，DAC/MAC是"模型"，两个维度

━━━ 补充·访问控制矩阵/ACL/能力表 行列辨析 ⭐⭐核心 ═══
① 三张表的行列（重点！别把"原始矩阵"和"拆分小表"的行列混在一起）
   原始访问控制矩阵（大表）：
     行 = 主体，列 = 客体，交叉点 = 该主体对该客体的权限
     ⚠️ 原始矩阵：行=主体！（有说法"矩阵行放客体"是错的）

   ACL（访问控制表）= 矩阵按"列"分解 → 以客体(文件)为中心
     小表：行 = 主体（每个主体一行），列 = 该客体对应的权限
     例：traceroute.mpg 的 ACL = 列出"谁能访问它"（主体在行）

   能力表 = 矩阵按"行"分解 → 以主体为中心
     小表：行 = 客体（每个客体一行），列 = 权限
     例：Administrator 的能力表 = 列出"他能访问什么"（客体在行）

   🔑 记忆锚点：
     ACL 以客体为中心 → 客体固定 → 主体在"行"
     能力表以主体为中心 → 主体固定 → 客体在"行"
     原始矩阵大表 → 行=主体、列=客体

② SAM文件属性的客体访问控制表？
   → SAM文件的ACL默认只允许SYSTEM账户完全控制，
     管理员也无法直接读取（需提权到SYSTEM）⭐
   ⚠️ 这解释了为什么安全日志/SAM敏感数据普通用户看不了
```

---

### 🔴 Part 2：案例② 密码学保密通信模型（2023Q3 完整真题，35min）

📌 纸笔计时30min，完整手写，然后对照答案红笔标采分点。
   ⚠️ 密码学是🔴必考，这道题把"先哈希后加密"考透了。

```
【2023年 下午案例 第3题 · 完整真题】

【说明】密码学的基本目标是在有攻击者存在的环境下，保证通信双方(A和B)
之间能够使用不安全的通信信道实现安全通信。密码技术能够实现信息的保密性、
完整性、可用性和不可否认性等安全目标。
符号定义：M=消息，H=Hash函数，E=加密，D=解密，K=密钥，
SKA/PKA=A的私钥/公钥，SKB/PKB=B的私钥/公钥，||=连接操作。

【图2-1 保密通信模型一】
用户A侧：M → H → H(M)；M与H(M)拼接 → E(K加密) → 输出 M||E_K[H(M)]
传输链路 → 用户B侧
用户B侧：接收密文 → D(K解密) 拆分出M、H(M)；M → H 算出H(M)'；对比

问题1（6分）请问图2-1所设计的保密通信模型能实现信息的哪些安全目标？
图2-1中的用户A侧的H和E能否互换计算顺序？如果不能互换请说明原因；
如果能互换请说明对安全目标的影响。

【图2-2 保密通信模型二】
用户A侧：M → H → H(M)；M || H(M) → E(K) 整体加密
传输 E_K[M||H(M)] → 用户B
用户B：D(K)解密得到M、H(M)；M重新计算哈希H(M)'，比对校验

问题2（4分）请问图2-2设计的保密通信模型能实现信息安全的哪些特性？

【图2-3 保密通信模型三（支持不可否认）】
用户A：M → H → H(M)；H(M) → E(SKA私钥签名)；M || 签名密文 → E(K对称加密) → 传输(1)______
用户B接收后D(K)解密，拆分M、签名密文；签名密文 → D(2)______ 验证签名得到原始哈希；本地计算M的哈希比对

问题3（5分）请问图2-3中(1)，(2)分别应该填什么内容？

【参考答案】（做完对信管网核对）
问题1：实现"完整性"（哈希校验）+"对哈希的机密保护"（E_K加密哈希，防攻击者同时篡改M和重算H(M)）
       H和E【不能互换】⭐：若先加密后哈希，哈希只能验证"密文"完整性，
       无法验证"明文"完整性——攻击者篡改明文后，接收端无法通过哈希发现明文被改
问题2：实现"机密性"（整体加密，M和H(M)一起加密）+ "完整性"（哈希校验）
       ⭐模型二=整体加密，保密+完整都满足
问题3：(1) = M || E_K[ E_SKA[H(M)] ]  ← M + 用会话密钥K加密的(用A私钥签名的哈希)
       (2) = D(PKA)  ← 用A的公钥验证签名
       ⭐签名用私钥、验签用公钥；先签名后加密=推荐顺序
```

**✋ 注意**：模型一问题1的答案（完整性+哈希机密性）是基于密码学原理的判断，**做完务必对照信管网核对**——如果信管网答案有出入，以信管网为准。

**✏️ 细致追问（口答）**：
```
① 先签名后加密 vs 先加密后签名？
   → 推荐先签名后加密（Sign-then-Encrypt）：接收方能验证明文完整性+不可否认
     先加密后签名缺陷：只能验证密文完整性，不能验证明文完整性
② 模型一为什么要加密H(M)而不加密M？
   → 目的在防篡改：加密哈希让攻击者不能同时伪造M和H(M)，保证完整性校验有效
③ 模型二和模型一的区别？
   → 模型二整体加密（M+哈希一起加密）→ 多了保密性；模型一M明文
④ 数字签名实现哪些安全目标？
   → 完整性（哈希）+ 不可否认性（私钥签名）+ 真实性（身份认证）
⑤ 对称加密K在这里负责什么？
   → 会话密钥，负责实际数据的机密性；非对称(公钥)负责密钥交换/签名
```

---
