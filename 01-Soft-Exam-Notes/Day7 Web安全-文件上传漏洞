Day7 Web安全-文件上传漏洞

# 文件上传漏洞考点笔记

## 1. 核心概念
### 1.1 黑白名单机制
- **黑名单**：禁止指定危险后缀（.php/.asp等），绕过方式多（大小写、双后缀、特殊后缀），安全性低。
- **白名单**：仅允许指定合法后缀（.jpg/.png等），几乎无法绕过，是推荐的安全方案。

### 1.2 文件重命名机制
- 作用：破坏攻击者路径预判，防止直接访问恶意文件；避免文件覆盖。
- 绕过场景：路径拼接缺陷、目录穿越漏洞。

## 2. 常见绕过方式
| 绕过类型 | 原理 | 典型Payload |
| --- | --- | --- |
| 文件头伪造 | 添加合法图片头绕过内容校验 | `GIF89a` + 恶意PHP代码 |
| 畸形后缀绕过 | 利用不区分大小写/双后缀解析规则 | `.pHp`、`shell.php.jpg` |
| 中间件解析漏洞 | 利用服务器解析规则缺陷 | Apache：`shell.php.aaa`；IIS：`shell.asp;.jpg` |
| .htaccess解析 | 自定义Apache解析规则 | `AddType application/x-httpd-php .jpg` |

## 3. 文件上传漏洞防御方案（软考必背）
1.  **前端**：仅做用户提示，不可作为安全校验。
2.  **后端**：
    - 采用白名单校验文件后缀。
    - 校验文件头与文件内容，防止伪造。
    - 限制文件大小与上传路径。
3.  **服务端**：
    - 上传文件随机重命名。
    - 限制上传目录执行权限，禁止脚本执行。
    - 修复中间件解析漏洞，禁用危险配置。
4.  **架构**：上传目录与Web目录分离，禁止直接访问。

## 4. 话术模块
- 简述文件上传白名单与黑名单的区别？
  答：黑名单禁止危险后缀，易被绕过，安全性低；白名单仅允许合法后缀，几乎无法绕过，安全性高，是推荐的校验方式。
- 服务器对上传文件重命名的作用是什么？
  答：主要作用是破坏攻击者对文件路径的预判，防止直接访问恶意文件；同时避免同名文件覆盖，降低漏洞利用成功率。
- 如何从前端到后端完整防御文件上传漏洞？
  答：前端仅做友好提示；后端白名单+文件内容校验；服务端随机重命名+限制目录执行权限；架构上上传目录与Web目录分离，修复中间件解析漏洞。
