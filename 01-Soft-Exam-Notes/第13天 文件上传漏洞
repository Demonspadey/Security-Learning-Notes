## 软考核心知识点
### 1. 文件上传漏洞定义
Web服务端未严格校验上传文件的后缀、文件类型、文件内容、文件头，允许上传恶意脚本、木马、Webshell，被攻击者利用执行恶意代码的高危漏洞。

### 2. 漏洞危害
- 上传Webshell，控制网站后台
- 上传恶意脚本，实现远程代码执行
- 植入持久化后门，长期控制业务系统
- 上传超大文件，耗尽服务器存储，造成拒绝服务

### 3. 漏洞核心成因
- 仅前端JS校验，无后端二次校验
- 只校验文件名后缀，不校验文件真实内容
- 黑名单过滤不全面，可被畸形后缀绕过
- 上传目录具备脚本执行权限

### 4. 常见上传校验方式
1. 前端JS校验：仅本地限制，可抓包/禁用JS绕过，安全性最低
2. 后缀名校验：分为黑名单、白名单两种机制
3. MIME类型校验：校验请求头`Content-Type`字段
4. 文件头校验：检测图片、文档等文件特征头
5. 文件内容检测：查杀恶意代码、木马特征

### 5. 黑白名单安全对比
| 类型 | 原理 | 安全性 | 软考推荐 |
| :--- | :--- | :--- | :--- |
| 黑名单 | 拦截已知危险后缀（如php、jsp） | 低，易被绕过 | ❌ 不推荐 |
| 白名单 | 仅允许业务合法文件（如jpg、png） | 高，默认拒绝未知 | ✅ 首选答案 |

### 6. 软考下午简答｜防护措施（背诵模板）
1. 严格限制上传文件后缀，采用白名单机制，禁止脚本格式文件
2. 服务端二次校验，禁止依赖前端JS校验
3. 校验文件真实类型、文件头、文件内容，防止文件类型绕过
4. 上传目录设置无执行权限，隔离存储路径
5. 重命名上传文件，随机哈希命名，防止路径遍历与解析漏洞
6. 限制单文件大小、上传频率，缓解拒绝服务风险

---
