## 一、背诵速记卡片
### 卡片1 BLP保密模型
1. 安全目标：机密性，军队涉密系统
2. 读规则：No Read Up 不上读（主体≥客体才能读）
3. 写规则：No Write Down 不下写（主体≤客体才能写）
4. 数据流：信息只能向上流动，杜绝高密数据外泄

### 卡片2 Biba完整性模型
1. 安全目标：完整性，银行、医疗核心系统
2. 读规则：No Read Down 不下读（主体≤客体才能读）
3. 写规则：No Write Up 不上写（主体≥客体才能写）
4. 数据流：信息只能向下流动，防止低质数据污染高价值数据
5. 核心对比：与BLP读写规则完全反向

### 卡片3 Linux扩展ACL
1. ACL按客体存储，能力表按主体存储
2. getfacl查看ACL；setfacl配置单用户/组、默认、递归ACL
3. 传统chmod UGO权限粒度粗，ACL支持精细化单独授权

### 卡片4 Windows SID
1. 用户/组唯一标识，删除重建同名用户SID不同
2. 高频RID：500管理员、501来宾、502 KRBTGT账户

### 卡片5 Windows安全描述符四组件
Owner SID、Group SID、DACL、SACL
DACL：管控访问权限；SACL：管控审计日志；拒绝ACE优先级高于允许ACE

### 卡片6 RBAC1角色继承
上级角色自动继承下级全部权限，递归汇总所有层级权限，贴合企业多层级组织架构

## 二、刷题高频错题陷阱汇总
1. BLP/Biba场景混淆：涉密军用选BLP；金融医疗防篡改选Biba
2. 读写规则记反：BLP不下写，Biba不上写，极易搞反丢分
3. ACL与能力表存储对象混淆：ACL绑定文件（客体），能力表绑定用户（主体）
4. DACL/SACL功能颠倒：DACL管访问，SACL管审计
5. SID特点易错：同名用户删除重建，SID不会复用
6. ACE权限冲突：拒绝永远优先，不要误以为允许生效

## 三、案例标准答题模板
### 模板1 BLP场景分析答题
例：机密级用户导出文件到公开U盘
答：不允许，遵循BLP *-属性No Write Down；主体密级（机密）高于客体（公开），高密数据写入低密设备会造成机密泄露，系统拦截该操作。

### 模板2 Biba场景分析答题
例：高完整核心系统读取低完整日志
答：不允许，遵循Biba简单完整条件No Read Down；高完整性主体读取低完整性数据，会被可能篡改的脏数据污染，造成业务逻辑错误。

### 模板3 Windows安全描述符问答
问：管理员想要记录文件访问日志配置哪项？
答：配置SACL（系统访问控制列表），添加审计ACE，成功/失败访问行为会记录到Windows安全事件日志中；DACL仅控制能否访问，不负责日志审计。

### 模板4 ACL作用简答
传统Linux文件权限仅支持所有者、组、其他三类权限，粒度粗糙；ACL可对任意独立用户、用户组单独分配读写执行权限，支持目录默认ACL自动继承，实现精细化权限管控。

## 四、易混对比速查表
1. BLP vs Biba
BLP：保密、不上读不下写、军用；Biba：完整、不下读不上写、金融医疗
2. ACL vs 能力表
ACL：依附文件客体，getfacl查看；能力表：依附用户主体，票据/令牌系统使用
3. DACL vs SACL
DACL：自主访问控制，允许/拒绝资源访问；SACL：系统审计控制，记录访问操作日志
4. SID vs UID
Windows用户唯一标识SID；Linux用户标识UID；SID永久唯一，UID可重复复用
