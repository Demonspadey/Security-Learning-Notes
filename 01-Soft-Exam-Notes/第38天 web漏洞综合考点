## 一、核心目标
1. 系统掌握8类高频Web漏洞全套考点
2. 区分易混漏洞差异，熟练各类题型作答思路
3. 牢记标准答题话术，应对填空、选择、简答、综合案例

## 二、必考速记总表
涵盖：SQL注入、XSS、CSRF、越权、文件上传、反序列化、文件包含、目录遍历

## 三、模拟练习题（附你的作答批改）
### 一、填空题
1. SQL注入漏洞核心防护手段是__________，漏洞作用层级为__________。
✅你的答案：参数化查询，预编译语句。代码开发层（正确）

2. XSS跨站脚本漏洞攻击对象是__________，无法直接控制服务器。
✅你的答案：用户浏览器（正确）

3. CSRF漏洞核心防护依靠__________验证身份，利用用户有效会话发起请求。
✅你的答案：CSRF token（正确）

4. 越权漏洞分为__________越权和__________越权，属于业务逻辑类漏洞。
✅你的答案：水平、垂直（正确）

5. 文件上传漏洞最优校验方式为__________，黑名单校验容易被绕过。
✅你的答案：白名单校验机制（正确）

6. Python反序列化漏洞常用触发魔术方法是__________，可直接执行系统命令。
✅你的答案：__reduce__（正确）

7. 文件包含分为本地包含__________和远程包含__________，远程包含需开启对应配置。
✅你的答案：LFI、RFI（正确）

8. 目录遍历漏洞核心利用字符是__________，该漏洞仅读取文件，__________代码执行能力。
✅你的答案：../,无（正确）

### 二、单项选择题
1. 能够直接篡改、窃取数据库全部数据的漏洞是（）
A. XSS  B. SQL注入  C. 目录遍历  D. CSRF
✅你的答案：B（正确）

2. 仅影响访问用户浏览器，不会控制服务器的漏洞是（）
A. 文件上传  B. 反序列化  C. XSS  D. 文件包含
✅你的答案：C（正确）

3. 借助用户登录状态，冒用身份发起请求的漏洞为（）
A. 越权  B. CSRF  C. 路径穿越  D. SQL注入
✅你的答案：B（正确）

4. 低权限账号访问管理员后台，属于哪类漏洞（）
A. 水平越权  B. 垂直越权  C. LFI  D. RFI
✅你的答案：B（正确）

5. 负责上传恶意脚本文件，为提权提供载体的漏洞是（）
A. 文件上传  B. 文件包含  C. 目录遍历  D. 反序列化
✅你的答案：A（正确）

6. 八大漏洞中危害等级最高，可直接执行系统命令接管服务器的是（）
A. 越权漏洞  B. 反序列化漏洞  C. XSS  D. CSRF
✅你的答案：B（正确）

7. 只能读取服务器文件、无法运行代码，仅造成信息泄露的是（）
A. 文件包含  B. 目录遍历  C. SQL注入  D. 上传漏洞
✅你的答案：B（正确）

8. 可以加载解析恶意文件执行代码，常和上传漏洞组合拿权限的是（）
A. 目录遍历  B. 文件包含  C. CSRF  D. XSS
✅你的答案：B（正确）

### 三、判断题
1. 预编译参数化查询是防御SQL注入最有效的方式。（✅你的答案：对）
2. XSS漏洞可以直接获取服务器最高操作权限。（✅你的答案：错）
3. CSRF攻击过程中，攻击者无法获取用户账号密码与Cookie。（✅你的答案：对）
4. 仅依靠前端限制权限，就可以彻底防御越权漏洞。（✅你的答案：错）
5. 文件上传黑名单校验安全性远低于白名单校验。（✅你的答案：对）
6. 远程文件包含RFI需要服务器开启远程包含配置才可利用。（✅你的答案：对）
7. 目录遍历漏洞可以构造语句执行系统命令。（✅你的答案：错）
8. 反序列化漏洞属于高危代码执行类漏洞，风险等级极高。（✅你的答案：对）

### 四、简答题
#### 问题1
你的作答：
文件上传：负责将恶意脚本文件上传至服务器，提供攻击载体
文件包含：加载服务器内恶意文件并解析执行代码，实现权限控制
二者常组合利用，上传木马后通过文件包含执行，最终获取网站权限
批改：逻辑清晰，采分点齐全，满分

#### 问题2
你的作答：
功能不同:目录遍历仅读取文件内容，无代码执行能力。文件包含可加载并执行文件代码。
危害不同:目录遍历核心危害是信息泄露。远程文件包含可直接RCE,控制服务器
利用条件:目录遍历无需特殊配置，RFI远程文件包含需要服务器开启对应配置。
批改：多维度对比完整，概念区分准确

#### 问题3
你的作答要点完整，成因、危害、防护分层作答规范，贴合考场满分模板
批改：内容详实，要点无缺失，作答合格满分

#### 问题4
你的作答：
CSRF:用户登录目标站点后点击恶意脚本链接，攻击者利用同源自动携带cookie特性，冒充用户执行未授权操作
越权漏洞:攻击者手动篡改ID等权限参数，绕过权限校验，主动访问他人资源
批改：原理差异表述清楚，术语使用标准

### 五、综合区分问答题
你的作答：
1. SQL注入：针对数据库，篡改窃取数据，操控数据库信息
2. XSS跨站：攻击客户端浏览器，窃取用户会话、篡改页面
3. 文件上传：传入恶意木马文件，搭建攻击载体
4. 文件包含：运行恶意脚本，执行代码实现控权
5. 目录遍历：跨目录偷看服务器文件，仅泄露信息无代码执行
✅批改：漏洞核心特点总结到位，体系认知清晰
