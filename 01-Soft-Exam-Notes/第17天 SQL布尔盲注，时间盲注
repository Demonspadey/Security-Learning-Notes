## 一、核心目标
1.  背诵SQL注入防护高频考点
2.  掌握软考案例题标准答题话术
3.  完成SQL注入相关真题题型练习

---

## 二、高频考点清单
### 1. Web安全防护基础
- 输入过滤遵循：**白名单优于黑名单**
- 防御核心：杜绝SQL字符串拼接，使用预编译
- 特殊字符必须做转义处理

### 2. SQL注入危害
- 敏感数据泄露
- 登录权限绕过
- 数据库数据篡改、删除
- 服务器提权沦陷

### 3. 数据库安全加固
- 严格执行**最小权限原则**
- 禁用数据库高危函数
- 关闭Web端详细错误信息回显

---

## 三、案例题标准答题话术
### 题目：简述防止SQL注入的主要工程措施
> 标准作答模板：
1.  开发层面采用**参数化查询、预编译语句**，杜绝字符串拼接构造SQL；
2.  对用户输入做合法性校验，采用**白名单机制**过滤、转义特殊字符；
3.  数据库账号配置最小权限，限制敏感操作与跨库访问；
4.  关闭Web应用详细错误信息回显，防止敏感报错泄露架构信息；
5.  部署WAF设备，特征拦截各类SQL注入攻击载荷；
6.  定期开展代码安全审计与漏洞扫描，迭代修复安全隐患。

---

## 四、练习题与答案解析
### 1. 单选题
1.  B（SQL注入根源：用户可控输入未过滤，直接拼接SQL）
2.  B（预编译参数化查询是开发层最有效的防御手段）
3.  C（无回显无报错时，优先使用布尔盲注）
4.  B（最小权限原则可缩小攻击面，限制攻击者操作范围）
5.  B（白名单仅允许合法内容，防御可靠性更高）

### 2. 多选题
1.  ABCD（SQL注入危害全覆盖：数据泄露、登录绕过、数据篡改、服务器沦陷）
2.  ABC（弱口令不是防护措施，其余均为标准Web层防护手段）
3.  AB（sleep()、benchmark()是时间盲注常用延时函数）

### 3. 判断题
1.  ×（WAF不能替代代码层防护，无法抵御绕过型注入）
2.  √（布尔盲注依靠页面真假状态逐位猜解数据）
3.  ×（黑名单极易被绕过，无法完全防御SQL注入变种）
4.  √（关闭错误回显可截断报错注入的利用条件）

---
