## 一、应试核心背诵卡片
### 卡片1 VPN四要素高频简答
四大要素分别为隧道、数据加密、身份认证、密钥交换；
隧道负责数据包封装路由，加密保障报文机密性，认证确认双方身份，密钥交换安全协商加密密钥。

### 卡片2 两种VPN组网场景必考区分
1.远程访问VPN：单人终端接入企业内网，外出员工移动办公场景，选择题固定答案优选SSL VPN
2.站点到站点VPN：两端网关对接，分公司与总部内网互通，强制使用IPSec隧道模式

### 卡片3 AH、ESP核心选择题考点
AH协议号51：仅完整性校验、源身份认证，**永远不提供加密**
ESP协议号50：加密+认证+完整性三重能力，IPSec加密场景唯一标准答案
考试陷阱选项：凡是描述AH可以加密数据全部直接判定错误

### 卡片4 IPSec两种模式考点总结
传输模式：保留原生IP头部，仅加密上层数据，主机点对点通信，软考选择低频考察
隧道模式：生成全新外层IP头，整个原始数据包加密保护，站点网关组网必考模式

### 卡片5 IKE两阶段考点固化记忆
IKE第一阶段：产出ISAKMP SA，搭建加密协商通道，主模式6条消息、野蛮模式3条消息
IKE第二阶段：产出IPSec SA，专门加密实际业务流量，固定采用快速模式协商
IKE底层端口硬性考点：UDP 500

### 卡片6 SA安全关联必背知识点
1.SA物理属性为单向结构，双向通信必须两组独立SA
2.SA标识三要素：SPI、目的IP、AH/ESP协议
3.SAD数据库集中存储本机所有安全关联策略
4.流量选择器依靠五元组划分需要进入VPN隧道的流量

### 卡片7 IPSec与SSL VPN对比简答模板
IPSec：网络层协议，需要客户端软件，完整覆盖内网全部IP流量，适配固定分支机构长期组网
SSL VPN：应用层协议，浏览器免客户端直接访问，权限可精细化控制单条Web应用，适合外勤人员临时访问外网

### 卡片8 TLS六步流程案例默写标准答案
1.ClientHello：客户端提交加密套件清单、客户端随机数值
2.ServerHello：服务器选定加密套件，下发CA证书、携带服务器随机数
3.客户端校验证书：核对证书有效期、CA签发合法性、网站域名匹配三项内容
4.客户端生成预主密钥，使用服务器公钥加密报文后发送
5.通信双方各自结合预主密钥、两端随机数独立计算主密钥，主密钥不在网络传输
6.握手流程结束，双方启用对称加密持续传输业务数据

### 卡片9 TLS版本考点精简
TLS1.2：两次往返握手，算法兼容性广泛；TLS1.3简化握手缩减为单次往返，强制开启前向安全机制

## 二、Linux IPSec & OpenVPN笔试考点
1.strongswan核心配置文件ipsec.conf、ipsec.secrets，left/right分别定义两端网关IP，leftsubnet声明本地内网网段
2.OpenVPN依托SSL/TLS协议，默认UDP1194端口，证书认证为企业生产环境主流认证方式

## 三、经典选择题错题陷阱汇总
1.端口易错点：IKE固定UDP500，DNS为UDP53，HTTPS TCP443
2.模式易错点：站点VPN只选隧道模式，传输模式仅限终端点对点
3.协议易错点：加密功能仅ESP具备，AH只有认证无加密
4.SA易错点：SA单向属性，双向通信必须两条SA条目
5.握手易错点：主密钥本地两端自行运算，不会经由网络传输

## 四、标准案例答题模板
### 模板一 站点VPN方案简答题
总部与分公司部署站点到站点IPSec VPN，协议选用ESP保障加密完整性，整体采用隧道模式；原因是网关之间通信需要封装外层IP头部，加密整个内网数据包，适配两端网段长期互通。

### 模板二 外勤人员接入简答
外勤出差员工优先部署SSL远程访问VPN，依靠浏览器直接访问企业网页业务，无需额外安装客户端，部署维护成本更低，访问权限能够限定在指定办公网站。

### 模板三 证书过期故障作答
服务器证书过期后客户端证书校验步骤直接失败，标准TLS握手流程强制终止；浏览器弹出证书过期安全告警，用户手动忽略风险才可临时访问，正式业务环境禁止强行绕过证书校验。

高频错题复盘：AH加密误区、隧道模式IP头、IKE两个SA类型、TLS证书三项校验点
