## 一、核心目标
1.  熟记SQL注入相关高频考点
2.  掌握二次注入案例题的标准答题话术
3.  明确SQL注入三层防御措施的分类与落地方法

---

## 二、高频考点背诵清单（今日必背）
1.  堆叠注入利用分号执行多条SQL语句，MySQL环境最常见。
2.  二次注入根源：入库过滤、出库无过滤，数据从数据库读出后直接拼接SQL。
3.  SQL注入整体防御分三层：开发层、应用层、数据库层。
4.  输入过滤不能只依赖前端校验，后端必须做二次校验。
5.  禁止应用程序使用root、sa、sysdba等高权限数据库账号。
6.  安全配置：关闭数据库远程访问、删除默认测试库、禁用存储过程高危函数。

---

## 三、案例简答标准模板（可直接默写）
### 题目：简述二次SQL注入的成因及防范措施
#### 成因
用户恶意输入在存入数据库时被转义过滤，业务逻辑读取该数据时未做过滤，直接拼接SQL语句执行，引发二次注入。

#### 防范措施（无分层通用版）
1.  前后端同时做输入合法性校验，不依赖前端单一过滤。
2.  全程使用预编译参数化查询，杜绝动态SQL拼接。
3.  从数据库读取数据后，再次做特殊字符过滤与转义。
4.  数据库业务账号配置最小权限原则，限制高危操作。
5.  关闭数据库详细错误回显，隐藏系统敏感信息。
6.  部署Web应用防火墙，拦截SQL注入等恶意攻击载荷。

---

## 四、课后练习错题总结
### 判断题
- 堆叠注入依靠逗号分隔多条SQL语句执行（×）
- 二次注入漏洞本质是入库过滤、出库未过滤（√）
- 前端做了输入过滤就可以防止二次注入（×）
