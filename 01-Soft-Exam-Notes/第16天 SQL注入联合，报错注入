# 二、软考考点笔记
### 1. 选择/填空高频考点
1. `order by` 语句作用：探测SQL查询字段总数
2. Union 联合查询使用限制条件：前后查询字段数量一致
3. 报错注入成功关键条件：**数据库详细错误信息外泄**
4. SQL注入最根本防御措施：**参数化预编译查询**
5. 高危风险点：错误详情外泄、关键字未过滤、账号权限过大

---

### 2. 简答题必背模板
#### （1）联合查询注入防护措施
1. 过滤拦截 `Union、Select、Order` 等数据库高危关键字；
2. 全程使用**参数化预编译查询**，杜绝SQL语句直接拼接；
3. 限制数据库查询返回字段与内容，屏蔽敏感字段回显；
4. 部署WAF规则，拦截联合查询类攻击特征；
5. 数据库账号遵循最小权限原则，禁止多库跨表查询。

#### （2）报错注入防护措施
1. 关闭Web服务详细数据库错误回显，统一自定义错误页面；
2. 过滤 `updatexml、extractvalue` 等报错类高危函数关键字；
3. 采用输入白名单校验，拒绝非常规特殊字符与畸形语句；
4. 数据库层限制函数权限，禁用高危自定义函数与报错函数。

#### （3）Web注入类案例分析通用答题框架
- **漏洞**：Web系统未对用户输入进行有效过滤、净化与合法性校验，直接拼接进入SQL语句，存在SQL注入漏洞。
- **危害**：攻击者可非法查询、窃取、篡改、删除数据库敏感数据，严重时可获取服务器权限、破坏业务系统。
- **原因**：代码直接拼接用户输入、未使用参数化预编译、数据库错误信息外漏、数据库账号权限过大、未部署访问防护设备。
- **措施**：采用参数化预编译查询；加强输入过滤与白名单校验；关闭详细错误回显；部署WAF拦截攻击特征；数据库账号遵循最小权限原则。

---
