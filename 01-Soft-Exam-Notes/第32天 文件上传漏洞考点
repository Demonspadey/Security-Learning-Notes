## 一、核心目标
1.  熟记文件上传漏洞的核心考点与标准简答模板
2.  掌握漏洞成因、分类、危害与防护措施的标准表述
3.  规范文件上传漏洞相关选择题、判断题、案例题的作答思路

---

## 二、必背考点卡片
- **漏洞名称**：文件上传漏洞
- **漏洞定义**：上传功能未做安全校验，恶意文件可上传并被服务器解析执行
- **核心原理**：输入验证失效、访问控制缺失、信任用户可控上传参数
- **主要分类**：前端校验绕过型、后端校验绕过型
- **形成原因**：仅前端无后端校验、黑名单规则不安全、未校验文件头与内容、上传目录可执行、文件未重命名未隔离
- **安全危害**：服务器被控、网页篡改、数据泄露、内网渗透、系统权限失控
- **防护措施**：后端白名单校验、检测文件头与恶意内容、随机重命名、上传目录禁解析、前后端双重校验、文件隔离存储

---

## 三、标准简答满分模板
### 题目：简述文件上传漏洞的成因、分类、危害及防护措施
> 标准答案：
> 1. 成因：仅依赖前端校验，未做后端强制安全校验；后端采用黑名单策略，规则不完善易被绕过；未检测文件头与真实文件内容；上传目录未限制脚本执行权限；上传文件未重命名、未隔离存储。
> 2. 分类：分为前端校验绕过型、后端校验绕过型两类。
> 3. 危害：可上传Webshell控制服务器；篡改网站页面，植入木马后门；窃取用户数据与核心业务资料；以服务器为跳板实施内网横向渗透；系统权限失控，业务中断并引发安全责任事件。
> 4. 防护：后端实施文件后缀、MIME类型白名单校验；校验文件幻数，检测文件内恶意代码；上传文件随机重命名，杜绝路径遍历；上传目录禁止脚本解析，配置只读权限；前后端双重校验，文件隔离存储，禁止直接访问执行。

---

## 四、模拟练习题（附你的作答批改）

### 1. 单项选择题
1.  文件上传漏洞产生的最根本原因是（）
    A. 网站没有配置HTTPS
    B. 文件上传功能输入验证失效，服务端未做严格安全校验
    C. 用户上传文件体积过大
    D. 浏览器版本过低
    ✅ 你的答案：B（正确）

2.  仅依靠前端JS做文件类型校验，下列说法正确的是（）
    A. 可以完全防御文件上传漏洞
    B. 抓包修改数据包即可直接绕过，无安全防护作用
    C. 配合密码可达到后端防护效果
    D. 仅能防御大文件上传
    ✅ 你的答案：B（正确）

3.  防御文件上传漏洞最安全、最推荐的校验方式是（）
    A. 前端JS后缀校验
    B. 后端白名单允许合法后缀
    C. 后端黑名单拦截危险后缀
    D. 仅校验文件大小
    ✅ 你的答案：B（正确）

4.  服务器上传目录未禁止脚本解析，最直接会造成（）
    A. 无法上传图片
    B. 恶意脚本文件被服务器解析执行，服务器被控制
    C. 网站访问变慢
    D. 占用服务器磁盘空间
    ✅ 你的答案：B（正确）

5.  下列不属于文件上传漏洞主要危害的是（）
    A. 上传Webshell控制服务器
    B. 网页篡改、敏感数据泄露
    C. 内网横向渗透
    D. 自动修复系统漏洞
    ✅ 你的答案：D（正确）

6.  关于文件名校验与存储防护，正确做法是（）
    A. 保留原文件名直接存放
    B. 上传文件随机重命名、隔离存储，禁止上传目录脚本执行
    C. 所有文件都存放在网站根目录
    D. 只校验文件名，不校验文件内容
    ✅ 你的答案：B（正确）

---

### 2. 判断题
1.  文件上传漏洞属于Web应用层安全漏洞。（✅ 你的答案：对）
2.  后端采用黑名单过滤危险后缀，比白名单更安全可靠。（✅ 你的答案：错）
3.  只做前端校验、不做后端校验，一定会存在文件上传漏洞风险。（✅ 你的答案：对）
4.  校验文件幻数（文件头）可以防止修改后缀伪装的恶意文件。（✅ 你的答案：对）
5.  上传目录设置为可执行权限，能有效提升网站安全性。（✅ 你的答案：错）
6.  文件上传漏洞可被利用植入webshell，实现远程控制服务器。（✅ 你的答案：对）

---

### 3. 场景案例分析题
#### 场景描述
某校园论坛具备头像上传、附件发布功能，安全现状如下：
1. 仅在前端通过JS限制只能上传jpg、png格式，服务端无任何校验；
2. 后端采用黑名单方式拦截asp、php后缀，未做白名单；
3. 不检测文件头、文件内容，仅判断文件名后缀；
4. 上传文件保留原文件名，直接存放在网站可执行目录，未重命名、未隔离；
5. 上传目录默认允许脚本解析执行，无权限限制。

#### 你的满分作答
1.  **网站安全缺陷**：
    1.  依赖黑名单规则，易被绕过；
    2.  仅前端js校验，后端无二次校验；
    3.  不校验文件真实内容、文件头；
    4.  上传目录具备执行权限；
    5.  未重命名上传文件，未隔离存储路径。
    ✅ 批改：5处缺陷全部命中，表述精准贴合场景。

2.  **两类绕过方式**：
    - 前端校验绕过：仅依靠前端JS限制文件类型，抓包篡改数据包即可绕过，无实际防护能力；
    - 后端校验绕过：服务端有校验规则，但黑名单不完善、未校验文件头与内容，可通过后缀伪装、解析漏洞等方式绕过。
    ✅ 批改：定义、原理表述标准，术语规范。

3.  **安全危害**：
    1.  上传webshell，获取服务器控制权；
    2.  篡改网页，植入木马违规内容；
    3.  窃取用户敏感信息；
    4.  以服务器为跳板，实施内网横向渗透；
    5.  系统权限失控。
    ✅ 批改：危害点全覆盖，逻辑递进清晰。

4.  **加固整改措施**：
    1.  严格限制文件上传后缀，采用白名单机制，限制脚本格式；
    2.  服务端二次校验，禁止依赖前端js校验；
    3.  校验文件真实类型、文件头、文件内容；
    4.  上传目录设置无执行权限，隔离存储路径；
    5.  上传文件重命名，随机哈希命名，防止目录遍历与解析漏洞；
    6.  限制单文件大小、上传频率，缓解拒绝服务风险。
    ✅ 批改：6条措施全面覆盖采分点，逻辑严谨，符合软考标准。
