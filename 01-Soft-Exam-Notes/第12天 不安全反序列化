## 一、软考核心考点（教材 22.4 节）

### 1. 核心概念
- **序列化**  
  将对象、复合数据转化为字符串、字节流、文本格式，用于网络传输、Cookie存储、缓存、会话保存。
- **反序列化**  
  将外部接收的字符串/字节数据，还原为程序可直接调用的对象。
- **漏洞核心成因**  
  用户可控输入（Cookie、参数、请求头）**未经校验、无白名单、无签名**，直接在后端进行反序列化。

### 2. 常考风险（多选必背）
- 远程代码执行、系统命令执行
- 任意文件读取/写入
- 权限绕过、身份伪造
- 拒绝服务攻击（DoS）
- 信息泄露

### 3. 高频简答题模板
**问题：简述不安全反序列化漏洞的防护措施**
1. 优先采用 JSON、XML 等安全纯数据格式，避免使用危险序列化格式。
2. 对反序列化数据做白名单校验，限制可加载的类与数据类型。
3. 对序列化数据增加数字签名或加密，防止数据篡改。
4. 禁用危险魔术方法，限制第三方危险依赖组件。
5. 最小权限运行服务，降低漏洞利用后的危害范围。

### 4. 易混考点辨析
| 类型 | 特征标识 | 触发方式 |
| :--- | :--- | :--- |
| PHP 反序列化 | 以 `O:` 开头（格式：`O:类长度:"类名":属性数:{内容}`） | 依赖魔术方法触发 |
| Java 反序列化 | 大量 Base64 编码的长串数据 | 依赖第三方组件漏洞链 |

---
