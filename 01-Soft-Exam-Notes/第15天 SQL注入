## 软考学习
### 对应官方教材章节
《信息安全工程师教程》Web安全风险、软件安全防护章节

### 一、高频选择题核心考点
1. **SQL注入定义**
攻击者利用Web应用对用户输入过滤不严的缺陷，将恶意SQL语句插入用户可控参数中，拼接进后台原生SQL语句并执行，从而操纵数据库的攻击方式。

2. **SQL注入产生两大必要条件（必考）**
- ① 用户输入可控，参数可被篡改
- ② 后端直接拼接用户输入到SQL语句，无过滤、无转义、无预编译

3. **基础分类考点**
- 按数据类型：数字型注入、字符型注入（最常考）
- 按请求方式：GET注入（参数在URL）、POST注入（请求体传参）

4. **关键识别特征**
页面报错、内容异常回显、单引号触发报错、逻辑判断（and/or）页面变化。

### 二、SQL注入标准危害（选择+简答通用）
1. 非法查询、泄露数据库敏感数据，造成信息泄露
2. 篡改、删除业务数据，破坏数据完整性
3. 绕过登录验证，越权访问后台管理系统
4. 批量脱库、获取全员数据
5. 结合数据库高权限，横向渗透、控制服务器

### 三、下午案例简答 | 标准背诵模板
#### 问题1：简述SQL注入产生的原因
1. Web应用未对用户提交的参数做严格过滤、转义与合法性校验；
2. 后端采用字符串拼接方式构造原生SQL语句；
3. 未使用预编译、参数化查询等安全编码方案；
4. 数据库权限配置过大，Web业务账户拥有多余操作权限。

#### 问题2：简述防范SQL注入的核心措施（软考标准答案）
1. 优先使用预编译语句+参数化查询，杜绝SQL字符串拼接；
2. 对所有用户可控输入进行过滤、转义、特殊字符拦截；
3. 采用白名单机制校验输入格式，限制非法字符与关键字；
4. 配置数据库最小权限原则，缩减业务账号操作范围；
5. 部署WAF设备/安全防护规则，拦截注入特征载荷；
6. 关闭数据库错误回显，避免泄露库表结构信息。

### 四、易混淆考点辨析（复盘专用）
1. 文件上传漏洞：核心是上传恶意文件执行代码
2. 目录遍历漏洞：核心是跨目录读取系统敏感文件
3. SQL注入漏洞：核心是篡改数据库查询语句

三者成因不同、防护手段不同，案例分析高频区分考察。

### 五、今日软考小测验（真假判断）
1. 仅依靠前端JS过滤，可以防御SQL注入。（×）
2. 参数化预编译是防御SQL注入最有效的核心手段。（√）
3. 字符型注入通常依靠单引号实现语句闭合逃逸。（√）
