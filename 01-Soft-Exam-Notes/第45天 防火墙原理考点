## 一、必背理论点
1. 包过滤五元组：源IP、目的IP、源端口、目的端口、协议。
2. 状态检测：跟踪连接状态；应用代理：解析应用层数据。
3. 部署模式：路由模式（支持NAT）、透明模式（桥接）、混合模式。
4. 策略原则：最小开放，默认拒绝。

## 二、简答标准模板
### 模板1 防火墙工作原理+部署模式
1. 工作原理：依据规则过滤数据包。包过滤校验报文头部；状态检测维护连接表；应用代理深度解析应用层数据。
2. 部署模式：路由模式支持NAT，用于网络边界；透明模式为网桥，不改变网络拓扑；混合模式兼具两者能力。

### 模板2 三类防火墙区别
包过滤基于五元组，工作在网络层、传输层；状态检测增加连接状态跟踪；应用代理工作在应用层，防护能力最强。三者检测深度、防护能力依次提升。

### 模板3 无法防御Web攻击原因及方案
包过滤、状态检测工作在网络/传输层，无法解析应用层数据，不能识别SQL注入。解决方案：部署WAF，开启应用代理功能。

## 三、全套习题及作答记录
### （一）单选题 20题
作答：1.B 2.A 3.B 4.C 5.D 6.B 7.B 8.B 9.A 10.A
11.A 12.B 13.B 14.A 15.B 16.C 17.B 18.B 19.B 20.C
结果：全对

### （二）判断题 15题
作答：1.对 2.对 3.错 4.对 5.错 6.对 7.对 8.对 9.错 10.对
11.错 12.对 13.对 14.对 15.对
结果：全对

### （三）巩固单选10题 + 判断8题
结果：全对

## 四、综合案例题（参考答案+批改）
### 案例1 端口拦截
命令修正：`sudo iptables -A INPUT -s 110.20.30.40 --dport 3389 -j DROP`
规则含义：拒绝源IP 110.20.30.40 访问本机3389端口。

### 案例2 透明模式防护短板
原因：仅包过滤/状态检测，工作在底层网络，无法解析应用层攻击。
方案：开启应用代理、部署WAF、配置应用层策略。

### 案例3 出口NAT场景
选用路由模式，理由：支持NAT地址转换。
配置思路：默认拒绝外网入站，按需放行端口与IP，对内网流量做NAT转换。

### 案例4 规则顺序故障
原因：规则自上而下匹配，网段放行在前，单IP拦截失效。
方案：调整顺序，拦截规则放置在放行规则之前。

## 五、考前速记易错点
1. iptables 动作：`DROP` 丢弃数据包。
2. 透明模式无需配置IP，路由模式必须配置IP、支持NAT。
3. 规则顺序是高频故障考点，牢记「从上到下，匹配即停」。
4. 底层防火墙 ≠ WAF，分工不同，组合使用实现纵深防御。
