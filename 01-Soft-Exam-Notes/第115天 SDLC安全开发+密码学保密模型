Part 1：真题选择题 20道（40min）

📌 从2023上午真题抽取（新题库），覆盖各章节。答案统一放末尾。

```
1.【2023】密码法规定国家对密码实行分类管理，密码分类中不包含（  ） A.国产密码 B.普通密码 C.核心密码 D.商用密码
2.【2023】A方有一对密钥(KApub,KApri)，B方有一对密钥(KBpub,KBpri)，A方给B方发送信息M，对M加密为 M'=KBpub(KApri(M))。B方收到密文，正确的解决方案是（  ） A.KBpub(KApri(M')) B.KApub(KBpri(M')) C.KBpri(KApri(M')) D.KBpub(KApub(M'))
3.【2023】电子邮件炸弹能使用户在短时间内收到大量邮件，严重时使系统崩溃，该威胁属于（  ） A.非法访问 B.拒绝服务 C.欺骗 D.暴力破解
4.【2023】ISMS审核时不应检查的是（  ） A.人员安全意识培训记录 B.与客户签订的安全保密协议 C.机房出入登记记录 D.员工个人社交软件聊天记录
5.【2023】风险评估中资产识别环节不需要收集的信息是（  ） A.资产所有者、使用者、维护人员 B.资产价值、存放位置、用途 C.资产存在的漏洞、脆弱点 D.资产类别、重要程度
6.【2023】从实现层次角度，安全控制粒度最细的防火墙是（  ） A.包过滤防火墙 B.状态检测防火墙 C.应用代理防火墙 D.三者一致
7.【2023】不属于信息安全法律法规作用的是（  ） A.规范网络主体行为 B.惩罚网络违法犯罪 C.完全消除网络安全风险 D.保障网络空间秩序
8.【2023】Windows系统安全日志默认存放路径是（  ） A.C:\Windows\System32\winevt\Logs B.C:\Windows\Logs\security C.C:\Windows\System\log D.C:\Program Files\log
9.【2023】下列密码算法中，属于哈希算法的是（  ） A.AES B.RSA C.SHA-256 D.ECC
10.【2023】基于角色的访问控制(RBAC)核心是（  ） A.用户直接绑定权限 B.权限绑定角色，用户分配角色 C.按安全等级划分访问权限 D.按访问列表控制资源
11.【2023】SQL注入攻击主要破坏信息安全的（  ） A.保密性 B.完整性 C.可用性 D.可控性
12.【2023】不属于僵尸网络传播过程常见方式的是（  ） A.主动攻击漏洞 B.邮件病毒 C.恶意网站脚本 D.字典攻击
13.【2023】属于宏病毒的是（  ） A.Worm.Blaster.g B.Macro.Melissa C.Backdoor.Agobot.frt D.Trojan.huigezi.a
14.【2023】防火墙不能阻止（  ） A.内部威胁和病毒威胁 B.外部攻击和外部威胁 C.外部攻击 D.外部攻击、外部威胁和病毒威胁
15.【2023】合法许可的用户能够及时获取网络信息或服务的特性，是指信息安全的（  ） A.机密性 B.完整性 C.可控性 D.可用性
16.【2023】为防止Web页面恶意代码损害，可以采取的防范措施是（  ） A.在浏览器中安装数字证书 B.利用IP安全协议访问Web C.利用SSL访问Web D.将要访问的Web站点按可信度分配到浏览器不同安全区域
17.【2023】关于利用多媒体数据隐藏机密信息，叙述错误的是（  ） A.信息嵌入不影响多媒体本身的传送和使用 B.多媒体信息本身编码效率很高 C.人眼/人耳对某些信息有掩蔽效应 D.多媒体信息本身有很大冗余性
18.【2023】无线局域网产品须使用的系列密码算法中，密钥协商算法应使用的是（  ） A.ECDSA B.DH C.CPK D.ECDH
19.【2023】一个全局的安全框架必须包含的安全结构因素是（  ） A.审计、完整性、身份认证、保密性 B.审计、完整性、身份认证、保密性、可用性、真实性 C.审计、完整性、身份认证、可用性 D.审计、完整性、保密性、可用性
20.【2023】Kerberos系统涉及四个基本实体，其中为用户提供服务的设备或系统被称为（  ） A.Kerberos客户机 B.应用服务器 C.票据发放服务器TGS D.认证服务器AS

答案：1A 2B 3B 4D 5C 6C 7C 8A 9C 10B 11B 12D 13B 14A 15D 16D 17B 18D 19B 20B
```

---

### 🔴 Part 2：错题重做 + 轮回卡79天（40min）

📌 各年份错题库反复错的重做 + 轮回卡快速过：
```
· 2022年：Q35 网络层VPN隧道技术 / Q39 CIDF事件分析器
· 2021年：Q60 网闸读写交替 / Q74 audit定期审查
· 2023年新错题（做完今天20道若有错，记入错题库）
· 轮回卡79天快速翻一遍，重点易混点
```

---

# 🌤️ 下午（2.5h）：SDLC安全开发 + 密码学保密模型 案例

---

### 🔴 Part 1：案例① SDLC安全开发（2019Q5 完整真题，35min）

📌 纸笔计时30min，完整手写（第1遍）。重点：弱口令/隐私保护/缓冲区溢出绕过。

```
【2019年 下午案例 第5题 · 完整真题】

【说明】信息系统安全开发生命周期(Security Development Life Cycle(SDLC))是微软提出的从安全角度
指导软件开发过程的管理模式，它将安全纳入信息系统开发生命周期的所有阶段。

问题1.（4分）在培训阶段，需要对员工进行安全意识培训，要求员工向弱口令说不！针对弱口令最有效的
攻击方式是什么？以下口令中，密码强度最高的是（  ）。
  A. security2019  B. 2019Security  C. Security@2019  D. Security2019

问题2.（6分）在大数据时代，个人数据正被动地被企业搜集并利用。在需求分析阶段，需要考虑采用隐私
保护技术防止隐私泄露。从数据挖掘的角度，隐私保护技术主要有：基于数据失真的隐私保护技术、
基于数据加密的隐私保护技术、基于数据匿名的隐私保护技术。
请问以下隐私保护技术分别属于上述三种隐私保护技术的哪一种？
  (1) 随机化过程修改敏感数据
  (2) 基于泛化的隐私保护技术
  (3) 安全多方计算隐私保护技术

问题3.（4分）有下述口令验证代码：
  int verify_password(char *password){
    char buffer[8];
    int authenticated = 0;
    strcpy(buffer, password);     // 复制用户输入到8字节缓冲区（无边界检查！）
    if(strcmp(buffer, "secret") == 0)
      authenticated = 1;
    return authenticated;         // 返回给main的valid_flag
  }
  // main中：若 valid_flag == 0 → 绕过口令验证
  【图5.2 堆栈布局】栈上：buffer[8] 在低地址，authenticated 在相邻高地址
  其中main函数在调用verify_password函数进行口令验证时，堆栈的布局如图5.2所示。
  请问调用verify_password函数的参数满足什么条件，就可以在不知道真实口令的情况下绕过口令验证功能？

问题4.（3分）SDLC安全开发模型的实现阶段给出了3种可以采取的安全措施，请结合问题3的代码举例说明？

【参考答案】
问题1：最有效的攻击方式=穷举攻击（暴力破解/字典攻击）⭐；密码强度最高=C(Security@2019)⭐
  （包含大小写+数字+特殊符号@，4类字符全有；其他选项缺特殊符号）
问题2：(1)随机化修改敏感数据→基于数据失真的隐私保护技术 ⭐
  (2)基于泛化→基于数据匿名的隐私保护技术 ⭐
  (3)安全多方计算→基于数据加密的隐私保护技术 ⭐
问题3：参数password满足：长度≥12个字符，前8个任意字符填满buffer，
  第9-12位为空字符(\0) ⭐
  原理：strcpy把前8字符写入buffer后，第9位的\0溢出覆盖相邻authenticated变量；
  空字符ASCII=0 → authenticated被覆盖为0 → 返回valid_flag=0 → 绕过验证 ⭐
  （补充：恰好输入8个任意字符，strcpy自动追加的字符串结束符\0也能覆盖authenticated为0）⭐
问题4：①使用批准的工具编写安全代码（如用安全的字符串函数）⭐
  ②禁用不安全函数：禁用C语言中有隐患的函数（如strcpy→strncpy）⭐
  ③静态分析：检测程序指针的完整性 ⭐
```

**✏️ 细致追问（口答）**：
```
① 为什么密码强度C最高？→ 大小写+数字+特殊符号都含（Security@2019）；A/B/D缺特殊符号
② 弱口令怎么防？→ 复杂度策略+长度+定期更换+账户锁定
③ 缓冲区溢出防范？→ strncpy替代strcpy、边界检查、/GS栈保护、静态分析
④ 和2018Q4心跳包、2023Q2缓冲区的联系？→ 都是"未校验长度/边界"→溢出
```

---

### 🔴 Part 2：案例② 密码学保密通信模型（2023Q3 第3遍，用2018Q2同题练，35min）

📌 纸笔计时30min，完整手写（第3遍）。注意：2018Q2=2023Q3同题，图完全一致。

```
【2018年/2023年 下午案例 同题 · 完整真题（第3遍）】

【说明】密码学的基本目标是在有攻击者存在的环境下，保证通信双方(A和B)之间能够使用不安全的通信
信道实现安全通信。密码技术能够实现信息的保密性、完整性、可用性和不可否认性等安全目标。
符号定义：M=消息，H=Hash函数，E=加密算法，D=解密算法，K=密钥，SKA=私钥，PKA=公钥，||=连接操作。

【图2-1 保密通信模型一】
用户A侧：M → H → H(M)；M与H(M)拼接 → E(K加密) → 输出 M||E_K[H(M)]
传输链路 → 用户B侧：D(K解密) 拆分出M、H(M)；M→H算H(M)'；对比H(M)与H(M)'

问题1.（6分）图2-1所设计的保密通信模型能实现信息的哪些安全目标？
  图2-1中的用户A侧的H和E能否互换计算顺序？如果不能互换请说明原因。

【图2-2 保密通信模型二】
用户A侧：M → H → H(M)；M||H(M) → E(K)整体加密；传输 E_K[M||H(M)] → B
用户B：D(K)解密得到M、H(M)；M重新计算H(M)'，比对校验

问题2.（4分）图2-2设计的保密通信模型能实现信息安全的哪些特性？

【图2-3 保密通信模型三（支持不可否认）】
用户A：M → H → H(M)；H(M) → E(SKA私钥签名)；M||签名密文 → E(K对称加密) → 传输(1)______
用户B：接收后D(K)解密，拆分M、签名密文；签名密文 → D(2)______ 验证签名得到原始哈希；本地计算M的哈希比对

问题3.（5分）图2-3中(1)、(2)分别应该填什么内容？

【参考答案】
问题1：能实现 完整性（对M哈希+加密哈希，B解密比对校验M是否被篡改）+ 对哈希的机密保护
  （E_K加密哈希，防攻击者篡改M后重算H(M)绕过校验）⭐
  ⚠️ M明文传输→无M机密性；"哈希保密性"保护的是哈希值，是完整性的支撑
  H和E不能互换：先加密再哈希只能验证密文的真实性，不能验证明文的真实性，
  攻击者篡改明文后接收方无法察觉 ⭐
问题2：机密性（整体加密，M和H(M)一起加密）+ 完整性（哈希校验）⭐
  无抗抵赖（没有签名）
问题3：(1)=E_K[M||E_SKA[H(M)]]  ⭐（M拼接签名密文后整体对称加密的传输报文）
  (2)=PKA  ⭐（题干"D(2)____"D已在括号外，只填密钥PKA）
  完整式子：传输E_K[M||E_SKA[H(M)]]；验签 D_PKA(E_SKA[H(M)])=H(M)
```

**✏️ 细致追问（口答）**：
```
① 三模型目标速记？→ 模型一=完整+哈希机密；模型二=机密+完整；模型三=机密+完整+不可否认
② 模型三为什么能抗抵赖？→ 私钥签名只有A能做，A无法否认
③ 这道题你错过的点？→ (1)别漏M和外层加密；(2)只填PKA；(3)H和E不能互换
```

---
