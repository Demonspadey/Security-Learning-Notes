## 一、核心目标
1.  熟记XSS/CSRF核心考点与标准简答模板
2.  规范两类漏洞对比题、案例题的作答思路
3.  扫清易混点，确保考试无概念混淆

---

## 二、必背考点卡片
1.  XSS三类核心：反射型、存储型、DOM型，核心区别在持久化与后端交互
2.  XSS最通用防护：**输出层HTML实体编码转义**
3.  CSRF核心利用点：浏览器**自动携带同源会话Cookie**
4.  CSRF首选防护：**CSRF Token 随机校验**
5.  同源策略三要素：协议、域名、端口必须完全一致
6.  XSS与CSRF是软考高频对比简答题，必须熟记差异

---

## 三、标准简答模板（可直接默写）
### 题目1：简述三类XSS漏洞的区别
> 标准答案：
> 1. 反射型XSS：非持久化，Payload放在URL参数中，单次访问触发，仅危害当前访问用户；
> 2. 存储型XSS：持久化存入服务端数据库，所有访问页面用户都会触发，影响范围最大；
> 3. DOM型XSS：纯前端JS解析触发，不经过后端服务器处理，隐蔽性高，容易被防护忽略。

### 题目2：简述CSRF漏洞的攻击原理与防御措施
> 标准答案：
> 1. 攻击原理：跨站请求伪造（CSRF）是攻击者利用浏览器自动携带同源会话Cookie的特性，诱导已登录目标站点的用户访问恶意页面，伪造用户身份向目标站点发送未授权请求，冒用用户权限执行违规操作，攻击者无需窃取用户Cookie，仅借助用户已登录状态完成攻击。
> 2. 防御措施：
> - 校验CSRF Token：服务端生成随机、唯一、不可预测的Token，嵌入请求并校验有效性；
> - 配置SameSite Cookie：限制Cookie仅同站携带，阻断跨站自动携带；
> - Referer来源校验：验证请求来源域名，拦截非法跨源请求；
> - 高危操作二次验证：增加验证码、密码二次确认，提升攻击门槛。

### 题目3：简述XSS与CSRF漏洞的核心区别
> 标准答案：
> 1. 漏洞本质不同：XSS是注入恶意脚本在用户浏览器执行，属于代码注入类漏洞；CSRF是伪造用户请求冒用身份操作，属于请求伪造类漏洞。
> 2. 攻击目标不同：XSS攻击目标是用户浏览器，窃取用户信息、修改页面内容；CSRF攻击目标是目标服务器，冒用用户权限执行未授权操作。
> 3. 利用条件不同：XSS需要站点存在输入输出过滤缺陷，能注入并执行脚本；CSRF需要用户已登录目标站点、请求无Token校验、浏览器自动携带Cookie。
> 4. 防护核心不同：XSS防护核心是输入过滤、输出编码、CSP安全策略；CSRF防护核心是Token校验、SameSite Cookie、来源校验。

---

## 四、模拟练习题（附你的作答批改）

### 1. 单项选择题
1.  下列XSS类型中，恶意脚本不经过后端服务器、纯前端JS解析DOM触发、抓包无明显恶意特征的是（）
    A. 反射型XSS
    B. 存储型XSS
    C. DOM型XSS
    D. 盲注型XSS
    ✅ 你的答案：C（正确）

2.  恶意Payload提交后存入数据库，所有访问该页面的用户均会触发攻击，危害范围最大的XSS类型是（）
    A. 反射型XSS
    B. 存储型XSS
    C. DOM型XSS
    D. 本地型XSS
    ✅ 你的答案：B（正确）

3.  浏览器同源策略的三大判定要素是（）
    A. IP地址、端口、路径
    B. 协议、域名、端口
    C. 协议、IP、参数
    D. 域名、主机、资源类型
    ✅ 你的答案：B（正确）

4.  防御CSRF最主流、最稳定、最推荐的核心防护手段是（）
    A. Referer来源校验
    B. CSRF Token随机校验
    C. 增加登录验证码
    D. 前端输入过滤
    ✅ 你的答案：B（正确）

5.  为Cookie设置HttpOnly属性，主要防御XSS造成的哪种风险（）
    A. 页面篡改
    B. Cookie窃取与会话劫持
    C. 前端弹窗广告
    D. 网页挂马
    ✅ 你的答案：B（正确）

6.  下列关于XSS与CSRF区别说法正确的是（）
    A. XSS是请求伪造漏洞，CSRF是脚本注入漏洞
    B. XSS需要注入并执行脚本，CSRF无需脚本执行
    C. CSRF可以窃取用户数据，XSS只能冒用身份操作
    D. XSS防护核心是Token校验，CSRF防护核心是输出编码
    ✅ 你的答案：B（正确）

---

### 2. 判断题
1.  反射型XSS的Payload存放在URL中，非持久化，一次性触发，需要诱导用户点击链接。（✅ 你的答案：对）
2.  存储型XSS会将恶意脚本写入服务端数据库，持久化生效，危害远大于反射型XSS。（✅ 你的答案：对）
3.  DOM型XSS会在HTTP请求包中留下明显的恶意脚本特征，很容易被WAF检测拦截。（✅ 你的答案：错）
4.  CSRF攻击的核心原理是利用浏览器自动携带同源会话Cookie的特性，冒用用户身份执行操作。（✅ 你的答案：对）
5.  仅配置Referer校验就可以完全防御CSRF漏洞，不会被绕过。（✅ 你的答案：错）
6.  输出层HTML实体编码转义，是防御XSS最基础、最通用、最核心的防护手段。（✅ 你的答案：对）

---

### 3. 场景案例分析题
#### 场景描述
某新闻网站存在以下安全问题：
1. 文章评论区允许用户提交任意内容，数据直接存入数据库，前端直接渲染展示，未做任何过滤与转义；
2. 用户登录后修改密码功能仅校验会话Cookie，未加入任何Token校验，未配置SameSite属性，未校验请求来源；
3. 网站搜索功能将用户输入的关键词直接拼接在URL中，后端不做处理直接回显到页面；
4. 部分页面通过JS读取URL哈希参数，直接拼接innerHTML渲染页面内容。

#### 你的满分作答
1.  **漏洞分类匹配**：
    - 场景1：存储型XSS漏洞
    - 场景2：CSRF跨站请求伪造漏洞
    - 场景3：反射型XSS漏洞
    - 场景4：DOM型XSS漏洞
    ✅ 批改：分类完全正确，无错漏。

2.  **评论区（存储型XSS）危害与防护**：
    - 危害：恶意脚本持久化存入服务器数据库中，所有访问页面用户都会触发，危害性很大。
    - 防护：输入白名单校验机制，特殊字符转义处理，禁止非法关键字。
    ✅ 批改：危害描述精准，防护措施采分点齐全。

3.  **修改密码接口（CSRF）攻击流程与防护**：
    - 攻击流程：用户已登录目标站点，保持会话cookie，访问恶意页面，伪造用户携带同源会话cookie向目标站点请求，校验通过，执行恶意操作。
    - 防护措施：
      1. 设置token校验，用户访问服务器生成随机，唯一，不可预测性的token，嵌入请求参数或请求头中，服务端接收请求后先校验token有效性。
      2. 设置cookie的samesite属性，限制cookie仅在同站请求时携带，阻断跨站请求自动携带cookie。
      3. 同源referer校验，验证请求头中的referer字段，确认请求来源为合法可信域名。
      4. 关键二次校验，转账，修改密码，删除数据等高风险操作，增加短信验证码，登录密码二次验证。
    ✅ 批改：攻击流程逻辑完整，防护措施为标准官方答题模板。

4.  **两类XSS区别**：
    - 搜索功能漏洞是xss反射型漏洞，payload放在url参数中，用户单次点击触发，仅危害当前访问页面用户。
    - js渲染页面属于dom型xss漏洞，仅前端js解析触发，不经过后端服务器处理，隐蔽性高，易被防护忽略。
    ✅ 批改：两类漏洞特征、触发方式、是否过后端、隐蔽性对比全部答到点上。
