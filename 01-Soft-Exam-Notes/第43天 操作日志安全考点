## 一、核心目标
1.  熟记系统日志安全「定义/成因/危害/防护」四要素
2.  掌握日志文件、查询命令、关键字的高频考点
3.  能快速区分文本/二进制日志、认证日志与Web日志的边界
4.  吃透日志安全、暴力破解相关选择题与案例题

---

## 二、背诵考点（四要素模板）
### 系统日志安全 标准四要素
1.  **定义**
系统日志是记录主机登录、远程访问、操作行为、服务运行、身份认证的文件集合，日志审计是安全运维、入侵溯源的核心手段。

2.  **成因**
服务器使用弱口令、对外开放远程SSH服务；系统未开启完整日志记录；日志未定期留存与异地备份；日志文件权限管控宽松，易被篡改。

3.  **危害**
攻击者通过暴力破解非法登录主机，窃取敏感数据、植入恶意程序、篡改系统配置；日志被篡改/删除后，安全事件无法溯源追责。

4.  **防护**
设置高强度账号密码；限制远程登录IP范围；开启全量日志记录；定期开展日志审计；日志异地多份备份；收紧日志权限，禁止普通用户篡改。

---

## 三、高频固定考点速记
1.  等保规范：安全日志最低留存时长 **180天**
2.  登录失败关键字：`Failed password`
3.  登录成功关键字：`Accepted password`
4.  SSH登录失败筛选组合：`sshd.*Failed password`
5.  日志安全三核心：权限收紧、定期审计、异地备份
6.  日志与命令绑定：
    - `last` ↔ `/var/log/wtmp`（历史登录/重启）
    - `who`/`w` ↔ `/var/run/utmp`（当前在线用户）
    - `cat`/`grep` ↔ `/var/log/auth.log`（认证明细）

---

## 四、全套练习题+作答批改复盘
### （一）基础选择题（20题）
> 错题集中在「日志与命令绑定」，已完成复盘补漏，全部考点掌握。

### （二）强化选择题（25题）
> 满分作答，日志分类、文本/二进制区分考点完全吃透。

### （三）拔高选择题（20题）
> 满分作答，细节考点（如`last`命令可查系统重启、二进制日志支持复制备份）已补全。

### （四）判断题（37题）
> 仅1道细节错题，已修正概念：二进制日志可通过复制文件完成备份，不支持文本编辑筛选。

---

## 五、易错点复盘（考场必避坑）
1.  `auth.log` 仅记录**认证行为**，不记录SQL注入、XSS、文件包含等Web攻击
2.  `last` 可同时查看历史登录与系统重启记录，无法查看当前在线用户
3.  二进制日志（utmp/wtmp）不支持`cat`/`grep`/`vi`，但支持`cp`/`mv`等基础文件操作
4.  日志审计是等保硬性要求，不可仅靠强密码替代
