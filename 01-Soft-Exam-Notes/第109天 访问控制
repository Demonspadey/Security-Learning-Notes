Part 1：案例① 访问控制DAC/MAC（2023Q1 完整真题，25min）

📌 纸笔计时20min，完整手写（0遍→完整手写），对照答案。

```
【2023年 下午案例 第1题 · 完整真题】

【说明】访问控制是保障信息系统安全的主要策略之一，其主要任务是保证系统
资源不被非法使用和非常规访问。访问控制规定了主体对客体访问的限制，并在
身份认证的基础上，对用户提出的资源访问请求加以控制。当前，主要的访问控制
模型包括：自主访问控制(DAC)模型和强制访问控制(MAC)模型。

问题1. 针对信息系统的访问控制包含哪三个基本要素？

【参考答案】
问题1：主体、客体、访问控制策略（授权规则）⭐
  · 主体=发起访问者（用户/进程）
  · 客体=被访问资源（文件/数据/设备）
  · 访问控制策略=允许/拒绝的规则
```

**✏️ 细致追问（口答，拓展）**：
```
① DAC vs MAC 核心区别？
   → DAC=所有者自主决定权限(ACL/能力表/矩阵)；MAC=系统强制策略(BLP/Biba)
② BLP/Biba 方向？
   → BLP保密=不上读+不下写；Biba完整=不下读+不上写（相反）
③ 三种实现方式行列？
   → 能力表(主体为中心)/ACL(客体，行=主体⭐)/矩阵(行列交叉)
④ 权限最小化原则？→ 只给完成任务所需最小权限
```

---

### 🔴 Part 2：案例② 缓冲区溢出gets（2023Q2 完整真题，30min）

📌 纸笔计时25min，完整手写（第2遍），对照答案。

```
【2023年 下午案例 第2题 · 完整真题】

【说明】某一本地口令验证函数(C语言环境，X86_32指令集)包含如下关键代码；
某用户的口令保存在字符数组origPassword中，用户输入的口令保存在字符数组
userPassword中，如果两个数组中的内容相同则允许进入系统。
char origPassword[12] = "Secret";
char userPassword[12];
gets(userPassword);
if(strncmp(origPassword, userPassword, 12) != 0) { exit(-1); }

问题1. 用户在调用gets()函数时输入什么样式的字符串，可以在不知道原始口令
"Secret"的情况下绕过该口令验证函数的限制？

【参考答案】
问题1：输入长度为24的字符串，前12个字符和后12个字符一样 ⭐
  解析：栈上origPassword和userPassword相邻，gets()不检查边界
  → 输入超过12字节溢出覆盖origPassword
  → 前12覆盖userPassword、后12覆盖origPassword，strncmp比较12字节相等
  → 绕过验证
```

**✏️ 细致追问（口答）**：
```
① gets()为什么不安全？→ 不检查缓冲区长度，可溢出
② 安全替代？→ fgets()（C11已移除gets）
③ 栈溢出原理？→ 数组向高地址覆盖相邻变量
④ 缓解措施？→ /GS栈cookie/ASLR/DEP(NX)/strncpy替代strcpy
⑤ 和2020Q5(verify_password)区别？
   → 2020覆盖int变量(authenticated=0)；2023覆盖相邻字符串数组
```

---
