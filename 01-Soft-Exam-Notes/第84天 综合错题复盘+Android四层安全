## 一、高频薄弱考点背诵卡片
### 卡片1：蠕虫&漏洞对应（单选/填空题必考）
1. Blaster冲击波 → RPC DCOM漏洞
2. Slammer蠕虫 → MS SQL缓冲区溢出漏洞
3. Wannacry勒索病毒 → SMB永恒之蓝漏洞
4. Morris蠕虫 → 早期多漏洞复合传播蠕虫
5. Stuxnet震网 → 工控系统专用恶意蠕虫

### 卡片2：恶意代码分类简答题背诵
1. 宏病毒：依附Office文档，依靠宏脚本执行触发破坏行为
2. 文件型病毒：感染EXE、COM、SYS系统可执行文件
3. 蠕虫：利用网络漏洞自主传播，无需依赖宿主文件
4. 木马：伪装正常程序，实现远程控制、用户信息窃取
5. 僵尸网络：大量受控主机组成集群，统一接收攻击者下发指令
6. 逻辑炸弹：满足预设条件后触发数据破坏，无自我复制特性

### 卡片3：访问控制易混考点
1. 最小特权：分配完成本职工作所需的最低权限
2. 职责分离：关键业务操作拆分多人执行，防范单人作恶
3. 白名单：默认拒绝全部流量，仅放行指定对象，安全等级更高
4. 黑名单：默认放行全部流量，仅拦截已知恶意对象
5. 通配符掩码规则：0匹配对应比特，255忽略对应比特

### 卡片4：分组加密模式区分（选择题高频）
1. ECB：无反馈模式，安全性最差，考试选项中优先排除
2. CBC、PCBC：明文先异或再加密
3. CFB：先加密初始化向量，再与明文异或输出密文

### 卡片5：系统安全考点
1. Syslog日志标准四要素：时间、主机、进程、日志内容
2. GB17859五级安全操作系统：具备自动故障恢复功能

## 二、PKI核心考点（2022下午案例大题）
### 1. CA与RA职能区分（选择题必考点）
1. CA（认证中心）：体系核心机构，签发、备份、吊销数字证书；**不负责加密业务数据**
2. RA（注册中心）：辅助审核机构，核验用户身份信息，提交证书申请至CA

### 2. 数字信封+数字签名 流程默写模板（案例题标准答案）
发送方标准步骤：
1. 对明文做哈希运算生成消息摘要
2. 使用发送方私钥加密摘要，生成数字签名
3. 拼接明文与数字签名内容
4. 生成随机会话密钥，对称加密拼接后的完整数据
5. 使用接收方公钥加密会话密钥（生成数字信封）
6. 发送密文 + 加密后的会话密钥

接收方标准步骤：
1. 使用自身私钥解密数字信封，获取对称会话密钥
2. 会话密钥解密密文，拆分还原明文、数字签名
3. 使用发送方公钥解密签名，提取原始消息摘要
4. 本地对明文重新哈希，比对两份摘要是否一致

### 3. 三大安全特性简答背诵
1. 机密性：依靠会话密钥对称加密实现
2. 完整性：通过哈希摘要比对校验明文未篡改
3. 不可否认性：发送方私钥唯一签名，身份无法抵赖

### 4. 考试易错选项总结
1. 签名仅使用**发送方私钥**，验签仅使用**发送方公钥**
2. 数字信封加密会话密钥，仅使用**接收方公钥**
3. 先加密后签名缺陷：无法验证密文来源，丢失不可否认安全特性
4. 证书吊销专用文件：CRL（单选固定标准答案）

## 三、Android四层安全体系（2021下午案例考点）
自上而下四层固定结构，层级与安全机制一一对应（要求完整默写）
1. 应用程序层：APP签名校验，校验程序完整性，防止程序被篡改
2. 应用框架层：权限声明机制，AndroidManifest.xml定义权限，管控资源访问权限
3. 系统运行库层：安全沙箱隔离，各APP独立进程运行，跨应用数据隔离
4. Linux内核层：ASLR地址空间随机化，防御缓冲区溢出类内存攻击

## 四、SRC漏洞报告答题模板（案例写作题）
三段标准答题结构，踩分点完整：
1. 漏洞描述：写明漏洞类型、风险等级、受影响业务系统
2. 复现步骤：分步写出完整、可落地操作复现流程
3. 危害证明：说明漏洞可造成的数据泄露、服务器控制、业务中断等安全风险

## 五、今日注册表选择题考点
1. `HKLM\SOFTWARE\Microsoft\Cryptography`：Windows系统加密、PKI密钥全局配置
2. `HKLM\SOFTWARE\Microsoft\SystemCertificates`：系统根证书存储目录，存放可信CA证书
3. `HKCU\Software\Microsoft\Windows\CurrentVersion\Internet Settings`：浏览器SSL/TLS证书验证策略配置

## 六、Wireshark过滤表达式选择题考点
1. `tls.handshake.type == 1`：匹配TLS Client Hello握手包，用于HTTPS握手流程分析
2. `dns.qry.type == 1`：匹配DNS A记录域名解析请求，用于域名流量异常排查

## 七、轮回速记背诵卡
1. Day24 ACL：标准ACL仅匹配源IP，扩展ACL匹配五元组完整流量信息
2. Day21 RADIUS：UDP协议传输，认证端口1812，计费端口1813
