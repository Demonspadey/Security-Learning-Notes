# Day5 软考笔记：同源策略与CSRF漏洞

---

## 一、同源策略（Same-Origin Policy）
### 1. 核心定义
- **同源判定标准**：协议、域名、端口三者必须**完全一致**，缺一不可，否则视为跨域。
- 例：`https://a.test.com:443/index.html`
  - 同源：`https://a.test.com:443/login.html`
  - 跨域：`http://a.test.com`（协议不同）、`https://b.test.com`（域名不同）、`https://a.test.com:8080`（端口不同）

### 2. 安全作用
- 限制跨域读取Cookie、LocalStorage、DOM对象
- 限制跨域AJAX请求获取响应数据
- **高频易错点**：同源策略**不阻止跨域发送请求**，仅阻止读取跨域响应结果。

---

## 二、CSRF（跨站请求伪造）漏洞
### 1. 漏洞本质
- 利用**浏览器自动携带目标站点Cookie**的特性，伪造用户身份执行敏感操作。
- 核心逻辑：利用服务器对用户Cookie的信任，而非直接窃取Cookie。

### 2. 漏洞形成条件（多选必背）
1. 用户已登录目标网站，会话Cookie有效且未过期
2. 浏览器会自动携带目标站点Cookie发起跨域请求
3. 目标站点敏感接口仅依赖Cookie验证身份，无额外校验
4. 存在可被伪造的敏感操作接口（改密、转账、修改资料等）

### 3. CSRF与XSS的核心区别（对比题必背）
| 对比维度 | CSRF（跨站请求伪造） | XSS（跨站脚本） |
| :--- | :--- | :--- |
| 攻击原理 | 伪造用户请求，利用服务器对Cookie的信任 | 注入恶意脚本，利用浏览器对网站的信任 |
| 攻击目标 | 冒充用户执行权限内操作 | 窃取用户Cookie、会话、敏感数据 |
| 利用前提 | 用户必须登录目标站点，Cookie有效 | 无需用户登录，访问恶意页面即可触发 |
| 防护核心 | Token校验、Referer校验、SameSite Cookie | 输入过滤/转义、CSP策略、HttpOnly Cookie |

### 4. CSRF防御方案（大题必写）
1. **CSRF Token校验**
   - 服务器为每个用户会话生成随机唯一Token，绑定会话
   - Token嵌入表单隐藏域/请求头，**禁止存放在Cookie中**
   - 服务器校验请求携带的Token与会话中的Token是否一致

2. **Referer白名单校验**
   - 校验请求头Referer是否为本站可信域名
   - 局限性：用户可通过浏览器设置禁用Referer，导致合法请求被拦截

3. **Cookie SameSite属性**
   - `SameSite=Strict`：完全禁止跨域请求携带Cookie
   - `SameSite=Lax`：仅允许部分安全请求（如GET跳转）携带Cookie
   - 现代浏览器原生防护，降低CSRF风险

4. **敏感操作二次验证**
   - 短信验证码、原密码验证、生物识别等，从根源杜绝伪造请求的有效性

### 5. 高频易错考点
- Token直接存放在Cookie中，无法防御CSRF（跨域请求会自动携带Cookie中的Token）
- Referer请求头无法被攻击者伪造，只能被用户禁用
- HTTPS站点向HTTP站点发起请求时，不会携带Referer头
