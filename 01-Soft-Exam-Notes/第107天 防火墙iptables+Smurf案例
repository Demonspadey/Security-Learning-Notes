Part 1：案例·防火墙iptables（2022Q1 完整真题，40min）

📌 纸笔计时30min，完整手写（第2遍），然后对照答案红笔标采分点。

```
【2022年 下午案例 第1题 · 完整真题】

【说明】某企业网络拓扑结构：互联网接入防火墙1，防火墙1下联交换机1；
交换机1连接防火墙2；防火墙2分别连接交换机2（DMZ区网段192.168.70.0/24，
网站服务器192.168.70.140、数据库192.168.70.141）、交换机3（内网办公网段
192.168.11.0/24，王工主机192.168.11.2）；交换机3后方部署设备X隔离办公网
与生产网。防火墙采用Linux iptables实现访问控制。

拓扑：互联网→防火墙1→交换机1→防火墙2→(交换机2 DMZ / 交换机3 内网→设备X→生产网)

问题1：设备X需要实现两个网络接近物理隔离、仅可控单向数据交换，
该设备名称是什么？

问题2：该双防火墙+DMZ的架构属于哪种防火墙体系结构？

问题3：防火墙1在整体防护体系中属于第几道防线？

问题4：iptables相关问题
  (1) 若INPUT/FORWARD/OUTPUT链默认策略为ACCEPT，该策略属于白名单
      还是黑名单机制？
  (2) iptables四表五链中，控制数据包转发、入站出站过滤的是哪个表？
  (3) 写出设置INPUT链默认全部拒绝的命令。

问题5：配置iptables规则
  (1) 互联网访问DMZ网站服务器，防火墙1需要放行哪两个标准端口？
  (2) 编写规则：允许所有外网访问192.168.70.140的80、443端口，禁止外网
      访问数据库192.168.70.141任何端口。
  (3) 内网网段192.168.11.0/24为/24网段，子网掩码是多少？
  (4) 编写规则：仅允许192.168.11.2通过22端口SSH访问整个DMZ网段
      192.168.70.0/24。

【参考答案】
问题1：网闸（GAP，物理隔离+可控单向数据交换）⭐
问题2：屏蔽子网体系结构（双防火墙夹DMZ）⭐
问题3：第一道防线（防火墙1 = 外网边界第一道）⭐
问题4：(1) 黑名单机制（默认ACCEPT=放行，只封禁特定）⭐
       (2) filter表（控制转发/入站出站过滤）⭐
       (3) iptables -P INPUT DROP ⭐
问题5：(1) 80（HTTP）和443（HTTPS）⭐
       (2) iptables -A FORWARD -d 192.168.70.140 -p tcp --dport 80 -j ACCEPT
           iptables -A FORWARD -d 192.168.70.140 -p tcp --dport 443 -j ACCEPT
           iptables -A FORWARD -d 192.168.70.141 -j DROP ⭐
       (3) 255.255.255.0 ⭐
       (4) iptables -A FORWARD -s 192.168.11.2 -d 192.168.70.0/24 -p tcp --dport 22 -j ACCEPT
           iptables -A FORWARD -d 192.168.70.0/24 -p tcp --dport 22 -j DROP ⭐
```

**✏️ 细致追问（口答）**：
```
① 白名单 vs 黑名单？
   → 白名单=默认拒绝，只放行规则内；黑名单=默认放行，只封禁规则内
② -P 和 -A 的区别？
   → -P=设置链默认策略(policy)；-A=追加一条规则(append)
③ 屏蔽子网 vs 屏蔽主机？
   → 屏蔽子网=双防火墙夹DMZ(最安全)；屏蔽主机=单防火墙+堡垒主机
④ 网闸和防火墙区别？
   → 网闸=物理隔离+单向；防火墙=逻辑隔离+双向
⑤ 为什么只放行80/443？
   → 网站对外只需HTTP/HTTPS，最小暴露原则
```

---

### 🔴 Part 2：案例·防火墙Smurf（2020Q3 完整真题，30min）

📌 纸笔计时25min，完整手写，然后对照答案红笔标采分点。

```
【2020年 下午案例 第3题 · 完整真题】

【说明】防火墙作为网络安全防护的第一道屏障，通常用一系列的规则来实现
网络攻击数据包的过滤。

问题1（3分）图3-1给出了某用户Windows系统下的防火墙操作界面，请写出
Windows下打开以下界面的操作步骤。
附图：Windows Defender防火墙控制面板主界面（左侧菜单：允许应用或功能
通过防火墙、更改通知设置、启用或关闭防火墙、还原默认值、高级设置）

问题2（4分）Smurf拒绝服务攻击结合IP欺骗和ICMP回复方法使大量网络数据包
充斥目标系统。图3-2流量：所有数据包源地址192.168.27.1，目的地址
192.168.27.255，协议ICMP，信息为Echo(ping) request
  (1) 上述攻击针对的目标IP地址是多少？
  (2) 在上述攻击中，受害者将会收到ICMP协议的哪一种数据包？

问题3（2分）如果要在Windows系统中对上述Smurf攻击进行过滤设置，应该在
图3-1中"允许应用或功能通过Windows Defender防火墙"下面的选项中选择哪一项？

问题4（2分）要对入站的ICMP协议数据包设置过滤规则，应选择图3-3的哪个选项？
图3-3规则类型选项：程序(P)、端口(O)、预定义(E)、自定义(C)

问题5（4分）在图3-4的端口和协议设置界面中，请分别给出"协议类型(P)"
"协议号(U)""本地端口(L)""远程端口(R)"的具体设置值。
图3-4界面：协议类型、协议号、本地端口、远程端口、ICMP自定义设置项

【参考答案】⭐权威版（qicoder专业解析确认）
问题1：[开始]→[控制面板]→[系统和安全]→[Windows Defender防火墙]⭐
问题2：(1) 192.168.27.1（受害主机，被攻击的源地址）⭐
       · 源地址192.168.27.1是伪造的受害IP；目的192.168.27.255是广播地址
       · 广播让网内所有主机向源地址192.168.27.1回Echo reply → 淹没它
       (2) Echo reply（回显应答）⭐
问题3：高级设置（进入防火墙高级设置，配置入站规则）⭐
问题4：自定义（ICMP不是程序/端口/预定义，需自定义规则）⭐
问题5：协议类型=ICMPv4；协议号=自动填1；本地端口=不用填/空白；远程端口=不用填/空白 ⭐
       · ICMP工作在网络层，无传输层端口 → 端口信息不需要填写
       · 选ICMPv4，协议号自动填1
```

**✏️ 细致追问（口答）**：
```
① Smurf攻击原理？→ ICMP放大：伪造受害者源IP，向广播地址发Echo request，
   多台主机回Echo reply给受害者，DDoS
② ICMP协议号？→ 1（TCP=6，UDP=17）⭐
③ 为什么用广播地址？→ 放大效应，一次请求多台主机回包
④ 防御Smurf？→ 路由器禁广播转发 + 防火墙过滤ICMP + 边界设备防IP欺骗
⑤ 和泪滴(Teardrop)区别？→ Smurf=ICMP放大DoS；泪滴=IP分片重组漏洞
```

---
