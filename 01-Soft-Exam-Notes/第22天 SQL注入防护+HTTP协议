## 一、核心目标
1.  熟记SQL注入防护的核心考点与分层答题逻辑
2.  掌握HTTP协议、GET/POST请求、WAF等高频考点
3.  规范案例题作答话术，贴合软考阅卷采分标准

---

## 二、精简背诵考点
1.  SQL注入根本原因：用户可控输入未做过滤，直接拼接SQL语句执行。
2.  防御SQL注入最根本手段：预编译参数化、ORM映射。
3.  输入防护原则：白名单优先于黑名单，特殊字符强制转义。
4.  数据库安全三原则：最小权限、禁用高危账户、关闭详细错误回显。
5.  WAF防护属于边界设备层防御，可拦截特征化SQL注入攻击。
6.  GET请求参数暴露在URL中，保密性差；POST请求参数在请求体，相对更安全。
7.  Referer可做简易CSRF校验，但可伪造，不能作为唯一防护手段。

---

## 三、模拟练习题（附你的作答批改）

### 1. 单项选择题
1.  防御SQL注入最根本、最有效的核心手段是（）
    A. 部署WAF设备
    B. 预编译参数化查询、ORM框架
    C. 关闭数据库错误回显
    D. 禁用高危存储过程
    ✅ 你的答案：B（正确）

2.  信息系统防范SQL注入，输入校验应遵循的最优原则是（）
    A. 黑名单过滤
    B. 白名单校验优先
    C. 仅过滤单引号
    D. 不做任何校验
    ✅ 你的答案：B（正确）

3.  下列不属于数据库层防范SQL注入措施的是（）
    A. 账户最小权限配置
    B. 禁用高危存储过程
    C. 统一自定义错误页面
    D. 关闭不必要数据库账户
    ✅ 你的答案：C（正确）

4.  关于HTTP GET与POST请求说法正确的是（）
    A. GET参数放在请求体中，保密性更好
    B. POST参数暴露在URL地址栏
    C. GET参数在URL中明文可见，保密性差
    D. POST有长度限制，GET无限制
    ✅ 你的答案：C（正确）

5.  依靠特征库匹配、在网络边界拦截SQL注入攻击的防护设备是（）
    A. IDS
    B. WAF
    C. 防火墙
    D. 日志审计系统
    ✅ 你的答案：B（正确）

6.  利用Referer字段做安全校验，最大缺陷是（）
    A. 传输速度慢
    B. 可以被客户端伪造
    C. 只能用于GET请求
    D. 占用带宽过高
    ✅ 你的答案：B（正确）

---

### 2. 判断题
1.  SQL注入产生的根本原因是用户可控输入未过滤，直接拼接SQL语句执行。（✅ 你的答案：对）
2.  输入层防护中，黑名单校验安全性高于白名单校验。（✅ 你的答案：错）
3.  应用层关闭数据库详细错误回显，可抑制报错注入信息泄露。（✅ 你的答案：对）
4.  ORM框架底层已做参数化处理，可有效防止SQL注入。（✅ 你的答案：对）
5.  WAF可以拦截所有变形、绕过型SQL注入，无需开发层防护。（✅ 你的答案：错）

---

### 3. 场景案例分析题
#### 场景描述
某企业Web系统开发时，直接将用户输入的查询参数拼接进SQL语句；开发仅简单过滤了单引号，采用黑名单方式拦截关键字；数据库使用最高权限root账户运行业务；系统默认返回数据库原生错误详情，且未部署任何边界安全设备。攻击者利用双引号、内联注释绕过黑名单，构造SQL注入语句，泄露全站数据。

#### 你的作答批改
1.  **漏洞成因**：
    - 服务端对用户输入未严格过滤、转义和合法性校验
    - 后台程序直接采用字符串拼接方式构造SQL语句
    - 未使用参数化查询、预编译等安全编码方式
    - 单纯依赖黑名单，无法完全防御SQL注入，易被大小写、编码、注释绕过
    - 账户操作权限过高，敏感信息对外暴露
    ✅ 批改：全部踩中软考采分点，逻辑完整，表述规范。

2.  **防护方案（五层体系）**：
    - 开发层：采用预编译语句、参数化查询，通过ORM框架编写业务数据，杜绝SQL语句拼接
    - 输入层：采用输入白名单校验机制，对特殊符号进行转义处理，禁止非法关键字符
    - 数据库层：遵循账户最小权限原则，关闭默认账户，禁用高危函数，降低漏洞利用风险
    - 应用层：关闭数据库详细错误回显信息，配置统一错误页面，避免敏感信息暴露
    - 设备层：部署Web应用防火墙(WAF)，拦截注入攻击载荷
    ✅ 批改：分层清晰，完全贴合官方答题模板，可直接写在考卷上。

3.  **安全风险（GET注入场景）**：
    - 非法查询数据库，造成敏感信息泄露
    - 篡改、删除业务数据，破坏数据完整性
    - 绕过身份认证，越权访问后台管理系统
    - 批量窃取全站数据，实现脱库
    - 恶意操作数据库，引发系统瘫痪或服务器被控
