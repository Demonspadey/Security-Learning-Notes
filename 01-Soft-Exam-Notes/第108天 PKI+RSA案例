Part 1：案例① PKI数字信封（2022Q3 完整真题，35min）

📌 纸笔计时30min，完整手写（第2遍），对照答案红笔标采分点。

```
【2022年 下午案例 第3题 · 完整真题】

【说明】某单位搭建PKI体系，CA负责签发证书，RA负责用户信息审核登记；
用户A、B持有公私钥对PKA/SKA、PKB/SKB，数字信封、数字签名结合传输文件。

【字符流程简图（先签名后加密）】
明文M → 哈希 → H(M) → 用SKA签名 → Sign=E_SKA(H(M))
M||Sign → 随机会话密钥K对称加密 → 密文包C=E_K(M||Sign)
K ← 用PKB加密 → 传输：C + E_PKB(K)
接收方B解密：E_PKB(K)→D_SKB得K；C→D_K拆分M、Sign
Sign→D_PKA得H(M)；本地计算M哈希比对

问题1：PKI中CA和RA各自核心职能分别是什么？
问题2：上述流程实现哪三个安全特性？
问题3：如果颠倒顺序，先对称加密整体、再对密文签名，会存在什么缺陷？
问题4：用户需要撤销证书时，CA通过什么文件发布失效证书列表？
问题5：PGP采用什么去中心化信任模型，无统一CA？

【参考答案】
问题1：CA=证书签发/管理（颁发、备份、撤销）；RA=用户注册信息审核登记 ⭐
问题2：机密性(K对称加密) + 完整性(哈希校验) + 不可否认性(SKA私钥签名) ⭐
问题3：先加密整体再对密文签名，缺陷：
  只能验证"密文"完整性，不能验证"明文"完整性
  → 接收方解密后无法确认明文是否被篡改 ⭐
问题4：CRL（证书吊销列表，Certificate Revocation List）⭐
问题5：Web of Trust（信任网/去中心化信任模型）⭐
```

**✏️ 细致追问（口答）**：
```
① 先签名后加密 vs 先加密后签名？
   → 推荐先签名后加密（验证明文完整+不可否认）；先加密后签名只能验密文
② 数字信封 = ？
   → 用接收方公钥加密会话密钥K（机密性）；K加密数据
③ CA和RA区别？→ CA签发/RA审核（签/审分工）
④ CRL vs OCSP？→ CRL=证书吊销列表；OCSP=在线证书状态查询
⑤ 数字签名和数字信封区别？
   → 签名=私钥签/公钥验(完整+抗否认)；信封=公钥加密/私钥解(机密)
```

---

### 🔴 Part 2：案例② RSA密码学（2024Q4 完整真题，30min）

📌 纸笔计时25min，完整手写（口答过，转完整手写），对照答案。

```
【2024年 下午案例 第4题 · 完整真题】

密码学中，根据加密和解密过程所采用密钥的特点可以将密码算法分为两类：
对称密码算法和非对称密码算法。此外，密码技术还用于信息鉴别、数据完整性
检验、数字签名等。

问题1. (3分)密码学的三大安全目标C.I.A分别表示什么？
问题2. (3分)对于RSA密码的参数：p,q,n,φ(n),e,d，哪些参数是可以公开的？
问题3. (2分)如有RSA密码算法的公钥为(55, 3)，请给出对小王的年龄18进行
加密的密文结果。
问题4. (2分)对于RSA密码算法的公钥(55, 3)，请给出对应私钥。

【参考答案】
问题1：C=机密性(Confidentiality)、I=完整性(Integrity)、A=可用性(Availability)
问题2：可公开 n、e；保密 p、q、φ(n)、d ⭐
问题3：c = 18^3 mod 55 = 2 ⭐（18²=324→49；18³=882→2）
问题4：55=5×11，φ(55)=40；d=3⁻¹ mod 40 = 27 → 私钥(55, 27) ⭐
```

**✏️ 细致追问（口答）**：
```
① RSA安全性基于什么难题？→ 大整数因子分解
② 为什么公钥可公开？→ 从(n,e)推d需分解n，计算不可行
③ 消息m限制？→ 0 ≤ m < n
④ 加密用公钥、解密用私钥；签名用私钥、验签用公钥
```

---
