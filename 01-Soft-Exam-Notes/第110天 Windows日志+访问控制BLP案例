Part 1：案例① Windows日志（2022Q4 完整真题，35min）

📌 纸笔计时30min，完整手写（第2遍），对照答案红笔标采分点。

```
【2022年 下午案例 第4题 · 完整真题】

【说明】Windows系统日志分为系统日志、安全日志、应用日志，事件ID 4625
代表登录失败、4624代表登录成功；某服务器持续大量4625审计日志，
Wireshark抓包显示大量访问3389端口流量。

【图 事件查看器界面】
事件查看器( eventvwr.msc )
Windows日志 ├─应用程序 ├─安全【选中，大量4625报错】 └─系统
事件ID：4625  任务类别：登录  来源：Microsoft-Windows-Security-Auditing

问题1：打开Windows事件查看器的运行命令是什么？
问题2：事件ID4625代表哪种登录行为（单选）
  A 本地成功登录  B 远程登录失败  C 远程登录成功  D 本地登录失败
问题3：3389对应什么服务端口？
问题4：写出Wireshark过滤仅显示访问3389流量的显示过滤器；
问题5：针对该暴力登录攻击，写出两条服务器防护措施。

【参考答案】
问题1：eventvwr.msc
问题2：B（远程登录失败）⭐
问题3：远程桌面服务（RDP）⭐
问题4：tcp.port == 3389 ⭐
问题5：①改默认端口 ②防火墙仅允许特定IP访问3389
  （或账户锁定/强密码/NLA网络级认证）⭐
```

**✏️ 细致追问（口答）**：
```
① 4624/4634/4720？→ 登录成功/注销/创建用户
② RDP爆破检测特征？→ 大量4625 + tcp.port==3389
③ 安全日志权限？→ 仅管理员可读
④ iptables封禁攻击者3389？→ iptables -A INPUT -s IP -p tcp --dport 3389 -j DROP
⑤ 和2023Q4(NTA 64994)区别？→ 本次3389 RDP；2023是64994端口
```

---

### 🔴 Part 2：案例② 访问控制BLP（2025Q4 完整真题，30min）

📌 纸笔计时25min，完整手写（第2遍），对照答案。

```
【2025年 下午案例 第4题 · 完整真题】

【说明】访问控制是保障信息系统安全的主要策略之一...当前，主要的访问控制
模型包括：自主访问控制(DAC)模型和强制访问控制(MAC)模型。

问题1. 针对信息系统的访问控制包含哪三个基本要素？
问题2. BLP模型是一种强制访问控制模型，请问：
  (1) BLP模型保证了信息的机密性还是完整性？
  (2) BLP模型采用的访问控制策略是上读下写还是下读上写？
问题3. Linux系统中文件net.txt的权限属性：-rwx------ 1 root root...
  (1) 文件net.txt属于系统的哪个用户？
  (2) 文件net.txt权限的数字表示是什么？

【参考答案】
问题1：主体、客体、访问控制策略 ⭐
问题2：(1) 机密性 ⭐ (2) 下读上写 ⭐（BLP：不上读+不下写）
问题3：(1) root ⭐ (2) 700 ⭐（rwx=7, ---=0, ---=0）
```

**✏️ 细致追问（口答）**：
```
① BLP vs Biba 方向？→ BLP保密=不上读不下写；Biba完整=不下读不上写
② DAC vs MAC？→ DAC所有者自主；MAC系统强制
③ 700 和 755 区别？→ 700仅属主rwx；755属主rwx+组rx+其他rx
④ 访问监控器在哪级？→ 第5级访问验证保护级（GB17859）
```

---
