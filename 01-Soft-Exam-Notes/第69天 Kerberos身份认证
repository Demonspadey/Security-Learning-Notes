## 一、背诵核心卡片（上午选择速记）
### 卡片1 认证基础
1. 认证构成：标识 + 鉴别
2. 三要素：所知、所有、固有；同类要素叠加不算双因素认证
3. 三层逻辑：认证（你是谁）→授权（能做什么）→审计（做了什么）
4. 挑战应答：随机数防重放

### 卡片2 Kerberos必背（案例默写）
1. 四大实体：客户机、AS、TGS、应用服务器；AS+TGS=KDC
2. 流程口诀：AS发TGT，TGS发服务票据
3. 加密规则：TGT外层用户密钥加密，服务票据由目标服务器密钥加密
4. 攻击区分：黄金票据=伪造TGT；白银票据=伪造服务票据
5. 易错坑：Kerberos基于对称加密，无不可否认性

### 卡片3 MAC/HMAC高频坑
1. MAC只能保证完整性、数据源可信；**无不可否认性**
2. 数字签名依托私钥，唯一具备不可否认能力
3. HMAC = 哈希+共享密钥，纯哈希无密钥无法校验发送方身份

### 卡片4 实体认证与S/Key
1. 最优安全方案：双因素认证
2. S/Key一次性口令核心作用：抵御重放攻击
3. IPSec属于网络层安全协议，不属于身份认证协议

## 二、下午案例标准答题模板
### 模板1 Kerberos流程简答题
问：简述Kerberos完整认证流程
答：
1. 用户客户端向AS发送用户名发起认证请求；
2. AS校验用户身份，生成TGT票据，使用用户密码派生密钥加密后返回客户端；
3. 客户端携带TGT向TGS申请访问指定业务服务；
4. TGS校验TGT合法性，生成服务票据，使用目标应用服务器密钥加密返回；
5. 客户端携带服务票据访问应用服务器；
6. 应用服务器解密票据完成双向认证，开放对应服务权限。

### 模板2 黄金票据、白银票据区分
答：
1. 黄金票据：伪造TGT（票据授予票据），可访问域内全部服务，危害极高；
2. 白银票据：伪造服务票据，仅能访问单一指定应用服务。

### 模板3 MAC与数字签名区别简答
答：
1. MAC/HMAC基于双方共享对称密钥，运算速度快，可校验报文完整性与消息来源，但双方均可生成合法校验码，无法实现不可否认；
2. 数字签名基于非对称密码，签名使用发送方独有私钥，验签使用公钥，具备不可否认性，但运算效率更低。

## 三、本次15道刷题错题&陷阱汇总
1. 第3题：认证三要素不含「所行（行为）」，仅所知/所有/固有
2. 第4题：双因素认证必须两类不同要素，密码+指纹才符合；指纹+人脸、密码+PIN属于同类，不满足
3. 第8题：Kerberos使用对称加密，不具备不可否认性，该描述为错误选项
4. 第9题：服务票据使用应用服务器密钥加密，不是用户密码
5. 第12、13题：MAC核心坑点——无不可否认性，对比数字签名必出考题
6. 第14题：S/Key唯一防护目标：重放攻击
7. 第15题：IPSec不属于身份认证协议，是网络层加密协议

## 四、易混对比速记表
1. Kerberos vs PKI证书
Kerberos：对称密钥、KDC票据、短期临时凭证、内网SSO
PKI(X.509)：非对称证书、CA签发、长期身份凭证、互联网通用
2. MAC vs 纯哈希
纯哈希：无密钥，仅本地校验完整性，任何人可伪造匹配哈希
HMAC：带共享密钥，可校验消息发送方身份，防伪造请求
3. S/Key vs 静态口令
静态口令：固定不变，截获可无限重放
S/Key哈希链口令：一次一失效，彻底抵御重放攻击
