## 一、核心背诵卡片（理论+实例+答题话术）
### 卡片1 Snort设备分类
理论：Snort属于NIDS网络入侵检测系统，旁路镜像部署，无直接阻断数据包能力；HIDS为主机型入侵检测，部署在单台主机内。
实例：交换机配置SPAN镜像出口全量流量至Snort服务器做审计。
答题话术：NIDS旁路采集全网流量，仅生成告警、日志，可联动防火墙阻断攻击，自身不能丢弃数据包。

### 卡片2 预处理器必考考点
理论：预处理器负责分片重组、协议解码，抵御分片、字符编码等流量规避手段；规则匹配功能属于检测引擎。
实例：攻击者拆分超大ICMP分片绕过特征匹配，预处理器重组完整载荷后规则正常触发。
答题话术：IP分片重组、HTTP协议解码由预处理器实现，用于对抗流量规避；数据包特征匹配由检测引擎完成。

### 卡片3 sid编号规范
理论：sid是规则唯一标识，配置重复sid会导致Snort校验失败、无法启动；百万以上编号分配给用户自定义规则。
实例：自行编写的检测规则统一使用1000000以上sid，不和官方内置规则冲突。
答题话术：sid全局唯一，用户自定义规则sid推荐大于1000000，sid重复会造成Snort启动报错。

### 卡片4 content大小写陷阱
理论：content关键字默认严格区分大小写，nocase参数可取消大小写匹配限制。
实例：content:"<script>"仅匹配小写，添加nocase后Script、SCRIPT均可命中。
答题话术：content原生区分大小写，模糊匹配大小写必须增加nocase规则选项。

### 卡片5 IDS与IPS核心区分
理论：IDS旁路部署，仅审计告警；IPS串联链路，可直接drop丢弃恶意数据包。
实例：网络边界串联IPS实时拦截Web注入，内网旁路部署IDS留存攻击日志用于事后溯源。
答题话术：IDS旁路只能告警记录流量，IPS串联在线可实时阻断攻击流量。

## 二、30道选择题标准答案+易错陷阱汇总
1.B 2.C 3.B 4.B 5.A 6.A 7.B 8.A 9.B 10.B
11.B 12.B 13.B 14.C 15.C 16.A 17.B 18.A 19.B 20.B
21.B 22.B 23.B 24.B 25.D 26.C 27.A 28.D 29.B 30.C
### 高频错题陷阱
1. IDS固有缺陷：旁路镜像模式无法直接丢弃攻击包，仅能联动防火墙二次阻断
2. 规则语法要求：规则选项括号内每条参数必须以分号结尾
3. offset/depth顺序：先offset指定起始字节，再depth限制检索长度
4. 纯SYN报文无应用载荷，无法匹配content字符串特征
5. 签名库定期更新原因：新型攻击特征持续出现，旧规则无法覆盖0day、变种攻击

## 三、下午案例题标准采分答案
### （1）检测SYN端口扫描规则
alert tcp any any -> any any (msg:"TCP SYN端口扫描";flags:S;sid:1000020;rev:1;)
得分点：flags:S、alert动作、规范sid与rev版本

### （2）检测忽略大小写SQL注入规则
alert tcp any any -> any 80 (msg:"SQL注入攻击检测";content:"' OR";nocase;sid:1000021;rev:1;)
得分点：特征"' OR"、nocase参数、锁定80端口Web流量

### （3）DNS隧道攻击判定+阻断命令
攻击类型：DNS隧道隐蔽外传数据
阻断命令：iptables -A OUTPUT -s 内网主机IP -p udp --dport 53 -j DROP

### （4）HTTPS加密流量无法检测简答
原因：SSL/TLS加密全部应用层载荷，旁路NIDS无解密密钥，仅能读取网络层头部，看不到明文攻击特征。
解决方案：
1. 边界部署SSL解密代理，解密流量后交由IDS检测
2. 服务器部署HIDS，审计主机本地进程、文件、流量行为
3. 部署NTA流量分析系统，依靠流量行为特征识别异常外联

## 四、轮回速记复习卡
### 往期复习
轮回14：BLP保密模型不上读不下写；Biba完整性不下读不上写；iptables白名单顺序：回环→已建立连接→业务端口放行
轮回15：IDS旁路仅告警、IPS串联可阻断；签名检测识别已知攻击，异常检测识别0day；DNS隧道流量走UDP53，数据藏子域名
### 今日新增背诵点
1. Snort三大运行参数：-v嗅探、-l日志存储、-c加载规则配置
2. pass代表白名单放行流量，sid编号不可重复
3. 预处理器核心功能：分片重组，抵御流量规避攻击

## 五、今日自检清单
□ Snort三种运行模式命令区分清晰
□ 规则完整结构（规则头+选项）可独立默写
□ content、nocase、sid、flags、offset核心选项作用熟记
□ sid编号分段规范、重复sid故障后果牢记
□ IDS旁路无法直接丢弃流量，IPS串联可实时阻断
□ DNS隧道攻击识别与iptables应急封禁命令
□ HTTPS加密流量NIDS检测局限性与三种解决方案
