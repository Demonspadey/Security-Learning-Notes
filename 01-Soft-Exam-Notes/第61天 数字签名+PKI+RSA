## 一、核心目标
1. 熟记单选高频考点，精准区分数字签名与加密、PKI各组件职责
2. 掌握RSA计算题标准答题步骤，拿下下午案例计算分
3. 梳理简答模板，覆盖数字签名、PKI组件、证书管理核心题型

---
## 二、单选高频考点（必背）
### 考点1 数字签名核心特性
- 三大作用：真实性、完整性、不可否认性
- 核心易错点：不提供机密性，不能替代数据加密
- 密钥规则：私钥签名，公钥验签

### 考点2 RSA参数公私划分
- 公开参数：n（模数）、e（加密指数）
- 保密参数：p、q（大质数）、φ(n)（欧拉函数）、d（私钥指数）
- 安全基础：大整数质因数分解难题

### 考点3 PKI组件职责
- 核心信任根：CA（证书签发、吊销）
- 身份初审：RA（无签发权限）
- 证书吊销清单：CRL（批量离线校验）
- 证书存储查询：LDAP
- 实时证书查询：OCSP（在线单张查询）

### 考点4 证书管理
- 证书核心：绑定公钥与身份
- 私钥泄露处理：立即向CA申请吊销证书
- 证书失效两种情况：自然到期、主动吊销（CRL标记）

---
## 三、简答/计算题标准答题模板
### 模板1 简述数字签名的核心作用与流程
作答：
数字签名基于非对称加密技术实现，核心作用包括：
1. **身份真实性**：验证发送方身份，防止假冒；
2. **数据完整性**：检测数据是否被篡改；
3. **不可否认性**：防止发送方事后抵赖行为。
执行流程：发送方先对原文生成哈希摘要，再用自身私钥加密摘要生成签名；接收方用发送方公钥解密签名得到原哈希，同时对原文重新计算哈希，二者一致则验签通过。

### 模板2 简述RSA密钥生成流程及公开/保密参数
作答：
1. 密钥生成步骤：
   ① 选取两个大质数p、q；
   ② 计算模数 n = p × q；
   ③ 计算欧拉函数 φ(n) = (p-1)(q-1)；
   ④ 选取与φ(n)互质的整数e作为公钥指数；
   ⑤ 计算e关于φ(n)的模逆元d，作为私钥指数。
2. 公开参数：n、e，构成公钥对外发布；
3. 保密参数：p、q、φ(n)、d，必须严格保管。

### 模板3 简述CA与RA的职责区别
作答：
1. CA（证书认证机构）是PKI的核心信任根，负责数字证书的最终签发、管理、吊销全生命周期，是信任的源头。
2. RA（注册机构）是CA的前端延伸，仅负责用户身份信息初审、证书申请受理与材料审核，不具备证书签发权限。

### 模板4 简述CRL与OCSP的区别
作答：
1. CRL是证书撤销列表，由CA定期发布，适合批量离线校验证书有效性，实时性较弱。
2. OCSP是在线证书状态协议，支持单张证书实时在线查询，实时性强，适合高频校验场景。

### 计算题标准答题格式（RSA签名/加密）
**示例：已知p=5，q=11，e=3，计算明文M=8的签名值S**
解：
1. 计算模数：n = p × q = 5 × 11 = 55
2. 计算欧拉函数：φ(n) = (p-1)(q-1) = 4 × 10 = 40
3. 计算私钥d：e × d ≡ 1 mod φ(n)，解得 d = 27
4. 签名公式：S = M^d mod n
5. 快速幂计算：
   8^1 mod 55 = 8；8^2 mod 55 = 9；8^4 mod 55 = 26；8^8 mod 55 = 16；8^16 mod 55 = 36
   S = 8^27 mod 55 = (36×16×9×8) mod 55 = 2
6. 最终签名值 S = 2

---
## 四、易错题坑点汇总
1. 功能边界坑：数字签名无机密性，加密无不可否认性，二者不可混淆
2. 密钥使用坑：加密用对方公钥，签名用己方私钥，顺序与主体不可记反
3. 组件权责坑：RA只有初审权，无签发权；CA才是签发主体
4. 计算细节坑：模幂运算每一步都要取模，避免大数计算算术失误
5. 证书失效坑：过期是自然失效，吊销是有效期内主动作废；吊销的证书需要CRL标记
