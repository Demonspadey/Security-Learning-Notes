## 一、核心目标
1.  熟记CSRF四大防御机制的核心考点与软考标准简答模板
2.  掌握Token、SameSite、Referer的原理、优缺点与适用场景
3.  规范CSRF防御相关选择题、判断题、案例题的作答思路

---

## 二、必背考点卡片
1.  CSRF首选防御：**CSRF Token校验**，核心是随机、唯一、不可预测、与会话绑定
2.  SameSite三模式：Strict（完全禁止）、Lax（均衡模式，推荐）、None（无防御）
3.  Referer校验缺陷：可被伪造、可被清空，仅能作为辅助防护
4.  企业落地优先级：Token > SameSite > Referer > 验证码
5.  验证码作用：阻断自动化伪造请求，作为高风险操作的兜底防护

---

## 三、标准简答模板
### 题目1：简述CSRF漏洞的四种标准防护措施
> 标准答案：
> 1. CSRF Token校验：服务端生成随机、唯一、不可预测的令牌，请求必须携带并校验有效性；
> 2. 配置Cookie SameSite属性：设置Lax/Strict模式，限制Cookie跨站自动携带；
> 3. Referer来源校验：验证请求来源域名合法性，拦截非法跨域请求；
> 4. 高危操作二次验证：改密、转账、提现等高风险操作增加验证码/登录密码确认。

### 题目2：简述CSRF Token防御机制的实现流程与安全优势
> 标准答案：
> 1. 实现流程：用户登录时，服务端生成与会话绑定的随机Token，下发到前端表单/请求头中；前端请求必须携带该Token，
     服务端校验一致性，校验不通过则拦截请求。
> 2. 安全优势：攻击者无法预测或伪造合法Token，可防御所有类型CSRF攻击，绕过难度极高，是目前最主流、最安全的防御方案。

### 题目3：简述Cookie SameSite三个属性的区别与适用场景
> 标准答案：
> - Strict：最严格，完全禁止跨站携带Cookie，防御最强，但兼容性差；
> - Lax：均衡模式，允许安全GET请求跨站携带Cookie，禁止危险请求，兼顾安全与兼容，为企业推荐配置；
> - None：关闭限制，无CSRF防御能力，需配合HTTPS+Secure使用。

---

## 四、模拟练习题（附你的作答批改）

### 1. 单项选择题
1.  目前防御CSRF漏洞最主流、最安全、企业首选的核心防护手段是（）
    A. Referer来源校验
    B. CSRF Token随机令牌校验
    C. Cookie SameSite属性配置
    D. 登录验证码校验
    ✅ 你的答案：B（正确）

2.  下列关于CSRF Token防御原理说法正确的是（）
    A. Token用于加密用户Cookie，防止被窃取
    B. 服务端生成随机不可预测Token，请求必须携带并校验通过
    C. Token仅需前端生成，服务端无需校验
    D. Token和会话Cookie无关，可全局共用
    ✅ 你的答案：B（正确）

3.  Cookie SameSite属性中，最严格、完全禁止跨站携带Cookie的模式是（）
    A. Strict
    B. Lax
    C. None
    D. Default
    ✅ 你的答案：A（正确）

4.  SameSite=Lax模式的特点是（）
    A. 完全禁止跨站携带Cookie，兼容性差
    B. 允许安全GET请求跨站携带Cookie，禁止POST等危险请求，兼顾安全与兼容
    C. 任何情况都允许跨站携带Cookie，无防御效果
    D. 仅对HTTPS站点生效，HTTP站点无效
    ✅ 你的答案：B（正确）

5.  Referer校验防御CSRF的最大缺陷是（）
    A. 实现复杂、开发成本极高
    B. Referer可被伪造、可被清空，无法完全信任
    C. 会阻止正常用户访问，影响业务
    D. 只能防御POST请求，无法防御GET请求
    ✅ 你的答案：B（正确）

6.  高风险操作（改密、转账、删除数据）适合采用的CSRF兜底防护是（）
    A. Referer校验
    B. 验证码/登录密码二次校验
    C. 前端JS过滤
    D. 增加页面访问权限
    ✅ 你的答案：B（正确）

---

### 2. 判断题
1.  CSRF Token必须具备随机、唯一、不可预测、与会话绑定的特性，否则可被绕过。（✅ 你的答案：对）
2.  SameSite=None 模式可以有效防御CSRF，是推荐配置。（✅ 你的答案：错）
3.  Referer校验可以完全防御CSRF漏洞，不会被绕过。（✅ 你的答案：错）
4.  Token校验防御CSRF的核心是：让攻击者无法伪造、预测合法请求参数。（✅ 你的答案：对）
5.  验证码属于强交互防护，能彻底阻断自动化CSRF伪造请求。（❌ 你的答案：错，正确答案：对）
6.  企业部署CSRF防御的常规优先级：Token > SameSite > Referer > 验证码。（✅ 你的答案：对）

---

### 3. 场景案例分析题
#### 场景描述
某电商网站用户中心支持修改登录密码、修改收货地址、余额提现功能，用户登录后会话Cookie有效。系统仅对部分接口做了简单Referer校验，未配置SameSite属性，未部署CSRF Token，余额提现与改密接口均无验证码二次验证。攻击者构造恶意页面，诱导已登录用户访问，成功跨站伪造请求，批量修改用户密码并窃取账户余额。

#### 你的满分作答
1.  **网站CSRF防护缺陷**：
    该网站存在无csrf token校验，未配置samesite属性，未对转账，修改密码，删除数据高危操作做二次验证，
    仅referer校验可被伪造。
    ✅ 批改：缺陷点全覆盖，表述精准，贴合软考采分点。

2.  **四种防御机制原理**：
    - token防御原理:服务端生成随机，唯一，不可预测的token，嵌入请求做比对。
    - samesite防御原理:仅当前登录携带cookie，禁止跨源自动携带会话cookie。
    - referer防御原理:验证请求来源的合法可信。
    - 验证码防御原理:对转账，修改密码，删除数据等高风险操作做二次验证。
    ✅ 批改：每种原理都答到核心关键点，表述规范、逻辑清晰。

3.  **企业加固优先级方案**：
    优先做csrf token校验，攻击者无法预测会话令牌。其次配置samesite属性，防止跨源自动携带cookie，
    然后校验referer来源，验证访问域名合法可信来源。最后设置二次验证，对转账，修改密码高风险操作执行短信，
    验证码二次校验。
    ✅ 批改：优先级顺序正确，每步作用解释到位，符合企业落地逻辑。

4.  **仅Referer无法防御的两点原因**：
    1. 仅依靠referer校验，攻击者可以伪造referer来源绕过防御
    2. referer头可能缺失或被移除。
    ✅ 批改：两点都是软考标准标准答案，直击Referer校验的局限性。
