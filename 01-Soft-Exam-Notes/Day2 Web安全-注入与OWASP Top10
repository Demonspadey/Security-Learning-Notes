# Web安全-注入与OWASP Top10

## 1. 核心考点清单
- 注入攻击（SQL/命令/NoSQL/XXE）
- 失效的身份认证
- 敏感数据暴露
- 安全配置错误
- XSS跨站脚本

## 2. 高频考点：SQL注入
- **成因**：用户输入未严格过滤或校验，直接拼接到SQL语句，导致恶意语句被执行。
- **危害**：窃取/篡改数据、执行系统命令、拿下服务器权限。
- **防御措施（必背）**：
  1.  预编译语句 + 参数化查询（最有效）
  2.  数据库账号最小权限原则
  3.  输入白名单校验（如ID只允许数字）
  4.  部署WAF过滤恶意请求

## 3. 简答题模板
**问：SQL注入为什么是OWASP Top10的头号风险？**
- 利用门槛低：只要存在可控输入+SQL拼接即可利用，无需复杂漏洞链。
- 危害极大：可直接窃取用户数据、篡改业务数据，甚至控制服务器。
- 防御易被绕过：很多开发依赖“过滤单引号”等浅层次防护，容易被绕过。
- 影响范围广：几乎所有使用数据库的Web应用都可能受影响。
