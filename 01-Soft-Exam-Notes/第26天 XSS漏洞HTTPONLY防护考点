## 一、核心目标
1.  熟记五类XSS防护手段的工作原理与适用场景
2.  掌握软考标准简答模板，案例题可直接套用
3.  规范XSS防护相关选择题与判断题的作答思路

---

## 二、必背考点卡片
1.  XSS最核心、最通用的防护手段：**输出层HTML/JS实体编码转义**
2.  输入防护原则：**白名单校验优先级远高于黑名单过滤**
3.  HttpOnly：禁止JS读取Cookie，防范会话劫持，但无法阻止脚本执行
4.  CSP：限制脚本、资源加载来源，禁止非法内联脚本执行
5.  防护缺陷：仅前端过滤、仅黑名单、无转义、无HttpOnly、无CSP均为重大漏洞

---

## 三、标准简答模板
**题目：简述XSS漏洞常用防护措施**
> 标准答案：
> 1. 输入检测过滤：对用户可控输入做白名单校验，过滤危险字符与恶意标签；
> 2. 输出编码转义：页面回显时对HTML/JS特殊字符实体转义，避免脚本解析执行；
> 3. Cookie安全配置：设置HttpOnly属性，防止JS窃取Cookie与会话劫持；
> 4. 部署CSP策略：限制资源加载来源，禁止非法内联脚本执行；
> 5. 安全开发规范：使用安全框架，避免危险DOM操作，规范前端渲染。

---

## 四、模拟练习题（附你的作答批改）

### 1. 单项选择题
1.  防御XSS最通用、最基础、最核心的前端渲染层防护手段是（）
    A. 输入黑名单过滤
    B. 输出特殊字符HTML实体转义
    C. 部署WAF
    D. 增加验证码
    ✅ 你的答案：B（正确）

2.  为Cookie设置HttpOnly属性，主要防御XSS的哪种危害（）
    A. 网页篡改
    B. 会话Cookie窃取、劫持账号
    C. 页面挂马
    D. 前端弹窗广告
    ✅ 你的答案：B（正确）

3.  下列关于HttpOnly说法正确的是（）
    A. 设置HttpOnly后可以完全阻止所有XSS攻击
    B. HttpOnly可以禁止JS读取Cookie，但无法阻止脚本执行
    C. HttpOnly属于输入层防护
    D. HttpOnly可以防御SQL注入漏洞
    ✅ 你的答案：B（正确）

4.  CSP（内容安全策略）的核心防护作用是（）
    A. 过滤用户输入的恶意字符
    B. 限制页面脚本、资源加载来源，禁止非法内联脚本执行
    C. 对输出内容做编码转义
    D. 加密用户Cookie信息
    ✅ 你的答案：B（正确）

5.  关于XSS输入过滤防护，下列说法正确的是（）
    A. 黑名单过滤可以完全防御XSS，不会被绕过
    B. 白名单校验安全性远高于黑名单过滤
    C. 只过滤script标签就可以防御所有XSS
    D. 输入过滤不需要服务端实现，前端过滤即可
    ✅ 你的答案：B（正确）

6.  攻击者通过大小写混淆、编码变形、标签闭合等方式突破防护，属于（）
    A. XSS防护绕过
    B. SQL注入绕过
    C. CSRF攻击
    D. 文件上传绕过
    ✅ 你的答案：A（正确）

---

### 2. 判断题
1.  输出编码转义是防御XSS漏洞最根本、最通用的安全措施。（✅ 你的答案：对）
2.  输入黑名单过滤规则容易被变形、编码、大小写混淆绕过。（✅ 你的答案：对）
3.  Cookie设置HttpOnly后，JS无法读取Cookie，可彻底阻止XSS漏洞。（✅ 你的答案：错）
4.  CSP策略能够限制内联脚本执行，大幅降低XSS攻击成功率。（✅ 你的答案：对）
5.  只依靠前端过滤就可以彻底防御XSS漏洞，不需要服务端处理。（✅ 你的答案：错）

---

### 3. 场景案例分析题
#### 场景描述
某网站对XSS仅做前端简单黑名单过滤，拦截`<script>`、`alert`关键字；服务端未做任何输出转义；Cookie未设置HttpOnly；未配置任何安全响应头。攻击者通过大小写变形`<ScRiPt>`、编码Payload、闭合HTML标签等方式成功绕过过滤，执行恶意脚本窃取用户登录Cookie，实现会话劫持。

#### 你的满分作答
1.  **防护缺陷分析**：
    - 未做白名单校验机制，仅依赖黑名单过滤，容易被大小写、编码绕过。
    - 仅前端过滤，后端服务器没有进行有效过滤和非法字符转义处理。
    - 未设置HttpOnly，容易被攻击者窃取Cookie和会话劫持。
    - 未设置CSP安全策略，脚本执行容易被解析。
    - 前端输出未做编码转义，脚本可被解析执行。
    ✅ 批改：5点全覆盖，逻辑分层清晰，完全贴合软考采分点。

2.  **HttpOnly的作用及其局限性**：
    HttpOnly可以禁止前端读取Cookie，但无法阻止脚本执行。
    ✅ 批改：表述精准，直击核心考点，满分。

3.  **完整防护方案（五维度）**：
    - 采用输入白名单校验机制，非法字符转义处理。
    - 前端输出执行编码转义，防范脚本解析。
    - 设置HttpOnly，防止Cookie窃取和会话劫持。
    - 部署CSP安全策略，限制脚本解析。
    - 前端安全渲染，遵循安全开发规范，使用安全框架。
