# 第1天 软考学习笔记：密码学基础 高频考点+答题模板
> 学习日期：2026.06.22 | 对应计划：第1天 | 方向：信息安全工程师软考
> 考情：⭐选择高频 | 🔥案例计算必考
---
## 一、核心目标
1. 熟记选择题高频考点，快速区分算法类型与适用场景
2. 掌握RSA计算题标准答题步骤，拿下案例计算分
3. 梳理易错题坑点，规避绝对化表述、概念混淆类失分

---
## 二、单选高频考点（必背）
### 考点1 算法分类匹配
- 对称加密：DES、3DES、AES、SM1、SM4、RC4
- 非对称加密：RSA、ECC、SM2、ElGamal
- 哈希算法：MD5、SHA系列、SM3
- 易错点：SM3是哈希不是加密，SM2是非对称不是对称

### 考点2 密钥长度速记
- DES：有效密钥56位，总长度64位
- 3DES：密钥长度112位/168位
- AES：128/192/256位
- SM4：分组128位，密钥128位
- MD5：输出128位；SHA-1：输出160位；SM3：输出256位

### 考点3 场景选型
- 大文件/大数据加密 → 对称加密
- 密钥交换、数字签名、身份认证 → 非对称加密
- 文件完整性校验、防篡改 → 哈希函数
- 跨网络安全传递对称密钥 → 数字信封

### 考点4 主机基础考点
- net user：查看所有本地用户
- whoami：查看当前登录用户
- eventvwr.msc：事件查看器，系统日志排查
- 账户注册表路径：HKLM\SAM\SAM\Domains\Account\Users
- 隐藏账户特征：用户名以$结尾，net user默认不显示

---
## 三、案例计算题标准答题模板
### 模板 RSA密钥生成与加解密计算
**例题：已知p=11, q=13, e=7，求私钥d，并对明文m=2加密**
**标准答题步骤**：
解：
1. 计算模数n
$$ n = p \times q = 11 \times 13 = 143 $$
2. 计算欧拉函数φ(n)
$$ \varphi(n) = (p-1) \times (q-1) = 10 \times 12 = 120 $$
3. 计算私钥d
根据定义：\( e \times d \equiv 1 \pmod{\varphi(n)} \)
代入得：\( 7d \equiv 1 \pmod{120} \)
试算得：\( 7 \times 103 = 721 \)，\( 721 \mod 120 = 1 \)
因此 \( d = 103 \)
4. 加密计算
加密公式：\( C = M^e \mod n \)
代入得：\( C = 2^7 \mod 143 = 128 \)
5. 验算（可选，避免失误）
解密：\( M = 128^{103} \mod 143 = 2 \)，与原文一致，计算正确。

**踩分说明**：
- 每个公式1分，步骤分占70%，数值算错仅扣少量分数
- 必须标注每个参数的含义，踩分点更清晰

---
## 四、易错题坑点汇总
1. 绝对化表述坑：哈希函数是“抗碰撞”不是“绝对无碰撞”，遇到“一定、绝对”需警惕
2. 密钥长度坑：DES有效密钥是56位，不是64位；64位是总长度，含8位校验位
3. 公式混淆坑：加密用公钥e，解密用私钥d，记反则整题失分
4. 场景混淆坑：数字信封中，公钥加密的是会话密钥，不是业务数据
5. 命令混淆坑：net user看所有用户，whoami看当前用户，功能边界不可混淆
