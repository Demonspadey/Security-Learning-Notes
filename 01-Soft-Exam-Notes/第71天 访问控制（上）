## 一、背诵速记卡片
### 卡片1 访问控制基础
1. 三要素：主体(主动)、客体(被动资源)、访问操作(r/w/x)
2. 安全流程：认证→访问控制(授权)→审计
3. 三层层级：物理门禁、逻辑系统权限、管理审批流程

### 卡片2 三大模型核心区分
1. DAC自主：所有者说了算，Linux chmod，缺点权限可传递扩散
2. MAC强制：系统安全标签管控，SELinux/BLP，高安全涉密场景
3. RBAC角色：权限绑角色，用户分配角色，企业OA/ERP首选，适配最小权限

### 卡片3 Linux权限必背
1. r=4 w=2 x=1；10位字符：类型+所有者+组+其他人
2. SUID(4)所有者s、SGID(2)组s、Sticky(1)其他t
3. umask计算：文件666、目录777，逐位相减，负数取0

### 卡片4 访问控制矩阵两种存储
ACL：按客体（文件）存储；能力表：按主体（用户）存储

### 卡片5 SELinux三种模式
Enforcing强制拦截、Permissive仅日志、Disabled关闭；查询命令getenforce

### 卡片6 BLP模型（MAC保密）
No Read Up（不读高密）、No Write Down（不写低密）

## 二、刷题高频错题陷阱汇总
1. 主体客体混淆：用户/进程是主体，文件/打印机是客体
2. DAC/MAC场景选错：军队涉密选MAC，普通企业自主管理选DAC，办公系统选RBAC
3. umask计算易错：文件默认666不是777，目录才是777
4. SUID位置混淆：所有者位s=setuid，组位s=setgid
5. ACL与能力表存储对象搞反：ACL绑定资源，能力表绑定用户
6. SELinux命令混淆：getenforce查询状态，setenforce修改模式

## 三、案例标准答题模板
### 模板1 场景选型问答
问：单位允许部门负责人自主分配文件权限，选什么模型？
答：DAC自主访问控制，DAC核心特征为资源所有者自主授予访问权限，灵活适配部门自主管理场景。

### 模板2 BLP权限判断答题
问：机密用户能否读取秘密文件、写入公开文件？
答：可读秘密文件，遵循No Read Up（自身密级更高，允许向下读取）；不可写入公开文件，遵循No Write Down，防止高密数据流向低密资源造成泄密。

### 模板3 umask计算答题示例
umask=027
文件权限：666 - 027 = 640(rw-r-----)
目录权限：777 - 027 = 750(rwxr-x---)

### 模板4 SUID风险简答
setuid程序运行时继承文件所有者权限，若程序存在漏洞，普通本地用户可利用漏洞提升权限至root，造成服务器权限泄露、横向渗透风险。

## 四、易混对比速查表
1. DAC vs MAC
DAC：所有者管控、灵活、易权限扩散、普通企业；MAC：系统强制、高安全、配置复杂、涉密军队
2. ACL vs 能力表
ACL：依附文件客体，记录谁能访问；能力表：依附用户主体，记录能访问哪些资源
3. SUID / SGID / Sticky
SUID：程序运行身份改为文件所有者；SGID：目录新建文件继承组；Sticky：目录仅能删除自身文件
4. SELinux三种模式
Enforcing拦截操作；Permissive仅日志；Disabled完全关闭
