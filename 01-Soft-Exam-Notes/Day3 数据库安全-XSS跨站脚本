# 软考信息安全工程师考点笔记：XSS跨站脚本攻击

## 1. 基本概念
- 全称：跨站脚本攻击（Cross Site Scripting，XSS）
- 本质：攻击者将恶意脚本注入网页，由用户浏览器执行
- 核心目标：窃取Cookie、会话劫持、篡改页面、钓鱼挂马等

## 2. XSS三大类型（必考）
| 类型 | 数据流程 | 触发条件 | 危害等级 |
| :--- | :--- | :--- | :--- |
| 反射型XSS（非持久型） | 用户输入 → 服务器反射回页面 → 浏览器执行 | 需用户点击恶意链接，一次性触发 | 中 |
| 存储型XSS（持久型） | 用户输入 → 存入数据库 → 所有访问者触发 | 无需诱导，自动触发 | 高 |
| DOM型XSS | 用户输入 → 前端JS处理 → 修改DOM树 | 浏览器解析前端JS时触发 | 中 | 可反射型也可存储型 |

## 3. XSS危害（简答考点）
1.  窃取用户会话Cookie，实现账号劫持
2.  伪造登录框，骗取账号密码
3.  篡改页面内容，发布虚假信息
4.  挂马、挖矿、控制用户浏览器
5.  结合CSRF执行未授权操作

## 4. XSS防御措施（高频多选/简答）
1.  **输入验证**：白名单限制输入内容、长度、格式
2.  **输出编码**：对特殊字符HTML转义
    - `<` → `&lt;`   `>` → `&gt;`
    - `"` → `&quot;` `'` → `&#x27;`
3.  **HttpOnly属性**：禁止JS读取Cookie，防窃取
4.  **CSP（内容安全策略）**：限制脚本加载源
5.  **禁用危险标签与事件**：script、iframe、onerror等

## 5. 易混淆点对比
- 反射型：一次性、URL携带、危害较小
- 存储型：持久化、数据库存储、危害最大
- DOM型：前端JS渲染、不经过服务器处理
