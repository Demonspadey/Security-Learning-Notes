【场景】有一台服务器的 SSH 账号，
        先做个基本的安全检查。

第①步：先看当前谁在线
──────────────────────────────────────────────────────────────────────────────
  w 或者 who
  # 如果有陌生IP连着的 → 如果工作就问，如果私下就可能出问题了，先断网再查

第②步：看最近谁登录过
──────────────────────────────────────────────────────────────────────────────
  last | head -20
  # 有没有凌晨的登录？
  # 有没有从陌生IP来的登录？
  # 有没有 root 直接从 SSH 登录？

  # 如果看到可疑IP，记录一下，后面可能用到

第③步：检查有没有异常账户
──────────────────────────────────────────────────────────────────────────────
  # 3.1 查有没有 UID=0 的非 root 账户
  awk -F: '$3 == 0 {print $1, "UID=0"}' /etc/passwd
  # 正常只有：root UID=0
  # 多了别的 → 克隆账户/后门！

  # 3.2 查哪些用户能登录系统
  grep "/bin/bash" /etc/passwd
  # 正常只有 root 和 自己
  # 多了不认识的可登录账户 → 后门！

  # 3.3 查哪些账号被锁了
  sudo awk -F: '$2 ~ /^[!*]/ {print $1 " 已锁定"}' /etc/shadow
  正常系统内置的都是锁定的，发现可疑未锁定或者自己账户就有问题了

第④步：检查 SUID 后门
──────────────────────────────────────────────────────────────────────────────
  # 找所有 SUID 文件
  find / -perm -4000 2>/dev/null
  # 正常 SUID 都在 /usr/bin/、/usr/sbin/ 下面
  # 看到 /tmp/ 或 /home/ 下的 SUID → 后门！

第⑤步：看网络连接
──────────────────────────────────────────────────────────────────────────────
  # 看哪些端口在监听
  sudo ss -tlnp
  # 重点关注：
  #   · 22（SSH）→ 正常
  #   · 80/443（Web）→ 如果装了Web服务就正常
  #   · 不认识的端口 → 查一下是什么服务

  # 看已建立的连接（有没有连到陌生IP）
  sudo ss -tnp | grep ESTAB

第⑥步：看计划任务
──────────────────────────────────────────────────────────────────────────────
  # 攻击者常把后门写成定时任务
  crontab -l 2>/dev/null          # 当前用户的计划任务
  sudo crontab -l 2>/dev/null     # root的计划任务
  ls -la /etc/cron.d/             # 系统计划任务
  # 不熟悉的脚本路径 → 查！

第⑦步：写巡检结论
──────────────────────────────────────────────────────────────────────────────
【巡检结果】
服务器IP：xxx
状态：✅ 正常 / ❌ 有问题

① 在线用户：只有自己 → 正常
② 最近登录：无异常 → 正常
③ 异常账户：无 → 正常
④ SUID文件：无异常 → 正常
⑤ 监听端口：22(SSH) → 正常
⑥ 计划任务：无异常 → 正常

Part 2｜模拟一个"被入侵"的场景
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Step 1：加一个隐藏账户
──────────────────────────────────────────────────────────────────────────────
  # /etc/passwd 加一个 UID=0 的后门账户
  echo "sysback:x:0:0:backup:/root:/bin/bash" | sudo tee -a /etc/passwd

Step 2：创建一个 SUID 后门
──────────────────────────────────────────────────────────────────────────────
  echo '#!/bin/bash' | sudo tee /tmp/.sysupdate
  sudo chmod u+s /tmp/.sysupdate

Step 3：在非标准端口开了个服务
──────────────────────────────────────────────────────────────────────────────
  # 用 nc 监听一个端口（10秒后自动退出）
  timeout 10 nc -lvp 4444 & sleep 1

Step 4：用 Part 1 的 7 步查一遍
──────────────────────────────────────────────────────────────────────────────
  awk -F: '$3 == 0 {print $1}' /etc/passwd
  # 看到：root 和 sysback →  异常！

  find / -perm -4000 2>/dev/null | grep -E "/tmp/|/home/"
  # 看到 /tmp/.sysupdate →  异常！

  grep "/bin/bash" /etc/passwd
  # 看到 root，自己和sysback →  异常！

  sudo ss -tlnp | grep 4444
  # 看到 4444 端口在监听 →  异常！

Step 5：清理痕迹
──────────────────────────────────────────────────────────────────────────────
  sudo sed -i '/sysback/d' /etc/passwd
  sudo rm -f /tmp/.sysupdate
  sudo pkill -f "nc -lvp 4444" 2>/dev/null
