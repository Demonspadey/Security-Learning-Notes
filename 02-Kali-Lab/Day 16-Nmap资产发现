**Step 1：资产发现（主机存活探测）**
──────────────────────────────────────────────────────────────────────────────
【Kali 上执行】

  # ping sweep — 发现网段内有哪些主机在线
  sudo nmap -sn 192.168.x.0/24
  # -sn = ping scan，只做主机发现，不扫端口
  # 输出：Nmap done: 256 IP addresses (5 hosts up)
  # 看到 Ubuntu IP 在线

  # 详细输出，显示 MAC 地址和厂商
  sudo nmap -sn 192.168.x.0/24 --reason

**Step 2：全端口扫描 + 版本探测**
──────────────────────────────────────────────────────────────────────────────
  # 扫 Ubuntu 的全部 65535 个端口
  # -p- = all ports
  # -sV = version detection
  # -T4 = timing template
  sudo nmap -p- -sV -T4 <UBUNTU_IP>

  # 把结果保存到文件
  sudo nmap -p- -sV -T4 <UBUNTU_IP> -oN /tmp/nmap_scan.txt
  # -oN = normal output（可读格式）

  cat /tmp/nmap_scan.txt

**Step 3：操作系统识别 + 脚本扫描**
──────────────────────────────────────────────────────────────────────────────
  sudo nmap -O --osscan-guess <UBUNTU_IP>
  # -O = OS detection
  # --osscan-guess = 即使不完全匹配也给出猜测

  # 用 NSE 脚本做安全检测
  sudo nmap --script safe -T4 <UBUNTU_IP>
  # --script safe = 运行所有"安全"类脚本（不会影响业务）

**Step 4：输出扫描报告**
──────────────────────────────────────────────────────────────────────────────
  # nmap 支持多种输出格式
  sudo nmap -p- -sV -O -T4 <UBUNTU_IP> \
    -oN /tmp/report.nmap \
    -oX /tmp/report.xml \
    -oG /tmp/report.grep

  # -oN = 可读文本
  # -oX = XML格式（可导入其他工具）
  # -oG = grepable格式（方便脚本处理）
