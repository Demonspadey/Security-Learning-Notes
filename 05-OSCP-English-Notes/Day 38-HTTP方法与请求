1. request
request — 请求
谐音助记：瑞奎斯特
例句：An HTTP request consists of a method, URL, headers, and body.
单词拆分：
HTTP request HTTP 请求；consist of 由…… 组成；method 请求方法；headers 请求头；body 请求体
整句总结：浏览器 / 客户端发给服务器的数据整体称为 HTTP 请求，由四部分构成。

2. method
method — 方法
谐音助记：麦瑟德
例句：Common HTTP methods include GET, POST, PUT, DELETE, and OPTIONS.
单词拆分：
common 常见的；include 包含
整句总结：HTTP 方法定义客户端想要执行什么动作（查、提交、新增、删除等）。

3. response
response — 响应
谐音助记：瑞斯邦斯
例句：The server response contains status code, headers, and body content.
单词拆分：
server response 服务器响应；status code 状态码；body content 正文内容
整句总结：服务器收到请求后，返回给客户端的数据称为响应。

4. payload
payload — 载荷、攻击载荷
谐音助记：佩洛德
例句：The exploit payload is sent to the target to trigger the vulnerability.
单词拆分：
exploit 漏洞利用；trigger the vulnerability 触发漏洞
整句总结：用来实现攻击效果的核心代码 / 指令，单纯用来触发漏洞、执行命令。

5. cookie
cookie — Cookie、会话标识
谐音助记：库奇
例句：Cookies are used to maintain session state and may contain sensitive tokens.
单词拆分：
maintain session state 维持会话状态；sensitive tokens 敏感令牌
整句总结：保存在浏览器的数据，服务器依靠 Cookie 识别用户登录身份，极易被窃取造成会话劫持。

📝 小练习：
① PUT 方法是做什么用的？  修改
② payload 和 request 的区别是什么？  payload是攻击载荷，request是请求
③ 填空：Cookies are used to maintain ___session__ state.
④ 判断：OPTIONS 方法可以查看服务器支持哪些 HTTP 方法。（对/错）  对
⑤ 翻译：The exploit payload triggers the vulnerability.  漏洞利用载荷除法该漏洞
